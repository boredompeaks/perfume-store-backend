# Section 17 — Security specification (task ledger)

Created 2026-09-19 from the S17 compliance prefetch. Spec lines: 4239–4426. Prefetch result: 36 rows — 19 IMPLEMENTED / 11 PARTIAL / 1 MISSING / 2 DEVIATES / 3 N-A (verified: SSRF — no outbound-URL surface; marketing consent — no marketing surface; jurisdiction evaluation — legal process, not code).

| Task ID | Section | Requirement summary | Spec lines | Status | Attempts |
|---|---|---|---|---|---|
| SPEC-17-01 | 17 | P2 — JWT lifecycle [R-17.5/17.8/17.10]: add SIMPLE_JWT config (lifetimes, ROTATE_REFRESH_TOKENS, BLACKLIST_AFTER_ROTATION + blacklist app), a token-blacklisting logout endpoint, and a rotation hook so password reset/email change invalidate outstanding tokens (accounts/urls.py, accounts/views.py, config/settings.py, requirements, tests) | 4239–4426 [R-17.5],[R-17.8],[R-17.10] | PENDING (P2 build-now; matches BACKEND_REQUESTS.md:36-41 contract) | 0 |
| SPEC-17-02 | 17 | P2 — DEVIATES [R-17.12]: refresh token persisted in localStorage (frontend/src/lib/tokens.ts:16,34; auth.tsx:57) — move refresh to an HttpOnly backend-set cookie (or documented memory+re-login fallback), keeping the memory-only access token | 4239–4426 [R-17.12] | PENDING (P2 build-now; coordinate with 17-01's cookie work) | 0 |
| SPEC-17-03 | 17 | P2 — DEVIATES [R-17.18]: CSRF unenforced on session-cookie cart/checkout mutations (cart/views.py:24-27; JWT-only DEFAULT_AUTHENTICATION_CLASSES skips DRF CSRF; live-confirmed POST/DELETE with session cookie and no token — BACKEND_REQUESTS.md:43-48). Add SessionAuthentication (auto-CSRF) or issue CSRF cookie + explicit SESSION_COOKIE_SAMESITE, activating the ready frontend X-CSRFToken slot (api.ts:29-39,101-104) | 4239–4426 [R-17.18] | PENDING (P2 build-now — top risk #1) | 0 |
| SPEC-17-05 | 17 | P2 — MISSING [R-17.9]: staff MFA (TOTP) mandatory for privileged roles (admin / staff.manage) at admin + staff login — zero MFA code today; the privilege-escalation-guarded admin remains single-factor | 4239–4426 [R-17.9] | PENDING (P2 build-now) | 0 |
| SPEC-17-06 | 17 | P3 — PARTIAL [R-17.17]: JSON-LD XSS sink — staff-controlled product.name/description embedded via raw JSON.stringify (products/[slug]/page.tsx:91,95 — '</script>' breakout; no \u003c escaping) + no CSP anywhere — escape payloads and add security headers via next.config.ts headers() | 4239–4426 [R-17.17] | PENDING (P3; staff-only write path limits exploitability) | 0 |
| SPEC-17-07 | 17 | P3 — PARTIAL [R-17.11]: zero SECURE_*/HSTS/SESSION_COOKIE_SECURE/CSRF_COOKIE_SECURE settings (implicit Django defaults only); TLS depth deferred to S22 but cookie/transport flags are settable now — env-gated hardening coordinated with S22 | 4239–4426 [R-17.11] | PENDING (P3; coordinate with S22 deployment loop) | 0 |
| SPEC-17-08 | 17 | P3 — PARTIAL [R-17.21]: no file-size validation on product image uploads (products/models.py:23-27; Pillow content-type only) — form-level max size + documented limits | 4239–4426 [R-17.21] | PENDING (P3) | 0 |
| SPEC-17-09 | 17 | P3 — PARTIAL [R-17.29/31/32/33]: retention/erasure — documented field-purpose table, deletion-request flow beyond manual email, explicit decision note on usernames in the audit mirror logger (common/models.py:52-53 pattern; accounts/views.py:56) | 4239–4426 [R-17.29],[R-17.31],[R-17.32],[R-17.33] | PENDING (P3) | 0 |
| SPEC-17-10 | 17 | P3 — Convention finding: ops dashboard (ops/views.py:41) uses blanket @staff_member_required — any staff role sees revenue+customer rows; sibling route correctly uses capability_required. Gate by capability_required("reports.read") | conventions finding 1 (S17 prefetch) | PENDING (P3; RBAC least-privilege alignment, SPEC-6-04 pattern) | 0 |

Owner attributions (no duplicate tasks):
- R-17.13/17.19/17.26 (RBAC, access control, privilege-escalation protection) → **SPEC-6-03a/b/c + SPEC-6-04** (delivered, verified).
- R-17.23 webhook spoofing (server-to-server Razorpay webhook listener with RAZORPAY_WEBHOOK_SECRET HMAC + idempotent confirm/refund handler) → **SPEC-1-06** (ledgered webhook owner; orchestrator-deduped from the S17 prefetch's proposed SPEC-17-04 — the S17 handoff omitted that cross-ref). Properties the webhook must carry: HMAC verification, replay/idempotency (unique payment-id upsert semantics per R-11.18), no duplicate fulfilment dispatch (R-11.16), /api/v1/ namespace per SPEC-9-02.
- R-17.20 abuse monitoring stream → **SPEC-7-01** (delivered: AuditEvent login-failure trail).
- V-02 fail-open DEBUG (config/settings.py:34, pinned tests/test_settings_security.py:39-50 with flip instruction) → **P2, owned by the S17 build loop itself** (flip + invert pin when S17 queue opens).
- SPEC-17-04 ID: NOT used (deduped to SPEC-1-06); IDs 17-06..17-10 assigned as above.

Top risks (severity-ordered, from prefetch): 1) CSRF unenforced on cookie-cart mutations (→17-03); 2) non-revocable 24h refresh token in localStorage, zero rotation/blacklist (→17-01/17-02); 3) DEBUG fails open (→S17 loop, V-02); 4) no staff MFA (→17-05); 5) no transport/cookie hardening (→17-07); 6) JSON-LD XSS sink (→17-06, P3); 7) no upload size cap (→17-08, P3).

Verified strong: deny-by-default capability permissions + ownership scoping + IDOR/isolation test suite; throttled auth/recovery/coupon/cart; validate_password + single-use reset tokens (test-pinned); HMAC-only payment verification; ORM-only queries (zero raw SQL); secrets env-only with CI scans + no-secrets log pin; minimal PII collection; no card data stored.
