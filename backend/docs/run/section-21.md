# Section 21 — Testing specification (task ledger)

Created 2026-09-19 from the S21 compliance prefetch. Spec lines: 4854–4937. Prefetch result: 21 rows — 12 IMPLEMENTED / 7 PARTIAL / 1 MISSING / 0 DEVIATES / 1 N-A (refund: feature deliberately unbuilt, V-03 — the charged-but-unfulfillable contract is pinned instead).

| Task ID | Section | Requirement summary | Spec lines | Status | Attempts |
|---|---|---|---|---|---|
| SPEC-21-1 | 21 | P2 — PARTIAL [R-21.2.6 orders half]: duplicate checkout submissions currently create two pending orders (orders/views.py:52-93 has no dedup guard) AND nothing pins either behavior (test_e2e_concurrency.py:29-35 asserts two 201s) — deliver a server-side dedup decision or, minimally, a deliberate contract pin; the two-201 assertion must be updated deliberately in the same commit | 4854–4937 [R-21.2.6] | IN-AUDIT (commit 3391d93: DEDUP GUARD chosen (preferred option) inside create_order's atomic block — cart row select_for_update as same-session serialization point, _find_duplicate_pending_order matches user's recent payable pending order (env CHECKOUT_DEDUP_WINDOW_SECONDS=300, payable gate mirrors verify's already-processed check) with identical fingerprint (sorted product/qty lines + coupon + 6 shipping fields) → replay 200 with INFO log, no second order/trail row; deliberate difference → fresh 201; two-201 assertion updated deliberately w/ contract comment; cross-session same-user duplicates stay SPEC-9-01 header-keyed scope; verify/stock/RBAC/lock-order untouched; 395 tests = 391 pass + 4 xfails, cov 100.00% @ 1665 stmts, makemigrations --check clean; 2 extra files disclosed: config/settings.py + .env.example for the env window — conventions-mandated) | 1 |
| SPEC-21-2 | 21 | P2 — PARTIAL [R-21.1.4 CSRF/XSS slice]: CSRF middleware configured but ZERO CSRF-enforcement tests; no XSS tests anywhere — pin CSRF enforcement on session-cookie-authenticated mutations (cart/checkout session flows) and one XSS-output assertion for rendered product/user content | 4854–4937 [R-21.1.4] | PENDING (P2 — PULL-FORWARD, cheap; pairs with SPEC-17-03's enforcement work; ≤4 files incl. new backend/tests/test_csrf_security.py) | 0 |
| SPEC-21-3 | 21 | P3 — PARTIAL [R-21.1.6]: no DB-restore or incident-recovery drill — scripted restore drill + incident-recovery checklist documenting the manual-reconciliation contract already pinned in orders/admin.py:28 (≤3 files, docs + one ops script) | 4854–4937 [R-21.1.6] | PENDING (P3 — ride the S21 loop; failed-jobs sub-part N-A: no async jobs exist) | 0 |
| SPEC-21-4 | 21 | P3 — doc hygiene: backend/docs/test-gaps.md:3,17 stale counts (178 tests/8 xf → 378/4) + frontend/vitest.config.ts:6 include glob narrows to *.test.ts — widen to *.{ts,tsx} so future component tests aren't silently dropped (≤2 files) | 4854–4937 + conventions | PENDING (P3 — ride the S21 loop) | 0 |

S21 loop queue (when the section opens): SPEC-2-09 (load tooling — locust/k6 covering catalogue/search/checkout contention/admin reporting; Owner: S21), SPEC-3-01 (hermetic e2e — the headline), SPEC-4-01 (ownership/order-detail pin), SPEC-21-3, SPEC-21-4. Pull-forward: SPEC-21-1, SPEC-21-2.

Owner attributions (no duplicate tasks): R-21.1.3/21.2.1/21.2.15 browser legs → SPEC-3-01; R-21.2.2/21.2.13 ownership pins → SPEC-4-01 (F-18 flip); R-21.2.7 webhook duplicate-effects → SPEC-1-06; R-21.1.5 load layer → SPEC-2-09; R-21.2.12 order-email surface → SPEC-1-12/SPEC-3-14/SPEC-19-1.

Verified strong: shared hermetic base common/testing.py (mocked Razorpay, locmem email, dummy keys, cache resets); exact static agreement with the 378-def/4-pin floor; CI gates backend (fail_under=90 wired) + frontend + secret-scan; server-owned pricing, coupon caps/races, oversell lock, verify idempotency, RBAC matrices all pinned with file:line evidence.
