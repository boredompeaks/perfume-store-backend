# Section 7 — Backend architecture (task ledger)

Migrated verbatim from the monolithic ledger 2026-09-19. Spec lines: 2263–2453. Compliance prefetch 2026-09-18: 4 IMPLEMENTED / 12 PARTIAL / 4 MISSING / 0 DEVIATES.

| Task ID | Section | Requirement summary | Spec lines | Status | Attempts |
|---|---|---|---|---|---|
| SPEC-7-01 | 7 | Audit trail: append-only audit-log model + write hooks for order/payment/auth events (reuse StockMovement pattern) + migration + tests — fulfils the audit-trail half of SPEC-1-11 | 2263–2453 [R-7.20] + S1[1.22] | SHIPPED (audit cycle-1 SHIP @ fac27e4: all 9 audit duties pass - diff scope exact (fac27e4 = 6 declared files; chore 6374ca4 docs-only), changelog truthful and measured independently (356 tests = 350 pass + 6 pre-existing xfails, exit 0; cov 100.00% @ 1573 stmts A+; makemigrations --check clean; common.0002 applies cleanly; initial=True verified as genuine autodetector output for a previously model-less app), direct ORM probes: instance update refused / instance delete refused / unknown prefix fails loudly, atomicity probed both directions (forced rollback discards trail row, commit persists it; endpoint injected-failure pins: checkout and verify roll back order+stock+coupon+cart+trail together), event correctness verified per branch incl. 429s write nothing / verify reuse emits nothing / re-verify of active account is a no-op; 6-05b LogEntry trail untouched (empty diff), no OrderStatusEvent, no reader surface or admin, no new env keys, stub+hardcode scans clean, 88-col clean on every hand-written line (generated-migration longlines per repo precedent); auditor hardening note (not a defect - disclosed model-level scope is the task contract): queryset .delete()/.update() bypass model guards by Django design, no code path and no admin registration exists; pushed 95626a6..6374ca4) | 1 |
| SPEC-7-02 | 7 | Logging baseline: env-driven LOGGING dict in settings (request errors + payment failures), wired so audit events and verify_payment failures emit — fulfils logging halves of SPEC-1-11/SPEC-2-08 | 2263–2453 + S1[1.22], S2[2.12] | SHIPPED (audit cycle-1 SHIP @ 6595861: all 8 audit duties pass - diff scope exact (6595861 = 6 declared files; chore 5502728 docs-only), changelog truthful and measured independently (377 tests = 371 pass + 6 pre-existing xfails, exit 0; cov 100.00% @ 1607 stmts A+; makemigrations --check clean), whitespace-ignored diff proves the 4-line indent shift is exactly the disclosed try/except wrap and nothing else, both judgment calls ratified by direct probe: record() issues exactly one INSERT with zero added queries (anon + authenticated actor), append-only guards intact (update/delete refused), forced post-record failure inside atomic still rolls the row back, category derivation unchanged; intent wrap: bare raise propagates unswallowed (500 asserted, traceback rides the orders.views record via exc_info, no persistence and no payment.initiated row, order state untouched); LOGGING config probed: garbage/NOTSET LOG_LEVEL falls back to INFO + warning without crashing, file handler only when LOG_FILE set (both ways probed), no hardcoded filesystem paths, no external services, django.request pinned ERROR, common.audit pinned INFO, disable_existing_loggers=False; wiring verified via assertLogs for all 6 verify failure branches (order ref + reason in each) + gateway failure + audit channel + django.request 5xx; no-secrets pin passes (Razorpay key id/secret absent from captured output); stub+hardcode scans clean; 88-col clean on every added code line (black not installed in venv - mechanical length scan); no network in tests, Razorpay still mocked; permission/response/throttle behavior unchanged) | 1 |
| SPEC-7-03 | 7 | Coupon quantize parity: quantize discount Decimals before compare/serialize in preview (orders/views.py:239-241,414-418) + checkout paths — flips pinned expectedFailure F-11 (orders/tests.py:190) | 2263–2453 [R-7.7 conventions finding 1] | SHIPPED (audit cycle-1 SHIP @ 88a542b: all 6 audit duties pass — diff scope exact (88a542b = 4 declared files: common/money.py new + orders/views.py + orders/tests.py + docs/changes.md; chore 20357d0 docs-only), changelog truthful and measured independently (377 tests = 372 pass + 5 expectedFailure, exit 0; cov 100.00% @ 1612 stmts A+; makemigrations --check clean), git diff -w proves the F-11 flip is marker + docstring + orphaned import unittest only — assertEqual byte-identical and now passing for real (30% of 2999.99 = 899.997 raw → 900.00 both paths); quantize applied post-division pre-cap/pre-clamp/pre-store/pre-serialize in BOTH paths (create_order + apply_coupon); grep-verified sole discount-math sites (fixed-type, verify_payment, cart, admin-CSV untouched); probe: quantize_money(899.997)→900.00, HALF_EVEN ties 1.005→1.00 / 1.015→1.02, all money fields on the path max_digits=10/decimal_places=2, Decimal end-to-end no float; no pragma on new code (money.py 4/4 stmts covered), stub+hardcode scans clean, 88-col clean on every added .py line (md prose longlines per repo precedent), no drive-by reformat, no new env keys, permission/atomicity semantics unchanged, no other pin depended on unquantized behavior (full suite green, xf set shrunk only by F-11); common/ placement consistent with shared-kernel precedent (audit/permissions/roles); frontend n/a by paths) | 1 |
| SPEC-7-04 | 7 | Search hardening: env-driven pagination page size (products/views.py:94 hardcoded 2, F-23) + stable default ordering (F-12, flips pinned expectedFailure) in products listing | 2263–2453 [R-7.6 conventions findings 5] SHIPPED (audit cycle-1 SHIP @ f33c05d: all 7 audit duties pass -- diff scope exact (f33c05d = 5 declared files: products/views.py + config/settings.py + .env.example + products/tests.py + docs/changes.md; bracketing chores 3833d76/1fa7e69/be090c4 docs-only), changelog truthful and measured independently (378 tests = 374 pass + 4 expectedFailure -- xf set shrunk only by F-12, exit 0; cov 100.00% @ 1615 stmts A+; makemigrations --check clean; UnorderedObjectListWarning ABSENT from auditor's full run output -- pre-existing ledger anomaly closed), git diff -w proves the F-12 flip is marker + docstring only -- assertion byte-identical (descending ids under identical timestamps) and now passing for real (view-level default order_by('-created_at','-id') is the correct mechanism: the pin prose anticipated Meta.ordering with an ASC id tiebreaker but the pinned assertion demands DESCENDING ids, model changes forbidden by task); behavioral probes on a throwaway in-memory DB: garbage PRODUCTS_PAGE_SIZE=not-a-number -> fail-safe 12, default 12 -> 15 rows = 2 pages, envelope keys unchanged (count/total_pages/current_page/next_page/previous_page/results), page-2 partition complete/non-overlapping with the -id tiebreaker deciding under identical created_at, identical requests x5 -> identical partition (determinism), override 3 -> 5 pages, whitelist orderings (-price/name) still sorted and invalid ordering falls to the new default path without a 500; 3 multi-page tests re-pinned with @override_settings(PRODUCTS_PAGE_SIZE=2), assertions unchanged; new test_page_size_is_env_driven ships with the feature (suite-growth rule: floor 377->378 ratified); stub+hardcode scans clean (sole network-pattern hit = docstring prose 'requests.'), no pragma on new code, 88-col clean on every added line, no drive-by reformat, permission/atomicity semantics unchanged (GET ordering path only, POST atomic block untouched), admin.py untouched (ModelAdmin paginator isolated, admin tests green in full suite), Razorpay still mocked, no network in tests; frontend n/a by paths (no frontend files in diff; grep-verified products/page.tsx:62 + sitemap.ts read envelope keys only, no client-side size hardcode); auditor rulings on builder-logged follow-ups: (a) stale sitemap.ts:7 size-2 comment = docs nit, acceptable-as-logged, (b) whitelist orderings lack tiebreakers for tied keys = pre-existing unchanged behavior, acceptable-as-logged, (c) _env_int passes 0/negative through (PRODUCTS_PAGE_SIZE=0 -> ZeroDivisionError 500 on the listing, probe-verified) = operator-misconfig hardening candidate P3, acceptable-as-logged, keeps the established _env_int pattern intact) | 1 |

Cross-referenced, no new deferred rows: R-7.3→SPEC-1-14 · R-7.5/7.8→SPEC-6-13 · R-7.10→SPEC-3-12 · R-7.12→SPEC-1-05/06/18 · R-7.13→SPEC-6-09 · R-7.14→SPEC-1-07/08 · R-7.15→SPEC-6-12/3-18 · R-7.16→SPEC-1-14 · R-7.17→SPEC-2-03/S19 · R-7.18→SPEC-3-25/S16. Convention findings on touch: slug/uniqueness IntegrityError retry, plural model name, function views — grandfathered, align when touched.
