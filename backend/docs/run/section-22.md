# Section 22 — Deployment and infrastructure (task ledger)

Created 2026-09-19 from the S22 compliance prefetch. Spec lines: 4938–5070. Prefetch result: 39 rows — 5 IMPLEMENTED / 9 PARTIAL / 13 MISSING / 5 DEFERRED / 7 N-A / 0 DEVIATES. Note: the compliance agent's session exposed no write tool — the full long-form table lives in its report; this file carries the registry digest + task queue.

| Task ID | Section | Requirement summary | Spec lines | Status | Attempts |
|---|---|---|---|---|---|
| SPEC-22-01 | 22 | P1 — Deployment deps + prod settings surface: psycopg MISSING from requirements.txt (prod DATABASE_URL crashes at first connection — settings path fully wired + test-pinned), add gunicorn + whitenoise + psycopg, wire DATABASE_URL sslmode→OPTIONS, STATIC_ROOT/collectstatic, flip-ready DEBUG contract | 4938–5070 [R-22.1],[R-22.2],[R-22.37] | PENDING (P1 — lands FIRST in the S22 loop; files: requirements.txt, config/settings.py, .env.example, tests/test_settings_security.py) | 0 |
| SPEC-22-02 | 22 | P1-before-cutover — MISSING [R-22.9/R-22.23]: zero backup config/script/schedule and no rollback/runbook docs — pg_dump/managed-backup schedule, restore drill hookup with SPEC-21-3, rollback procedure, §22.3 checklist as a living doc (deploy-runbook.md + README + one ops script) | 4938–5070 [R-22.9],[R-22.23] | PENDING (data-loss class — P1 before any production cutover) | 0 |
| SPEC-22-03 | 22 | P2 — MISSING: no CD pipeline or staging environment — env-gated deploy workflow + GitHub-environments secret provisioning reusing SPEC-2-10 artifacts (deploy-staging.yml + env examples) | 4938–5070 [R-22.1] | PENDING (P2, rides SPEC-2-10) | 0 |
| SPEC-22-04 | 22 | P2 — MISSING [R-22.13 deploy half]: error tracking/uptime wiring for deploy targets — env-gated sentry-sdk DSN + platform health-check registration on /health/ + alert routing; the metrics themselves ride SPEC-2-08 | 4938–5070 [R-22.13] | PENDING (P2, deploys SPEC-2-08) | 0 |
| SPEC-22-05 | 22 | P3 — verify/fix .gitguardian.yml filename on the DEFAULT branch: spec-comp copy is correct (WITH leading dot, 61b2360); owner's master commit 0afd032 landed it WITHOUT the dot → allowlist silently ineffective (FP noise only; gitleaks backstop unaffected). Owner-only fix (master frozen for agents) | CI config finding (S22 prefetch) | PENDING (P3 — owner action flagged 2026-09-19) | 0 |

S22 loop build order: SPEC-22-01 (P1) → SPEC-2-10 (P1, ledgered: Dockerfile + compose + Procfile/render + release-migrate + .dockerignore — the deployable unit) → SPEC-2-04 (P2, ledgered: storage backend + prod media serving — media silently 404s when DEBUG=false TODAY, correctness blocker for product images) → SPEC-22-02 → SPEC-22-03 → SPEC-22-04 → P3 tail (SPEC-17-07 transport hardening, SPEC-18-22/33 API-origin robots + CDN assetPrefix, SPEC-22-05).

Owner attributions (no duplicate tasks): metrics/monitoring rows R-22.14/24..34 → **SPEC-2-08** (S7 cross S22; + SPEC-19-1 substrate for funnel instrumentation); webhook lag R-22.30 → **SPEC-1-06**; job/worker metrics R-22.33 → **SPEC-2-03**; storage R-22.5 → **SPEC-2-04**; TLS/cookies R-22.6 → **SPEC-17-07**; MFA R-22.19 → **SPEC-17-05**; webhook registration R-22.17 → **SPEC-1-06**; restore drills R-22.10 → **SPEC-21-3**; CDN/robots R-22.16/39 → **SPEC-18-33/18-22**; DEBUG fail-open R-22.21 → **V-02/S17 loop**.

Verified solid (highlights): env-driven settings with production SECRET_KEY guard (settings.py:38-39); DATABASE_URL parser fully test-pinned (test_settings_security.py:73-211); real /health/ with 503 degradation across DB/media/SMTP/Razorpay/stock (ops/tests.py:21-88); migration-drift gate in CI; throttles env-tunable; secret hygiene dual-scanned; zero /debug routes; zero raw SQL.

Convention findings: DEBUG default 'true' fail-open (V-02, owned); media static() helper silently no-ops in prod (SPEC-2-04 correctness); no server/runtime deps + unpinned transitive deps (SPEC-22-01); actions pinned by major tag not SHA (P3 hardening option).
