# Changelog

Running log of repo-level changes. Keep updated with every commit that matters. (Source-code behavior is unchanged as of this entry — audit only.)

## 2026-09-18 — Phase 5 test suite (39 unit + 12 e2e implemented, coverage gate live)

Per `docs/test-gaps.md` and `docs/fix-plan.md` Phase 5. Final state: **178 tests, all passing, 100.00% coverage** (`coverage report` gate at `fail_under = 90`).
- `common/testing.py` — shared test base (`ApiTestCase`): locmem email, dummy Razorpay keys (`rzp_test_TESTINGONLY…`), MD5 hasher, temp `MEDIA_ROOT` (test uploads never touch the real `media/`), JSON-capable DRF client, factories + a `razorpay_mock` helper. **No test can hit the network or the real keys from `.env`** (V-01 containment).
- `accounts/tests.py` (35), `products/tests.py` (35), `cart/tests.py` (18), `orders/tests.py` (45), `ops/tests.py` (17) — the enumerated unit tests plus security extras (cross-user token reuse, ownership scoping, ordering-whitelist injection probes).
- `tests/` package (28) — the 12 e2e scenarios (happy path, verification gate, reset-via-outbox, multi-user isolation, oversell & coupon races, product lifecycle snapshots, cart persistence, pagination stability, admin status machine) + V-02 settings subprocess tests + admin-surface smoke.
- Razorpay is always mocked (`unittest.mock.patch('orders.views.razorpay.Client')`); race tests resolve through the verify-time row locks deterministically.
- 8 flip tests marked `@expectedFailure` pin the *fixed* behaviour for open findings (V-05 password policy ×2, V-02 DEBUG fail-open, missing-email validation gap, F-11 rounding parity, F-12 default ordering, F-18 cart ownership, first-save slug overflow) — they turn green automatically when the fixes land.
- `.coveragerc` + `coverage==7.16.1` in requirements + `.github/workflows/backend-tests.yml` (check → makemigrations --check → coverage-gated test run).

Bugs found and fixed while writing the tests (all behaviour, no API changes):

- `accounts/views.py` `_get_user(None)` → 500 on missing `uid` (verify-email / password-reset confirm); now 400 per the documented contract.
- `cart/views.py` non-numeric `product_id` → 500 (`ValueError` uncaught); now 404 like an unknown product.
- `accounts/admin.py` double-registered `User` (suite could not even boot); added the standard `admin.site.unregister(User)` swap.
- `products/admin.py` `format_html` without args crashed the changelist whenever a product had stock 0 (`TypeError`); now `mark_safe`.
- `products/admin.py` `prepopulated_fields` on a readonly `slug` crashed every product change page (`KeyError`); removed (the model generates slugs).
- `templates/admin/adjust_stock.html` dropped the `_selected_action` selection on apply — the stock-adjustment action silently did nothing from the UI; selection is now preserved.

## 2026-09-17 / 2026-09-18 — Audit & repo bootstrap

- Created git repo; pushed base to `github.com/boredompeaks/perfume-store-backend` (private).
- Added `.gitignore` (excludes `.env`, `db.sqlite3`, `media/`, `venv/`, caches) — **no secrets in the repo**.
- Added pinned `requirements.txt` (Django 6.1.1, DRF 3.18.1, SimpleJWT 5.5.1, cors-headers 4.9.0, razorpay 2.0.1, python-dotenv 1.2.3, Pillow 12.3.0) — installed and verified in `venv/` (excluded from git).
- Added `.env.example` template.
- Wrote docs suite: `README.md`, `docs/architecture.md`, `docs/conventions.md`, `docs/audit.md` (29 findings, F-01..F-29), `docs/vulnerabilities.md` (21 flagged: 3 critical / 4 high / 8 medium / 6 low), `docs/test-gaps.md` (**0 unit + 0 e2e tests exist; 39 unit + 12 e2e enumerated**), `docs/fix-plan.md` (6 phases), this file.
- Verified existing code WITHOUT modifying it:
  - `manage.py check` — 0 issues
  - `makemigrations --check --dry-run` — no drift
  - `manage.py test` — 0 tests found
  - `pip check` — no conflicts
  - `check --deploy` — 5 security warnings (HSTS, SSL redirect, secret, session/CSRF cookies)
  - E2E smoke (temp script, in-memory DB): **20/20 passed** — full auth flows, staff gates, cart, coupon, checkout, live Razorpay order creation, forged-signature rejection, password reset.
- New findings surfaced at runtime (added to audit): discount rounding drift preview-vs-checkout (F-11), unordered pagination warning (F-12), live Razorpay keys confirmed valid (F-01/V-01 urgency).

## 2026-09-18 — SPEC-1-01 (Section 1) — builder: permission_classes + explicit serializer fields

Per conventions.md:14/:18 (audit F-20, F-24; serializer half of V-19). Product write authorization moved from 4 duplicated inline `is_staff` checks to DRF `permission_classes`; `ProductSerializer` declares explicit `fields`. Public surface unchanged: same endpoints, same staff-only writes, same `403 {"detail": "Administrator access is required."}` body (canary `test_serializer_field_set_is_pinned` still green).
- `common/permissions.py` (new) — `IsAdminUserOrReadOnly`: SAFE_METHODS public, writes require `is_staff` (same gate as `IsAdminUser`). Raises `PermissionDenied` directly so guests keep the legacy 403 instead of DRF's 401 `NotAuthenticated` (JWT authenticator attached, no credentials). A blanket `IsAdminUser` was not usable: it would have locked anonymous catalogue GETs on the multi-method views.
- `products/views.py` — inline `request.user.is_staff` checks removed (was views.py:113/172/198/225); both FBVs decorated `@permission_classes([IsAdminUserOrReadOnly])`. Edge shift: anonymous writes to an unknown slug now answer 403 before the view body (was 404) — permission checks precede view code; no existence leak.
- `products/serializers.py` — `fields = '__all__'` → explicit whitelist of the exact 10 fields the API always exposed (`id, name, slug, description, price, size, stock, category, image, created_at`).
- `products/tests.py` — +8 tests: unit contract of `IsAdminUserOrReadOnly` (message body, SAFE_METHODS anon, staff-only writes via `assertRaises(PermissionDenied)`), API wiring (anonymous reads incl. OPTIONS stay 200, staff PATCH e2e, unknown-slug write → 403), serializer whitelist + drift guard against `products._meta.concrete_fields`.
- Suite: **186 passed** (8 `expectedFailure` flips unchanged), coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-1-02 (Section 1) — builder: throttle scopes on public mutating endpoints + uniform anonymous coupon errors

Per conventions.md "Every public mutating endpoint gets a throttle scope" / "Uniform responses on anonymous flows" (V-04, V-11). Public coupons/cart/auth mutations are now rate-limited and the coupon preview can no longer be used as a code-existence oracle. Coupon `apply_coupon` stays deliberately public (permission semantics unchanged).
- `config/settings.py` — `DEFAULT_THROTTLE_CLASSES = (ScopedRateThrottle,)`: views opt in per endpoint via `throttle_scope`; scope-less views are untouched. `DEFAULT_THROTTLE_RATES` env-driven with defaults: `coupon` 10/min, `cart` 60/min, `auth` 10/min (`THROTTLE_*_RATE` keys documented in `.env.example`).
- `orders/views.py` — `apply_coupon` decorated `@throttle_scope('coupon')`; **uniform anonymous message chosen: `{"error": "Invalid coupon code"}` (400)** for every failure reason (unknown / inactive / not-yet-valid / expired / usage limit / below minimum, replacing six distinct bodies incl. the `minimum_order_amount` detail — uniform for *everyone*, authenticated callers included, so the public preview never validates codes differentially). Cart resolution moved *before* coupon validation: a cartless caller now gets the same `404 {"error": "Cart not found"}` for every code instead of an existence oracle. Authenticated checkout keeps its differentiated messages (pinned contracts unchanged).
- `cart/views.py` — `cart_detail` (POST add) + `cart_item_detail` (PATCH/DELETE) scoped `cart` via new `CartMutationRateThrottle` (ScopedRateThrottle subclass that exempts safe methods, so GET never consumes the mutation budget).
- `accounts/views.py` / `accounts/urls.py` — `register` scoped `auth`; login moved to a thin `LoginView(TokenObtainPairView)` with `throttle_scope = 'auth'` (response contract = TokenObtainPairView's, unchanged). Token refresh left for a follow-up task.
- `common/testing.py` — `ApiTestCase._pre_setup` clears the default cache: DRF throttle history lives in the shared cache for the whole run, so per-test reset makes throttles deterministic regardless of configured rates.
- `orders/tests.py` (+4 net, incl. rewritten `test_coupon_rejections_are_uniform` pinning the exact uniform body), `cart/tests.py` (+3), `accounts/tests.py` (+3) — scope/rate wiring asserted on the view classes, engagement proven with tiny rates (`429` on coupon/cart-add/update, register, login), GET-exemption and rejection-consumes-budget proven. Engagement tests patch `ScopedRateThrottle.THROTTLE_RATES` (DRF binds rates at import, so `override_settings` cannot reach them) — no sleeps, no network.
- Suite: **196 passed** (8 `expectedFailure` flips unchanged), coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-1-03 (Section 1) — builder: registration enforces validate_password (same policy as reset)

Per conventions.md:19 ("Registration and password reset must run `validate_password` — the same policy everywhere"; V-05). `RegisterSerializer` dropped its bare `min_length=8` in favour of Django's shared password validators, so registration and reset can no longer disagree about password strength.
- `accounts/serializers.py` — `password` field lost the `min_length=8` crutch; new object-level `validate()` runs `validate_password(password, user=<transient User built from the submitted username/email>)`. The transient user is what makes `UserAttributeSimilarityValidator` compare against the username/email being registered — the same user-aware call the reset path (`accounts/views.py:175`) makes against the target account. MinimumLength (8), Common and Numeric validators all come from `AUTH_PASSWORD_VALIDATORS` (`config/settings.py`), so both flows enforce one policy with zero duplicated configuration.
- Error contract preserved (no frontend change): rejections raise `serializers.ValidationError({'password': list(error.messages)})` — the identical `{"password": ["message", ...]}` shape reset-confirm returns and `RegisterForm.tsx` renders via `fieldErrors.password.map(...)`. Uniform anonymous flow: no new response shapes, no new enumeration surface (a policy failure reveals nothing about username/email existence).
- `accounts/tests.py` (+2 net — 4 tests now assert the fixed policy: the 2 un-marked V-05 flips + 2 new): the two V-05 `@expectedFailure` flip tests (common password, numeric-only) were un-marked per their own docstrings and now assert the fixed behaviour live, with added message-content assertions; new `test_password_similar_to_submitted_attributes_rejected` (username-similar and email-equal passwords rejected — serializer-level user-awareness) and `test_policy_errors_match_reset_path_shape_and_block_creation` (list-of-strings shape parity with reset-confirm + a rejected password creates no user row and sends no verification email). The remaining `expectedFailure` (missing-email gap) is untouched — that is a different finding.
- Suite: **198 tests, OK (192 pass, 6 `expectedFailure` — the two V-05 flips turned green)**, coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-1-22 (Section 1 follow-up) — builder: throttle remaining public mutating endpoints + the username-available existence oracle

Per conventions.md "Every public mutating endpoint gets a throttle scope" (V-04 follow-up to SPEC-1-02). The six remaining public account routes are now rate-limited, and the public GET username-existence oracle loses its unbounded probe budget. Anonymous response contracts unchanged: the uniform 200 bodies on the recovery flows are pinned by a dedicated test — throttling only adds a 429 refusal.
- Scope choice (documented): `resend-verification`, `forgot-username`, `password-reset` — the three email-sending flows — get a dedicated, tighter `recovery` scope (default **5/min**, env `THROTTLE_RECOVERY_RATE`): each accepted request triggers an outbound email, so the budget *is* the mail-bomb bound. The non-mail mutations `verify-email` and `password-reset/confirm` join the existing `auth` budget (10/min) alongside register/login/token-refresh; their one-time tokens bound replay further. `username-available` (GET) and `token/refresh` are scoped `auth` too — `ScopedRateThrottle` has no safe-method exemption, so the GET oracle is throttled like the rest.
- `accounts/views.py` — `@throttle_scope` added to the six FBVs (`username_available`, `verify_email`, `reset_password` → `auth`; `resend_verification`, `forgot_username`, `request_password_reset` → `recovery`). No view-body changes: uniform anonymous bodies (no existence leaks) byte-identical.
- `accounts/urls.py` — token refresh routed through a thin `RefreshView(TokenRefreshView)` with `throttle_scope = 'auth'` (response contract = SimpleJWT's, unchanged).
- `config/settings.py` / `.env.example` — new env-driven `THROTTLE_RECOVERY_RATE` (default 5/min) documented alongside the other `THROTTLE_*_RATE` keys.
- `accounts/tests.py` (+9 net) — `test_all_account_urlpatterns_declare_throttle_scopes` walks the real URLConf asserting a scope on every route (a future endpoint cannot ship unthrottled silently); 7 engagement tests prove 429 with tiny rates via the established `mock.patch.object(ScopedRateThrottle, "THROTTLE_RATES", ...)` + explicit `cache.clear()` pattern (verify-email, reset-confirm, token-refresh via `RefreshToken.for_user`, the GET oracle, and the three recovery flows — each also asserts the throttled request triggers **no** further email); `test_recovery_throttling_preserves_uniform_bodies` pins the exact 200 bodies of the three recovery flows under a throttled config (known and unknown addresses alike).
- Suite: **207 tests, OK (201 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-5-01 (Section 5) — builder: dashboard aggregate orders, AOV and pending-fulfilment KPIs

Per spec §5.1 dashboard mockup (Orders / Average order value / Pending fulfilment cards). The dashboard previously rendered only per-status cards: no aggregate order count, no AOV anywhere, and its "pending" metric was payment-pending rather than true pending-fulfilment.
- `ops/services.py` — `get_stats()` now also returns `average_order_value` (Decimal end-to-end: paid revenue ÷ paid order count, quantized to `0.01` before serializing; guarded on the paid **count**, so an empty or all-unpaid store yields `0.00` instead of ZeroDivisionError) and `orders_pending_fulfilment` (count of status `confirmed`). "Paid" is exactly the existing gross-sales set `REVENUE_STATUSES = ("confirmed", "shipped", "delivered")` — `verify_payment` flips pending→confirmed at capture — and one aggregate query now yields revenue + paid count together. Pending fulfilment is deliberately only `confirmed` (paid, awaiting shipment): `shipped`/`delivered` have left the warehouse; `pending` is payment-pending. `/health/` untouched.
- `ops/templates/ops/dashboard.html` — three KPI cards added in the existing grid pattern: aggregate **Orders** (all statuses), **Average order value** (₹, subnote "paid revenue ÷ paid orders"), **Pending fulfilment** (subnote "paid, awaiting shipment (confirmed)"). The per-status cards (incl. `Orders · pending`) remain, with an explicit "awaiting payment" subnote so the payment-pending meaning is not silently repurposed. The admin-index `dashboard_cards.html` widget is untouched (additive stats keys keep it working).
- `ops/tests.py` — +4 net tests: AOV correctness on seeded mixed statuses (265.75/3 → "88.58", unpaid + cancelled orders excluded from both sides), zero-paid-orders guard (empty store and unpaid-only store → "0.00", no crash), pending-fulfilment counting only `confirmed` while `pending` stays a separate metric, and a staff dashboard render test asserting all three cards with real numbers plus the retained payment-pending labeling.
- Suite: **211 tests, OK (205 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-5-02 (Section 5) — builder: env-driven low-stock threshold + dashboard N+1 fix

Ops convention conformance (conventions.md env hygiene): the low-stock threshold was a hardcoded module constant and the dashboard resolved each recent-order customer with its own `User.objects.get` (N+1).
- `config/settings.py` — new `LOW_STOCK_THRESHOLD` setting read from the environment with default 5; parsed via a small `_env_int` helper so a malformed env value falls back to the default instead of crashing startup (a bare `int(os.getenv(...))` would 500 the whole app on bad input).
- `ops/services.py` — the hardcoded `LOW_STOCK_THRESHOLD = 5` constant is gone; `get_health()` reads `settings.LOW_STOCK_THRESHOLD` at call time via `_low_stock_threshold()` (not import time, so env changes and test overrides take effect immediately); the payload still echoes the configured threshold, degraded-DB path included.
- `ops/views.py` — the dashboard's per-order `User.objects.get(pk=row["user_id"])` loop is replaced by one batched `User.objects.in_bulk(user_ids)`; a user row that vanishes between `get_stats()` and the fetch degrades to the same "—" placeholder instead of crashing; rendered context keys and values are unchanged.
- `.env.example` — documents `LOW_STOCK_THRESHOLD=5` with its meaning and bad-value behavior.
- `ops/tests.py` — +6 net tests: threshold override reclassifies products on the next `get_health()` call with the echoed value updated; `_env_int` parses valid values and falls back on garbage; the dashboard low-stock table and its "(≤ n)" heading follow the override; recent-orders context shape pinned exactly (stats keys + one resolved username); N+1 regression guard (3 distinct customers ⇒ exactly one batched `auth_user … IN` query); a user vanishing mid-render yields the dash placeholder, not a 500.
- Suite: **217 tests, OK (211 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-5-03 (Section 5) — builder: sales-over-time chart on admin dashboard from real order data

Per spec §5.1 ("Illustrative chart only — actual dashboard must use real order and payment data"). The dashboard had no time-series view at all; this also fulfils the deferred SPEC-1-10 time-series analytics. Server-side render only: no JS, no CDN, admin works offline.
- `ops/services.py` — new `get_sales_series(days=None)`: daily revenue + order counts over a trailing calendar window ending today, over **paid orders only** — the same `REVENUE_STATUSES` set as the gross-sales KPI, so chart and revenue card can never disagree. Query shape: **one** grouped query (`TruncDate("created_at")` → `values("day")` → `Sum("total_amount")` + `Count("id")`, date-range filtered), then Python zero-fills the calendar range so the series has no gaps; ordered oldest → newest as `{date, revenue, orders}` dicts. Decimal end to end, quantized to `0.01` before leaving the function.
- `config/settings.py` + `.env.example` — `DASHBOARD_SALES_WINDOW_DAYS` (default 30) parsed via the existing `_env_int` (malformed values fall back to the default), following the `LOW_STOCK_THRESHOLD` precedent; `get_sales_series` reads it at call time and clamps nonsensical (≤0) values to a single day. An explicit `days=` argument overrides the setting.
- `ops/views.py` — dashboard context gains `sales_series`, `sales_max_revenue` (bar-height scale) and `sales_summary` (one-line accessible name stating the real paid-order count and revenue total); all computed from the same series.
- `ops/templates/ops/dashboard.html` — "Sales over time — Last N days" module between the KPI cards and the health monitor: a CSS bar chart (plain flex divs, height = `{% widthratio day.revenue sales_max_revenue 100 %}%`, grey 3px stubs for zero days, per-bar `title` tooltips), wrapped in `<figure role="img" aria-label="{{ sales_summary }}">` for screen readers, plus a collapsible "View as data table" with every day's exact date/revenue/orders as the non-visual alternative.
- `ops/tests.py` — +9 net tests (`SalesSeriesTests` ×6: gapless default window ordered oldest→newest ending today; per-day revenue/count sums from real paid orders with pending/cancelled/out-of-window money appearing on no day; `days=3` boundary — day-2 in, day-3 out; zero-fill between busy days; env-tunable window with explicit-arg override; ≤0 window clamps to one day. `DashboardSalesChartTests` ×3: chart renders the "Last 30 days" label with real values from the series context while unpaid money stays out; `aria-label` states the real totals ("2 paid orders, total revenue ₹72.00"); the env window drives label and series length together). Staff gating unchanged (no new URL; existing anonymous-redirect test still passes).
- Suite: **226 tests, OK (220 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-2-01 (Section 2) — builder: env-driven DATABASE_URL configuration with sqlite fallback

Per spec §2 database requirement (PostgreSQL for relational data, line ~218): the hardcoded `django.db.backends.sqlite3` DATABASES block becomes environment-driven via `DATABASE_URL`. Deployment tooling (dj-database-url + psycopg) is deliberately deferred to S22, so the URL is parsed with stdlib `urllib.parse` — no new pip dependencies in this task (the suite cannot connect to Postgres anyway).
- `config/settings.py` — new `_database_from_url(url)` helper placed beside the `DATABASES` assignment: `postgres://`/`postgresql://` map to `django.db.backends.postgresql` with NAME/USER (unquoted)/PASSWORD (unquoted)/HOST/PORT taken from the URL parts (each key emitted only when present); `sqlite://` maps to the sqlite engine, with one leading slash stripped so `sqlite:///relative/path` resolves against `BASE_DIR` while the four-slash form (`sqlite:////abs/path`) and a drive prefix (`sqlite:///C:/...`) name an absolute file — the absolute check runs on the raw string because `Path.isabs()` is platform-dependent. Anything missing, empty, malformed (unparseable URL, bad port via the ValueError that `.port` access raises), or of an unsupported scheme (`mysql://`, …) falls back to the previous sqlite dev config (`BASE_DIR / 'db.sqlite3'`) instead of crashing startup — the same fail-safe pattern as `_env_int`. Extra query params (e.g. `sslmode`) are parsed but deliberately ignored until S22 wires SSL options through; documented in `.env.example`.
- `.env.example` — `DATABASE_URL` documented with both example forms (postgres and sqlite), **commented out** so local dev keeps sqlite by default; supported schemes, fallback behaviour, and the ignored-query-params caveat spelled out. No real credentials anywhere.
- `tests/test_settings_security.py` — +18 net tests in two new classes. `DatabaseUrlParsingTests` (×14, pure — the helper is called directly so every parser branch is covered in-process, keeping settings.py at 100.00%: full-field postgres mapping incl. `p%40ss` → `p@ss` unquoting; minimal `postgresql://localhost/appdb` emits only NAME/HOST; socket-style `postgres:///appdb` emits NAME only; missing/empty/no-name/unparseable-IPv6/malformed-port URLs and `mysql://` all return the exact sqlite fallback dict; sqlite relative resolves against `BASE_DIR`, four-slash absolute and drive paths are used verbatim, `sqlite://` with no path falls back). `DatabaseUrlImportTests` (×4, subprocess per the file's existing import-time pattern — missing URL imports with the sqlite default; a postgres URL selects the postgres engine with NAME/USER/HOST/PORT mapped; malformed and unsupported URLs import cleanly with the sqlite fallback, i.e. no exception ever escapes settings import). The shared `run_settings_import` env filter now also strips `DATABASE_URL` from the parent environment so a developer's real `.env` value cannot leak into the hermetic subprocess assertions.
- Suite: **244 tests, OK (238 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (gate 90; settings.py 43 → 78 stmts, all covered), `makemigrations --check` clean.

## 2026-09-18 — SPEC-6-01 (Section 6) — builder: stock gate at order creation

Per spec [6.2.22] ("Publishing should not automatically make an out-of-stock product purchasable unless the store has explicitly configured backorders or preorders"): checkout no longer creates orders containing line items whose current stock is below the requested quantity — previously the customer could create an order and pay, learning of the problem only at payment-verify (409, money captured, no refund path yet). The create-time gate is advisory and read-only; the authoritative decrement stays in `verify_payment`'s atomic block because stock can change again between create and pay.
- `orders/views.py` — `create_order` validates every cart line's quantity against current product stock inside the existing subtotal loop (same `select_related('product')` snapshot, no extra query, no lock, no stock mutation); any insufficient line short-circuits to a 400 before coupon validation and before the `transaction.atomic()` order creation, so a rejection leaves no `Order`/`OrderItem` rows, no cart mutation, and no coupon state change. Error body: `{"error": "<message naming each product with requested vs available quantities>", "products": [{"name", "requested", "available"}, ...]}` — follows the view's existing primary-`error` + supplementary-field style (cf. the coupon `minimum_order_amount` body); the frontend renders `err.message` from the `error` string (confirmed read-only in `frontend/src/components/checkout/CheckoutView.tsx`; the `products` array is machine-readable extra the current UI ignores).
- `orders/tests.py` — +5 net tests (`CheckoutStockGateTests`): insufficient stock → 400 with the exact error shape and zero side effects (no order/item rows, cart line and stock untouched); zero-stock line rejected; boundary requested == available succeeds with 201; one bad line among several names only the bad product; two bad lines both named with the plural wording. The payment-time backstop is unchanged and stays covered by the pre-existing `test_insufficient_stock_at_verify_returns_409` (stock dropped between create and pay → 409, order stays pending) and `OversellRaceTests` (two qty-1 checkouts against stock 1 both still create — the gate is per-line advisory, the oversell race still resolves at verify).
- Suite: **249 tests, OK (243 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-6-02 (Section 6) — builder: ledger every inventory mutation; adjust_stock made atomic

Per spec [6.5.17] ("Do not permit silent inventory edits with no audit trail") and [6.5.8] (adjustments concurrency-safe): two mutation paths bypassed the `StockMovement` ledger (admin changelist inline stock edits, payment-time decrements) and `adjust_stock` was an unlocked read-modify-write (conventions.md "atomic + select_for_update on rows that gate concurrency"). Every stock change now lands a movement row with delta/reason/actor/timestamp/reference and a correct `stock_after`. Scope caveat (amended during audit cycle-2, which falsified this row's original closing claim "there is no ledger-free mutation path left"): this commit closed the two paths it knew about — REST PUT/PATCH (`ProductSerializer` writable `stock`) and the admin change form could still write stock with no ledger row; both are closed in the SPEC-6-02 audit-fix row below.
- `products/models.py` — `adjust_stock` re-reads the product row inside `transaction.atomic()` via `products.objects.select_for_update().get(pk=self.pk)` before applying the delta, so the below-zero guard and `stock_after` gate on the locked row's current value instead of the instance's stale snapshot (the payment flow locks/decrements the same row); ValueError guard semantics and the None return contract unchanged, and the in-memory instance is synced (`self.stock = locked.stock`). Added `StockMovement.Reason.SALE` ("Sale") for payment-time decrements. Stale docstrings replaced: the class docstring no longer claims the ledger tracks manual admin adjustments only, and `adjust_stock`'s docstring now states every mutation path lands a ledger row.
- `products/migrations/0006_alter_stockmovement_reason.py` — generated `AlterField` for the added `SALE` choice (choices are schema-relevant metadata; keeps `makemigrations --check` clean).
- `products/admin.py` — `stock` removed from `list_editable` (now `("price",)`): a changelist inline edit wrote no movement row and silently clobbered stock the adjust-action ledger was tracking; the stock column stays in `list_display` (display-only) and the adjust-stock intermediate-form action remains the single sanctioned way to move an existing row's stock from the admin (claim scoped during audit cycle-2: the change form's stock field stayed writable — see the SPEC-6-02 audit-fix row below). The `StockMovementInline` heading updated from "Inventory history (manual adjustments)" to "Inventory history (all mutations)" because the inline now also renders payment SALE rows.
- `orders/views.py` — inside `verify_payment`'s existing `transaction.atomic()` block (decrement not moved, oversell 409 gate untouched): each decremented product gets `StockMovement.objects.create(product, delta=-item.quantity, reason=SALE, stock_after=product.stock, note=f"Order #{order.id}", created_by=None)`. `stock_after` is exact without a re-fetch: the product row is `select_for_update`-locked and the new value was computed from that locked instance (plain `-=`, not an F() expression) before `save(update_fields=['stock'])`, so `product.stock` is the real post-decrement quantity. The 409 insufficient-stock path returns before any mutation, so it writes nothing.
- `products/tests.py` — +3 net tests: changelist renders stock display-only (`list_editable` config pin, `form-0-price` input present, no `form-0-stock` input, `field-stock` column still rendered); a hand-crafted changelist bulk-save POST carrying `form-0-stock=999` applies the sanctioned price edit but leaves stock untouched with zero movement rows; `adjust_stock` operates on the locked row, not the stale instance (row updated behind the instance's back to 50 → −5 lands 45, `stock_after` 45). One assertion string updated for the renamed inline heading.
- `orders/tests.py` — +1 net test: verified payment writes exactly one SALE movement per decremented product (two products → two rows) with delta, `stock_after`, system actor `created_by=None`, and note `Order #{order.id}`, and ledger values agree with refreshed product stock; plus edge-case assertions on existing payment tests — the rejected second verify writes no second movement (ledger idempotent behind the idempotency gate) and the 409 insufficient-stock path writes none.
- Suite: **253 tests, OK (247 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-6-02 (Section 6) — builder: audit cycle-2 fix — close the remaining ledger-free inventory paths

Per spec line 1845 ("Do not permit silent inventory edits with no audit trail"): the cycle-1 audit of the row above (commit 52ef87d) found BUG-1 [P1] — `ProductSerializer` exposed a writable `stock` field on PUT/PATCH, so an authenticated admin could silently set stock with zero StockMovement rows (auditor-reproduced: PATCH `{"stock": 999}` → 200, stock 999, no rows) — and BUG-2 [P2] — that row and the admin changelist comment claimed "no ledger-free mutation path left" / "single sanctioned mutation path", both false at the time. While pinning BUG-1, a builder probe found a further path the cycle-1 audit had not flagged: the admin **change page** still had `stock` in editable `fieldsets` (re-proven empirically before the fix: change-page POST `stock=999` → 200, stock 3 → 999, zero rows). All three are closed here; the opening-balance exemption (creation is not an edit — auditor scope note) is preserved on both surfaces, and `adjust_stock`, the admin adjust action and the payment decrements are untouched.
- `products/serializers.py` — `ProductSerializer.__init__` flips `stock` to read-only whenever the serializer carries an `instance` (PUT/PATCH of an existing row), so REST stock writes are ignored and every existing-row change is forced through the ledgered `adjust_stock`; creation keeps `stock` writable (opening balance). The field stays in the response payload, so the public shape is unchanged (`test_serializer_field_set_is_pinned` still green).
- `products/admin.py` — new `get_readonly_fields`: `stock` is read-only on the change page (row exists) while the add page keeps its input (opening balance) — the admin twin of the serializer fix; the changelist comment corrected to the scoped truth (adjust action is the only sanctioned way to change an *existing* row's stock; creation exempt).
- `products/tests.py` — +8 net tests: `ProductStockEditLedgerGuardTests` (PATCH and PUT carrying `stock` → 200, stock unchanged, zero movement rows; update responses still report stock; creation still sets opening stock with no movement row; anti-regression contrast that `adjust_stock` still writes its row) plus a serializer pin (`stock` read-only with an instance, writable without); `ProductAdminTests` gains two pins (change page renders stock display-only while the add page keeps the input; hand-crafted change-form POST with `stock=999` applies the sanctioned price edit but leaves stock untouched with zero rows). `test_staff_write_still_succeeds_end_to_end` re-payloaded from `{"stock": 7}` to `description`: its purpose (SPEC-1-01 staff-write wiring end-to-end) is unchanged, and the old payload encoded the exact ledger-free path this fix removes.
- `docs/changes.md` — the SPEC-6-02 row above amended in place: the falsified claims replaced with scoped, truthful statements pointing at this row.
- Suite: **261 tests, OK (255 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-2-01 (Section 2) — builder: gate cycle-1 fix — no password-shaped literal in the DATABASE_URL test

Per the release-engineer gate report (BUG-1 [P2], GATE_PHASE SENT-BACK cycle 1): GitGuardian incident 37423839 ("Generic Password") fired on the standalone assertion literal in `tests/test_settings_security.py:97` (shipped in 313782e) — the decoded form of the fabricated fixture URL's `p%40ss` password. False positive on a placeholder (no real credential), but a standalone password-shaped literal in source fails the secret-scan check-run on the promotion PR head and blocks the merge. Test semantics fully preserved: the parser must still be proven to URL-decode.
- `tests/test_settings_security.py` — `test_postgres_url_maps_all_connection_fields` now derives the expected password via the stdlib instead of hardcoding the decoded value: `assertEqual(db['PASSWORD'], unquote('p%40ss'))`. The oracle is the percent-encoded fragment already present in the fixture URL, so the assertion is exactly as strong as before — if the parser ever stopped unquoting, `db['PASSWORD']` would hold the raw encoded form and the test would fail. `git grep` confirms no password-shaped literal remains anywhere in source; the fixture URL, the parser under test, and every other assertion are untouched. `.gitleaks.toml` and `.github/workflows/*` deliberately not touched (release-engineer scope).
- Suite: **261 tests, OK (255 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-6-03a (Section 6) — builder: RBAC foundation — six staff roles, capability map, idempotent group sync

Per spec 6.12 (lines 2099–2262): staff RBAC needs named roles and capability identifiers before the staff/roles/audit-log endpoints land. Foundation only — no views or permission checks touched (later micro-tasks).
- `common/roles.py` (new) — the six staff roles (`support`, `catalogue`, `inventory`, `marketing`, `finance`, `admin`) as constants in a stable ordered tuple, plus `CAPABILITY_ROLES`, the single capability→roles structure built from the spec 6.12 identifier table (`products.read` … `settings.manage`) under least privilege (only `admin` holds `staff.manage`/`settings.manage`). `sync_role_groups()` `get_or_create`s one Django Group per role — idempotent on re-run, no check-then-act.
- `common/migrations/0001_sync_staff_role_groups.py` (new) — zero-risk data-only bootstrap calling `sync_role_groups` during `migrate` (no models in `common`, so `makemigrations --check --dry-run` stays clean); real reverse deletes exactly the six groups. `config/settings.py` — `common` added to `INSTALLED_APPS` so the migration loader discovers `common/migrations` (no models, no other effect).
- `tests/test_rbac_foundation.py` (new) — 2 tests: running sync twice leaves exactly the six stable-named groups (no duplicates, no strays); a deleted role group is re-created by the next sync (create branch of `get_or_create`).
- Suite: **263 tests, OK (257 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (1182 stmts, gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-6-03b (Section 6) — builder: capability permission classes on the roles map

Continues spec 6.12 RBAC: turns `CAPABILITY_ROLES` into usable DRF `permission_classes` so the next micro-task can gate the staff API views. No view touched, `IsAdminUserOrReadOnly` and `common/roles.py` untouched.
- `common/permissions.py` — `get_user_roles` (staff roles from Group membership, intersected with `STAFF_ROLES` so stray groups grant nothing), `user_has_capability` (map lookup, unknown identifiers deny by default — least privilege), `CapabilityPermission` base (pinned `capability`; unpinned base denies everyone; denials raise `PermissionDenied` for a uniform 403, mirroring the legacy gate), the `capability_permission(capability)` factory, and one named class per capability (`HasProductsRead` … `HasSettingsManage`, `admin` = full authority because the map already grants it every capability).
- `tests/test_rbac_foundation.py` — 10 new tests: anonymous and role-less-customer denied for all 14 capabilities; the full 14×6 role→capability matrix pinned against `CAPABILITY_ROLES`; unknown-capability and unpinned-base deny paths; named-class set pinned to the map (no drift, no extras, naming rule enforced); factory builds a correctly pinned subclass; `IsAdminUserOrReadOnly` regression guard (message, safe methods public, writes still legacy-`is_staff`-only).
- Suite: **273 tests, OK (267 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (1182 stmts, gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-6-03c (Section 6) — builder: capability gates applied to the staff API views + privilege-escalation guard

Applies SPEC-6-03b's permission classes to the staff-gated API surface per spec 6.12 (privilege-escalation guard, line 2261). The only DRF API views making staff-only decisions today are the two product endpoints (orders/cart/accounts are customer-scoped, the ops dashboard is a session-auth Django template view, not an API view — unchanged). No guard downgrades: writes move from the blanket `is_staff` flag to the `products.write` capability (`catalogue` + `admin` roles) — strictly a tightening, and role-less staff accounts lose product writes, pinned by test.
- `common/permissions.py` — `IsAdminUserOrReadOnly` gains the `write_capability` seam (None = legacy `is_staff` behaviour, byte-identical contract preserved); new `capability_or_read_only(capability)` factory reuses that seam for the public-read/staff-write shape (a plain `CapabilityPermission` would deny safe methods too), and `HasProductsWriteOrReadOnly` is the named instance pinned to `products.write`.
- `products/views.py` — both FBVs (`product_list`, `product_detail`) swap `IsAdminUserOrReadOnly` → `HasProductsWriteOrReadOnly`: reads stay public (incl. OPTIONS), writes now require the capability; the 403 body stays `"Administrator access is required."` for every denied caller.
- `common/testing.py` — the shared `make_staff` fixture now grants the `admin` role group: after the swap, API write authority flows from the roles map, so the fixture models the role-map equivalent of the legacy blanket authority. No test assertions changed.
- `tests/test_rbac_foundation.py` — 6 new tests: unit contract of `HasProductsWriteOrReadOnly` (seam subclass, not a `CapabilityPermission` — the named-class drift scan stays exact; safe methods public for everyone; writes follow the map, support role denied, legacy 403 body); privilege-escalation guard pins `staff.manage`/`settings.manage` as admin-exclusive **in the map itself** (the 14×6 matrix derives from the map and could not catch a widening) so any SPEC-6-05 assignment surface built on `HasStaffManage`/`HasSettingsManage` is admin-only by construction; every non-admin role is denied both classes.
- `products/tests.py` — 6 new view-level tests (real HTTP): anonymous reads public + legacy-403 write; customer denied; role-less staff denied (the deliberate tightening); per-role allow/deny matrix on product create (only `catalogue`/`admin` get 201, the other four roles 403); `catalogue` full edit+delete lifecycle on the detail view; a non-granting role (support) cannot PATCH or DELETE.
- Suite: **285 tests, OK (279 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (1182 stmts, gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-6-04 (Section 6) — builder: role-aware ModelAdmin least privilege + common/* folded into the measured set

Brings the Django admin surface under spec 6.12's least-privilege enforcement ([6.12.4] confirmation for sensitive actions, [6.12.5] privileged-action log trail via Django's own admin LogEntry, [6.12.7] no blanket staff access). Previously the admin's four model permissions checked Django model perms no staff role carries, so in practice only the superuser could work the admin; now each of the six registered ModelAdmins routes view/add/change/delete through `CAPABILITY_ROLES` (the `admin` role — which the map grants every capability — keeps full function, pinned by test).
- `common/admin.py` — new `RoleAwareModelAdmin` base: `capability_map` (permission kind → capability id; `None` = no role may, superuser bypass only), `action_capabilities` (bulk/export actions gated through the same `get_actions` seam Django uses for `allowed_permissions`, so a gated action vanishes from the dropdown *and* is an invalid choice if POSTed directly), `confirmation_required_actions` (destructive bulk actions interpose an explicit-confirmation interstitial — [6.12.4] — that re-POSTs with `confirm=yes` and the preserved selection before executing), `log_bulk_action` (one LogEntry per affected row — [6.12.5], since queryset bulk actions bypass `save_model` and would otherwise mutate rows unlogged), `has_module_permission` follows the map (the default would hide everything from role users), and Django's own superuser bypass is preserved verbatim as the only user-flag shortcut.
- `products/admin.py` — ProductAdmin: view `products.read`, add/change `products.write`, delete `products.publish` (hard delete ≥ unpublish in sensitivity, so a future publish narrowing narrows delete too); `adjust_stock` gated to `inventory.adjust` (its existing delta/reason input form *is* the explicit confirmation) and logs each adjusted row, `export_csv` to `products.read`.
- `orders/admin.py` — OrderAdmin: view `orders.read`, change `orders.fulfill`, add/delete capability-less (orders originate from checkout; hard delete would bypass the legal status flow — the status/cancel actions are the sanctioned paths); all five bulk actions capability-gated (`fulfill` ×3, `cancel` ×1, `read` for export), `cancel_pending` — the one destructive bulk action, an irreversible status change on a financial record — now requires explicit confirmation, and every mutating bulk action (status changes, cancel) writes a LogEntry naming the actor. CouponAdmin: all four kinds ride `discounts.write` (the map has no discounts.read split; promotions are marketing's domain).
- `ops/admin.py` — SiteSettingsAdmin: view/change ride `settings.manage` (admin only, per the [6.12] identifier table); the pre-existing unconditional singleton add/delete denials are kept and outrank even the superuser bypass.
- `cart/admin.py` — CartAdmin: view `customers.read`, mutations capability-less (carts are customer-owned session state; admin row edits would bypass the cart API's quantity/stock checks). Flagged as the one ModelAdmin the capability map couldn't express unambiguously — resolution: read-only for all staff, superuser bypass preserved.
- `accounts/admin.py` — StoreUserAdmin becomes `RoleAwareModelAdmin, DjangoUserAdmin` (base first, so the capability-driven methods win the MRO): view `customers.read`, add/change/delete `staff.manage` — the User row holds `is_staff` and the role groups, so touching it is privilege management (the [6.12] privilege-escalation guard carried to the admin).
- `templates/admin/action_confirmation.html` — the generic interstitial: lists affected objects, re-POSTs `action`/`confirm=yes`/`_selected_action` (or `select_across`) with the cancel link back to `{% url 'admin:index' %}`; adjust-stock's own deliberate form stays off the mechanism by design.
- `tests/test_admin_rbac.py` — 22 new tests: pinned registry equality (`admin.site._registry` == the six store admins, so a future ModelAdmin forgetting the base fails loudly); capability identifiers all drawn from the roles map; the "every declared action is capability-gated" drift invariant; the full view/add/change/delete × role × admin matrix derived from `CAPABILITY_ROLES` (plus role-less staff denied everywhere, admin-role full function, superuser bypass covering even capability-less kinds minus the SiteSettings singleton denials); per-role `get_actions` availability incl. `delete_selected` needing the delete capability (orders: nobody but superuser — matching `delete: None`); e2e through the real UI: view-only finance reads the order change page but gets 403 on save, marketing gets 403 on the orders changelist, `cancel_pending` renders the interstitial and mutates nothing until confirmed, superuser keeps direct execution, a directly POSTed gated action is inert (302 bounce, row untouched), and both gated bulk mutations (mark_shipped, confirmed cancel) leave a LogEntry naming the acting role user.
- `.coveragerc` — per the combined-audit follow-up, `common/*` folds into the measured set (was omitted): 1182 → 1406 stmts. Exactly one gap was exposed — `common/testing.py:135`, the `auth(token)` bearer-attach branch, only its `None` path was ever exercised — closed by a new test logging in via the API, clearing the credentials, re-attaching the raw token with `auth()` and proving 401 → 200 on an `IsAuthenticated` route.
- Suite: **307 tests, OK (301 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (1406 stmts — larger measured set, gate 90), `makemigrations --check` clean.

## 2026-09-18 — SPEC-6-05a (Section 6) — builder: staff + roles management surfaces on the admin

Implements [6.12.1] on the User admin: the six staff role Groups are now assignable/unassignable on staff users from the change page, gated by `staff.manage` (admin role) with Django's superuser bypass preserved, and every Group add/remove is audited with its own admin LogEntry naming the acting user and the role (spec 6.12: log privileged actions, protect role changes from unauthorized privilege escalation — line 2261).
- `accounts/admin.py` — StoreUserChangeForm (UserChangeForm + a `staff_roles` ModelMultipleChoiceField) replaces the raw `groups` M2M in the fieldsets: a plain Groups selector would let even an admin grant unmapped, non-role authority, so the surface exposes exactly the six role groups (queryset pinned to `STAFF_ROLES`, seeded with the user's current membership); `get_fieldsets` strips the roles section for anyone without `staff.manage` and for non-staff targets (least privilege — hidden, rebuilt per request, class-level fieldsets never mutated); `clean()` refuses roles on non-staff accounts (a non-staff user holding a role group is an escalation channel — they'd gain API authority while unable to ever reach the admin again); `save_model` re-checks the capability before any role-bearing write (defense in depth for callers that bypass the view flow, which is already 403-gated by `has_change_permission`), and `_apply_staff_roles` diffs only the six role groups (unrelated group memberships survive) logging `Added role "x".` / `Removed role "x".` per change via `log_change`.
- `tests/test_staff_roles_admin.py` — 12 new tests: roles field lists exactly the six roles, seeded with current membership (admin viewer); support-role viewer gets the roles section hidden (least privilege on the read-only page that rides `customers.read`); admin assigns two roles in one save → both groups land and two LogEntries, one per add, each naming the acting admin; role removal logged the same way; a support user POSTing their OWN change form with the admin role gets 403 (self-escalation denial — no group, no capability, no LogEntry); the same attack against another staff user is refused; a customer's change page has no roles field and a tampered POST carrying `staff_roles` is refused by the form guard (nothing written, no role LogEntry); demoting a staff user while selecting a role is rejected (re-render, nothing written); the superuser bypass still assigns roles and logs; the add view still creates role-free accounts (UserCreationForm path, no injectable roles); direct-call pins — `save_model` raises `PermissionDenied` for a staff.manage-less caller carrying role data, and `_apply_staff_roles` leaves non-role groups untouched.
- Suite: **319 tests, OK (313 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (1449 stmts, gate 90), `makemigrations --check` clean.

## 2026-09-19 — SPEC-6-05b (Section 6) — builder: privileged-action logging for API ops, audit-log route, User-form privilege-escalation guard

Per spec 6.12 [6.12.6] ("Log privileged actions", route `/admin/audit-log`) and the line-2261 guard ("Protect role and permission changes from unauthorized privilege escalation"), on top of SPEC-6-05a (LogEntry as the record source per the SPEC-6-05 split decision; the business-event audit model stays with SPEC-7-01):
- `common/audit.py` (new) — `log_api_action(request, obj, action_flag, change_message)`: the single reusable helper emitting a `django.contrib.admin.models.LogEntry` for API-side privileged writes (via `LogEntry.objects.log_actions`), so the admin chrome and the DRF API feed one audit source. DELETION contract: log before the delete — Django's collector clears the instance pk afterwards (the admin's own `log_deletion` does the same).
- `common/permissions.py` — `capability_required(capability)`: plain-Django twin of `CapabilityPermission` for admin-chrome routes that `permission_classes` cannot reach; same `CAPABILITY_ROLES` authority, Django's superuser bypass preserved (mirroring `RoleAwareModelAdmin`), anonymous → admin-login redirect, authenticated-unprivileged → 403.
- `products/views.py` — the capability-gated DRF writes (the only staff-privileged API mutations today: product create/PUT/PATCH/DELETE under `products.write`) now emit a LogEntry ("Created/Updated/Deleted via API.") naming the acting staff user, each inside one `transaction.atomic()` with the write so the trail can never lag the row; refused or invalid attempts write nothing and log nothing.
- `ops/views.py` + `ops/templates/ops/audit_log.html` (new) + `config/urls.py` — the spec's `/admin/audit-log` route: newest-first paginated LogEntry table (when/actor/action+model/object/details, human `get_change_message` rendering — never raw JSON — links for non-deleted objects), gated by `staff.manage` (admin role; superuser bypass preserved). File placement follows the existing admin-chrome route precedent (`admin/dashboard/`, registered before the admin include).
- `accounts/admin.py` — the flagged SPEC-6-05a hole closed: `get_readonly_fields` makes `is_staff`/`is_superuser`/`user_permissions` read-only for any non-superuser editor (admin role included) on the change page — the flags are excluded from the ModelForm, so a crafted POST is ignored, not validated away. Role assignment (`staff.manage`) is unaffected and the superuser trust anchor keeps full flag authority. Judgment call: `user_permissions` rides the same guard as line 2261's "permission changes" channel (model perms are inert under the roles map, so the restriction costs nothing).
- `tests/test_audit_log.py` (new, 10 tests) — per-flag logging with actor/object identity pins (ADDITION, CHANGE ×2 via PATCH+PUT, DELETION incl. record-outlives-row), denied/invalid attempts log nothing; route matrix: anonymous → admin-login redirect, all five non-admin roles 403, admin role 200 reading both API and admin-chrome entries, superuser bypass + empty state, JSON change messages render human, 55-entry pagination across two pages.
- `tests/test_staff_roles_admin.py` — +4 guard tests (admin-role editor: both flags absent from the form and a crafted dual-flag POST changes nothing while the sanctioned role save still lands; a customer cannot be promoted to staff; a `user_permissions` grant is ignored; superuser contrast still grants). One 05a test re-pointed: `test_demoting_to_non_staff_with_roles_selected_is_refused` now runs as a superuser editor — the demotion vector (editable `is_staff` combined with roles in one save) legitimately exists only where the flag is editable; the pinned guard is unchanged and still asserted there.
- Suite: **333 tests, OK (327 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (1495 stmts, up from 1449; gate 90), `makemigrations --check` clean.

## 2026-09-19 — SPEC-7-01 (Section 7) — builder: append-only business-event audit trail

Per spec 7.1 Audit module ([R-7.20] "Immutable or append-only business activity records") and the section-1 gap [1.22] (no audit trail for orders/payments/auth beyond StockMovement): a cross-domain `AuditEvent` ledger — complementary to SPEC-6-05b's privileged-action LogEntry trail, which is untouched — recording order lifecycle (created, paid), payment intent + verify outcomes (verified plus every failure branch: signature rejected, order not found, already processed, reference mismatch, stock conflict, coupon invalid), and authentication events (registered, login success/failure, email verified, password reset). Per-transition from→to status events stay with section 10 by design.
- `common/models.py` (new) — `AuditEvent`: category (order/payment/auth, derived from the event-type's dotted prefix so an unknown identifier fails loudly instead of writing an unqueryable row), event_type, actor + order FKs (both `SET_NULL` so events outlive the rows they describe, with identifying values preserved in the JSON `detail` payload), created_at. `AuditEvent.record()` is the single sanctioned write path, mirroring StockMovement's actor convention (anonymous/system → NULL); `save()` refuses updates and `delete()` refuses removals — append-only is enforced at the model level and pinned by tests. Placement: `common/` already hosts the audit twin (`common/audit.py`) and RBAC; an orders home would force accounts → orders imports for auth events.
- `common/migrations/0002_auditevent.py` (generated, unedited) — create table with indexes on category/event_type.
- `orders/views.py` — hooks in the three side-effectful order/payment flows, each inside the SAME `transaction.atomic()` as the effect it records (the StockMovement pattern): `create_order` (order.created), `create_payment` (payment.initiated on first intent persistence, wrapped in a new atomic with the order.save; the idempotent reuse path emits nothing), `verify_payment` (signature rejection before the block — a single atomic insert on its own; order-not-found, already-processed, reference-mismatch, stock-conflict, coupon-invalid inside the existing block; payment.verified + order.paid in the success tail).
- `accounts/views.py` — `LoginView.post` override records auth.login (200) and auth.login_failed (400/401 — super().post signals every rejection by raising) with the attempted username in detail and the actor resolved by the same exact-username match `authenticate()` used; 429 throttled refusals are raised before the view runs and write nothing. `register` (auth.registered), `verify_email` (auth.email_verified only when activation actually happens — a re-verify of an active account is a no-op), `reset_password` (auth.password_reset; the one-time token deliberately not stored) each wrap the state change + trail row in one `transaction.atomic()`.
- `tests/test_audit_trail.py` (new, 23 tests) — model contract (category derivation, unknown-prefix rejection, anonymous actor → NULL, update/delete guards, events outlive a deleted user+order via SET_NULL); endpoint-level trail assertions for every event type through the real flows; rollback-together pins: an injected audit-write failure inside checkout and inside verify_payment aborts the order/stock/coupon/cart effects with it (500, no new trail rows, order stays pending, stock intact); 429 refusals write nothing (rate pinned on the throttle class directly, since DRF snapshots `DEFAULT_THROTTLE_RATES` at import so `override_settings` cannot reach it).
- No reader surface (endpoint/admin) in this task — deliberate: SPEC-6-05b's `/admin/audit-log/` route is the LogEntry reader and stays untouched; surfacing AuditEvent can ride a later section with its own RBAC gate decision. No new env keys (no thresholds/keys introduced).
- Suite: **356 tests, OK (350 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (1573 stmts, up from 1495; gate 90), `makemigrations --check` clean.

## 2026-09-19 — SPEC-7-02 (Section 7) — builder: env-driven LOGGING baseline wired to the audit trail and payment failure paths

Per the section-7 observability item plus the logging halves of the section-1 gap [1.22] (no LOGGING config) and section-2 gap [2.12] (no monitoring — only the logging baseline lands here; sentry/metrics/alerts stay deferred to S22 per the ledger): an env-driven dictConfig baseline with request 5xx and payment failures visible at minimum, wired so audit events and every verify_payment failure branch actually emit log records. No AuditEvent semantics change: same insert, same transaction placement, same append-only guards.
- `config/settings.py` — `LOGGING` built by `_build_logging(app_level, file_path)`: console always wired (12-factor stdout; read-only-filesystem-safe, no hardcoded path), a rotating file handler (5 MB × 3) joins only when `LOG_FILE` is set; brace-style formatter (detail payloads render dicts, so %-style `%%` escaping would collide); `disable_existing_loggers=False`. `django.request` pinned at ERROR and the audit channel `common.audit` pinned at INFO — both level-only entries, so each record reaches the root handlers exactly once and stays visible even when `LOG_LEVEL` is raised. `_env_log_level` resolves `LOG_LEVEL` fail-safely (the `_env_int` pattern): unknown/`NOTSET` values fall back to INFO with a warning instead of crashing startup.
- `.env.example` — `LOG_LEVEL=INFO` (default) and optional `LOG_FILE` documented, including the read-only-FS rationale.
- `common/models.py` — `AuditEvent.record` now emits one INFO line (`audit <type> id=<pk> actor=<username> order=<pk> detail=<payload>`) on the dedicated `common.audit` channel after the insert, so the trail is observable in logs without a reader surface; the call writes no rows, so the rollback-together semantics from SPEC-7-01 are untouched.
- `orders/views.py` — module logger plus: a `logger.exception("Payment intent creation failed for order …")` wrap around the Razorpay intent call in `create_payment` (log-and-reraise — the 500 semantics are preserved exactly, only the missing order reference is added), and one logger call per AuditEvent-emitting verify_payment failure branch with the order reference and reason: signature rejected (WARNING), order not found (WARNING), already processed (INFO — benign double-submit), reference mismatch (WARNING), stock conflict (INFO — lost race), coupon invalid (INFO — lost race). No permission, throttle, or response changes.
- `tests/test_logging_baseline.py` (new, 21 tests) — env-driving pins (`LOG_LEVEL` case-insensitive, unknown/`NOTSET` → fallback + warning, missing → default); config structure pins (console always, root level tracks the resolved setting, django.request/`common.audit` pins, brace formatter); file handler only with an explicit path; `AuditEvent.record` INFO emission (stored identity incl. anonymous → `None`); all six verify failure branches + the gateway-failure 500 emit records via `assertLogs`; a no-secrets pin (Razorpay key id/secret absent from the captured output while both introduced surfaces are present).
- Suite: **377 tests, OK (371 pass, 6 `expectedFailure` flips unchanged)**, coverage **100.00%** (1607 stmts, up from 1573; gate 90), `makemigrations --check` clean.

## 2026-09-19 — SPEC-7-03 (Section 7) — builder: coupon discount quantize parity ([R-7.7], F-11 flip)

Per the section-7 money rule (exact decimal money, never float) and conventions.md's "always quantize before comparing/serializing": percentage coupon discounts are quantized to the 2-dp money precision in BOTH coupon-affected paths, so preview display, the audit trail and the stored order amount always agree to the paisa. Flips the pinned `expectedFailure` F-11 — expected-failure count drops 6 → 5.
- `common/money.py` (new) — `quantize_money()`: quantize to `0.01` with `ROUND_HALF_EVEN`, the rounding the DB adapter applies when a `DecimalField` value is written, so an in-memory value and the stored value it becomes are the same amount. Placement in `common/` (the shared-kernel app, like audit/permissions/roles): the money rule is cross-app — orders computes discounts today, pricing/reporting consumers would tomorrow; `ops/services.py` already uses the same inline quantize idiom and is deliberately untouched (no drive-by).
- `orders/views.py` — `create_order` (checkout) and `apply_coupon` (public preview) quantize the percentage discount immediately after the `/100` division, before the `maximum_discount` cap comparison, the subtotal clamp and any storage/serialization. Fixed-type discounts needed no change (the value comes straight off a `decimal_places=2` field); `verify_payment`, the cart endpoints and the admin CSV read DB-stored 2-dp values and do no discount math. Quantize is identity at 2-dp grid points, so clean amounts are unchanged; the audit trail now records `'total_amount': '2099.99'` where it previously recorded `'2099.9930'` (same value, money precision).
- `orders/tests.py` — pin flip only: `@unittest.expectedFailure` removed from `test_f11_rounding_parity_between_preview_and_checkout` per its own docstring instruction once the fix landed; the `assertEqual(Decimal(preview.data["discount"]), order.discount_amount)` assertion is byte-identical and now passes for real (30% of 2999.99 = 899.997 raw → 900.00 on both paths). The orphaned `import unittest` (sole use was the removed marker) dropped; `from unittest import mock` stays.
- No model changes, no new env keys — `makemigrations --check` clean.
- Suite: **377 tests, OK (372 pass, 5 `expectedFailure` — F-11 flipped green)**, coverage **100.00%** (1612 stmts, up from 1607; gate 90), `makemigrations --check` clean.

## 2026-09-19 — SPEC-7-04 (Section 7) — builder: env-driven products page size + stable default ordering ([R-7.6], F-23 + F-12 flip)

Per the section-7 conventions findings: the products listing page size is env-driven (the hardcoded 2 was a dev/test artifact) and the default listing queryset gets a deterministic total order, so pagination is stable across identical requests and the pre-existing UnorderedObjectListWarning anomaly is gone from the run output. Flips the pinned `expectedFailure` F-12 — expected-failure count drops 5 → 4.
- `config/settings.py` — `PRODUCTS_PAGE_SIZE = _env_int('PRODUCTS_PAGE_SIZE', 12)` (the established fail-safe pattern): a storefront-appropriate default replaces the leaked dev value; malformed env values fall back instead of crashing startup.
- `.env.example` — `PRODUCTS_PAGE_SIZE=12` documented with the F-23 rationale.
- `products/views.py` — Paginator takes `settings.PRODUCTS_PAGE_SIZE` (envelope keys unchanged: count/total_pages/current_page/next_page/previous_page/results); when no valid `ordering` param is given the queryset defaults to `order_by('-created_at', '-id')` — newest-first per storefront convention, with the unique `-id` tiebreaker making the sort total, so identical requests always partition the catalogue identically. Explicit whitelist orderings and all permissions/throttles untouched.
- `products/tests.py` — F-12 flip only: `@unittest.expectedFailure` removed from `test_f12_default_listing_order_is_deterministic` per its own docstring instruction; the assertion is byte-identical and now passes for real (identical timestamps force the tiebreaker → ids descending). Docstring updated to describe the landed mechanism (view-level default order_by; the model keeps no Meta.ordering): the pin prose anticipated a `Meta.ordering` fix with an `id` tiebreaker, but the pinned assertion demands DESCENDING ids under equal timestamps, so the landed tiebreaker is `-id`. Size-fixture adjustments (disclosed): the three tests that request page 2 / expect multi-page clamping get an explicit `@override_settings(PRODUCTS_PAGE_SIZE=2)` — assertions unchanged, they keep pinning genuinely multi-page behaviour instead of silently clamping onto page 1 at the new default; stale "hardcoded 2" comments dropped from `_list_all` and the envelope test. One new test `test_page_size_is_env_driven` pins the env knob (default-size envelope math + size-1 override → 3 pages of 1) — without it a regression back to a hardcoded 2 would be invisible to the override-decorated tests.
- Frontend consumers confirmed unaffected (grep only, no client edits): `products/page.tsx` and `sitemap.ts` read `total_pages`/`current_page`/`results` from the API envelope; nothing client-side hardcodes the page size, so the sitemap walk cost collapses automatically (~count/12 vs ~count/2 requests). The now-stale sitemap.ts comment about the size-2 backend is a docs-only nit left for a frontend task (outside this task's declared scope). Admin listing unaffected (ModelAdmin's own paginator).
- No model changes — `makemigrations --check` clean.
- Suite: **378 tests, OK (374 pass, 4 `expectedFailure` — F-12 flipped green)**, coverage **100.00%** (1615 stmts, up from 1612; gate 90), `makemigrations --check` clean.

## 2026-09-19 — SPEC-15-1 (Section 15) — builder: WCAG 2.2 AA input contrast fix via shared field class

Per spec 15.4 ("Sufficient text contrast", WCAG 2.2 AA target; S15 audit SPEC-15-1) — the two REAL failures fixed and measured with the WCAG relative-luminance formula (per-channel linearization → L = 0.2126R + 0.7152G + 0.0722B, ratio = (L1+0.05)/(L2+0.05)):
- OLD placeholder: `placeholder:text-ink-muted/70` composited #57534e at 70% over the white input background → #898783, **3.5855:1** on surface (< 4.5:1). NEW: full-strength `placeholder:text-ink-muted` → **7.6293:1** on surface, **7.1882:1** worst-case on paper (pass with wide margin).
- OLD border: `border-line` #e6e1d7 → L = 0.755797, **1.3031:1** on surface / 1.2277:1 on paper (< 3:1, WCAG 1.4.11 non-text). NEW: dedicated field-border token `--color-line-strong: #75706a` → **4.9041:1** on surface, **4.6205:1** worst-case on paper.
- `frontend/src/lib/ui.ts` (new) — one `inputClass` export (`border border-line-strong bg-surface px-3 py-2 text-sm placeholder:text-ink-muted focus:outline-none`) replaces the 7 duplicated copies, so future form-control fixes land everywhere at once. Width intentionally excluded from the base class: the 5 full-width forms append `w-full` at the element, CouponForm/FilterBar keep their `min-w-0 flex-1`/`w-56`/`w-24` appends — no conflicting width utilities in one class list. Placement: `src/lib/` is the established shared-module home (money/filters/redirect pattern) with colocated vitest tests; `lib/tokens.ts` is the AUTH-token module, not design tokens.
- `frontend/src/app/globals.css` — `--color-line-strong` added; the decorative `line` token deliberately untouched: grep shows 60+ non-input `border-line`/`divide-line` uses (dividers, cards, footers, badges) plus `var(--color-line)` in `.skeleton`/`.text-ghost`, all exempt from 1.4.11 and visually unchanged. #75706a stays on the palette's warm r>g>b axis between ink-muted (#57534e) and line (#e6e1d7).
- Consumers switched to the shared class — 7, not just the 4 the audit pinned: AddressForm, LoginForm, RegisterForm, EmailOnlyForm, ResetPasswordClient, CouponForm, FilterBar. Grep found the identical failing string (same /70 placeholder + border-line) in the 3 unpinned auth forms, so scoping to 4 would have left the same AA failure live in the tree. No markup or behavior changes beyond className sources/imports.
- `frontend/src/lib/ui.test.ts` (new, 2 tests) — token-level pins (`border-line-strong` + `placeholder:text-ink-muted` present; a guard that no `placeholder:…/NN` opacity modifier ever returns — the exact mechanism that broke 4.5:1). Deliberately not a whole-string snapshot so unrelated layout classes can evolve.
- Frontend evidence: BEFORE (aa01edf, deps `npm ci`) vitest **22/22 (4 files)** + clean build; AFTER vitest **24/24 (5 files, floor moved up)** + clean `next build`; `border-line-strong` confirmed present in the compiled `.next` CSS (utility actually generated). Playwright e2e NOT run locally per task instructions (non-hermetic, CI self-skips; SPEC-3-01 owns it). No backend files touched — backend suite/floor untouched; no new env keys; no model changes.
- Ledger note: section-15.md had parked inputClass centralization under SPEC-2-02; this task's handoff mandates it now, so SPEC-2-02 retains only the broader component-library scope.

## 2026-09-20 — SPEC-19-1 (Section 19) — builder: in-process notification service ([R-19.0]) with the order.paid proof event

Per spec §19 ("Notifications should be driven by business events rather than manually scattered email calls"): one send path, one template-loading home, and an explicit `dispatch()` that rides the audit-event hooks. In-process substrate only — no queue/outbox table/retries/backoff (SPEC-2-03's separate scope); the full order-email content set (shipped/delivered/cancelled/returns/refunds + admin alerts) stays with SPEC-1-12/SPEC-19-2. `AuditEvent.record` itself untouched — same insert, same transaction placement, same append-only guards (semantics ratified by the SPEC-7-02 audit).
- `common/notifications.py` (new) — `send_email(template_name, context, subject, recipient)`: renders `emails/<template_name>.txt` through the project's TEMPLATES DIRS and sends with `settings.DEFAULT_FROM_EMAIL` (sender never hardcoded), raising on failure so request-scoped flows keep their documented SMTP-503 behavior; `dispatch(event_type, context)`: registry lookup plus a log-and-continue wrapper (`logger.exception` on the dedicated `common.notifications` channel riding the existing LOGGING baseline) so a send failure can never break the caller's transaction; events with no registered handler are DEBUG-logged no-ops. Registry is explicit (spec 19.1), never a signal on `AuditEvent`.
- `templates/emails/` (new) — `verification_email` / `username_reminder` / `password_reset` / `order_confirmation` `.txt`. Placement: `TEMPLATES[DIRS]` already points at `backend/templates` (the admin templates' home), so no settings change; the three migrated templates render byte-identical bodies (uid/token passed separately so template autoescape can never rewrite the `&` link separator; files end without a trailing newline to keep the exact pre-service bodies).
- `accounts/views.py` — the scattered `_send_email` helper retired; `_send_verification_email`, `forgot_username` and `request_password_reset` now call `notifications.send_email` with identical subjects, recipients and links; every `try/except → 503` wrapper and uniform anonymous body is untouched (zero behavior change).
- `orders/views.py` — `verify_payment` success: `notifications.dispatch(AuditEvent.EventType.ORDER_PAID, {"order": order})` beside the existing `AuditEvent.record` calls, inside the same `transaction.atomic()` block (rollback-together like record; `dispatch` never raises, mirroring the SMTP-503 account-still-created behavior). One proof event end-to-end; the confirmation email carries order number, total and the frontend URL.
- `accounts/tests.py` — the two SMTP-failure tests' patch target moves from the retired `accounts.views._send_email` to `common.notifications.send_email` (required by the retirement; assertions byte-identical). The `_send_verification_email` patches were unaffected.
- `tests/test_notifications.py` (new, 10 tests) — byte-identity pins for the three migrated emails (body/subject/recipient/sender-from-settings); the one-send-path contract; `dispatch` unit behavior (unknown event no-op, `order.paid` send, missing-order context warning, swallowed-and-logged send failure); the end-to-end proof — verify success delivers the confirmation email, and a failing send leaves the payment 200/confirmed with the audit trail committed and an empty outbox.
- `common/testing.py` deliberately unchanged — the locmem outbox plus `mock.patch("common.notifications.send_email")` are sufficient; no test hook needed. `.env.example` — `FRONTEND_URL` comment extended (it now also feeds order-confirmation emails); no new env keys, no model/migration changes.
- Suite: **388 tests, OK (384 pass, 4 `expectedFailure` flips unchanged)**, coverage **100.00%** (1639 stmts, up from 1615; gate 90), `makemigrations --check` clean.

## 2026-09-20 — SPEC-21-1 (Section 21) — builder: server-side duplicate-checkout dedup guard ([R-21.2.6])

Per spec §21.2 ("Duplicate checkout submissions do not create duplicate orders or charges"): decision taken = MINIMAL SERVER-SIDE GUARD (the handoff's preferred option), not a two-201 contract pin — checkout never clears the cart (cleanup is post-payment), so a double-click / client retry resubmits the byte-identical payload while the first order is still payable, which previously minted a second payable order and with it a real double-charge path; nothing in the code or docs says the guard must not exist, and the ledger reserves only the header-keyed machinery (Idempotency-Key + unique-by-key store, covering cross-session simultaneous duplicates) for SPEC-9-01 — none of that is pre-built here.
- `orders/views.py` — `create_order`'s existing `transaction.atomic()` block now first `select_for_update()`s the cart row (the session-scoped serialization point: two rapid same-session POSTs queue there, so the loser re-runs the lookup after the winner has committed; the cart row is locked only on this path — verify_payment locks Order → Products → Coupon — so no new lock-order cycle, the SPEC-12-02 deadlock advisory is untouched) and consults `_find_duplicate_pending_order`: the user's recent pending order (within `CHECKOUT_DEDUP_WINDOW_SECONDS`, payable gate mirrors verify's already-processed check) carrying the identical fingerprint — sorted (product_id, quantity) lines + coupon + the six shipping fields. Match → replay that order with 200 (INFO log, no second order / item snapshot / order.created trail row); any deliberate difference → fresh 201 exactly as before. verify_payment untouched; no new models/migrations.
- `config/settings.py` + `.env.example` — `CHECKOUT_DEDUP_WINDOW_SECONDS = _env_int('CHECKOUT_DEDUP_WINDOW_SECONDS', 300)`: the guard's only threshold is env-driven per conventions (no magic numbers in code); outside the handoff's ≤3-file list for exactly this reason.
- `orders/tests.py` — `CheckoutDedupTests` (7 tests) pins the new contract: double-click replay (200 + same id, 1 order / 1 item / 1 order.created trail row), the replayed order is the one that gets paid (exactly one gateway order minted for it), a different user with an identical cart is unaffected (per-user keying), deliberate coupon / shipping differences and changed cart lines always create new orders, window expiry allows a genuine reorder, and a settled (no-longer-payable) order never blocks a fresh identical checkout.
- `tests/test_e2e_concurrency.py` — the two-201 assertion updated deliberately in the same commit: cross-user two-201 remains the pinned contract (the dedup guard is keyed per user, so the oversell decision stays at verify_payment's row locks); a comment explains the new contract and points at `CheckoutDedupTests` for the same-user collapse.
- Suite: **395 tests, OK (391 pass, 4 `expectedFailure` flips unchanged)**, coverage **100.00%** (1665 stmts, up from 1639; gate 90), `makemigrations --check` clean.

## 2026-09-20 — SPEC-21-2 (Section 21) — builder: CSRF-enforcement truth + API XSS-output safety pins ([R-21.1.4])

Per spec §21.1.4 (security test layer, CSRF/XSS): the S21 prefetch found CSRF middleware configured (settings.py:66,262) but ZERO CSRF-enforcement tests and no XSS tests anywhere. This task pins today's contract — no enforcement behavior changed (the gap itself is SPEC-17-03's build task); the pins make any future flip deliberate.
- `tests/test_csrf_security.py` (new, 12 tests) — CSRF half: `CsrfStackWiringTests` pins CsrfViewMiddleware installed + `DEFAULT_AUTHENTICATION_CLASSES` JWT-only (the load-bearing line SPEC-17-03 changes; the failure message points at the flip) and `SESSION_COOKIE_SAMESITE` 'Lax' as the relied-upon mitigation. `SessionCartCsrfTruthTests` pins today's truth per BACKEND_REQUESTS.md's live confirmation: POST/PATCH/DELETE on `/api/cart/*` with only the session cookie and no `X-CSRFToken` succeed (201/200/200), they still succeed under a fully-enforcing client (`enforce_csrf_checks=True` — the exemption is server-side view-level `csrf_exempt`, not a test-client artifact), and no API response ever issues a `csrftoken` cookie. `CsrfMiddlewareDifferentialTests` proves the middleware is live: tokenless POST `/admin/login/` under enforcement → 403 (so the cart pins mean "exempt", not "middleware absent"). `TokenAuthMutationCsrfTruthTests` pins checkout (201) and apply-coupon (200) succeeding without CSRF tokens (the JWT is non-ambient, so checkout's exploitable surface is the session cookie its cart resolution also requires). XSS half: `ApiXssOutputSafetyTests` pins the API boundary — script/meta/img payloads in product name/description and checkout full_name/address come back verbatim-as-stored (list/detail/cart/orders surfaces), `Content-Type: application/json`, payload literal in the raw JSON body — the API emits data, never markup; output encoding is the consumer's job (frontend JSON-LD sink is SPEC-17-06 / S21-4 territory; grep-verified the only server-side `render()` surfaces are staff-gated admin/ops templates, Django-auto-escaped, no `mark_safe` of user content, plus `.txt` emails).
- Docstrings in the new module state exactly what SPEC-17-03's `SessionAuthentication` change will flip (2xx → 403 on session-cookie flows) so updating the pins is a deliberate one-line contract update, not an accident.
- No production code, settings, middleware, or model changes — `makemigrations --check` clean.
- Suite: **407 tests, OK (403 pass, 4 `expectedFailure` flips unchanged)**, coverage **100.00%** (1665 stmts, unchanged — test-only addition; gate 90), `makemigrations --check` clean.

## 2026-09-20 — SPEC-8-01 (Section 8) — builder: customer-facing order numbers (ORD-YYYY-NNNNNN) + identifier-exposure strategy ([R-8.4],[R-8.5])

Per spec §8.3 ("Give orders a separate human-readable order number, such as `ORD-2026-001042`"; index on order number; non-sequential identifier exposure), orders were identified only by the sequential auto-pk that URLs/admin/serializers expose directly.
- **Exposure-strategy decision** (documented in the `Order` model docstring and the `OrderSerializer` docstring): the sequential `id` stays the internal key — it remains the URL/admin primary key and no URL changes — while `order_number` is the read-only customer-facing reference, surfaced at checkout (201 body) and on every order read. Guest checkout (SPEC-3-02) will key on `order_number`; the pk never leaves server-side routing.
- `orders/models.py` — `Order.order_number = CharField(max_length=20, unique=True, null=True, blank=True)`: 15 chars for `ORD-YYYY-NNNNNN` + headroom; `unique=True` is simultaneously the customer-reference guarantee, the spec 8.3 index, and the concurrency authority for generation. Nullable by design: checkout (the only production writer) always sets it, non-checkout ORM creations (16 test-fixture sites) keep working, and the migration backfills every pre-existing row, so the column is fully populated after migrating.
- `orders/migrations/0006_order_order_number.py` — AddField + `RunPython` backfill: per-year sequence in `id` order (UTC `created_at` year, the same clock the runtime generator reads), single writer inside the migration transaction so the unique constraint cannot be violated; reverse is a no-op. `makemigrations --check` clean.
- `orders/views.py` — generation inside `create_order`'s existing `transaction.atomic()` block (after the [R-21.2.6] dedup guard, untouched): `_next_order_sequence(year)` reads the year prefix's lexicographic max (zero padding keeps lexicographic == numeric) as a **fast path only**; the unique constraint is the race authority, and a bounded (5-attempt) retry loop with a per-attempt savepoint catches `IntegrityError`, regenerates from committed state, and re-raises loudly on exhaustion (conventions.md:17 — IntegrityError retry, never trust check-then-act). A rolled-back checkout never burns a number; same-session replays never reach the loop (dedup guard returns first).
- `orders/serializers.py` — `order_number` added to `fields` + `read_only_fields`; `orders/admin.py` — `list_display` gains `order_number` beside the pk.
- `orders/test_order_number.py` (new, 9 tests): format pin (`^ORD-\d{4}-\d{6}$` + current-year bucket), per-year sequence + restart-at-000001 rollover, unique-constraint pin, forced collision-retry (deterministic — first candidate collides, retry commits fresh, exactly one retry), exhausted-retries rollback (fails loudly, nothing left behind), dedup interplay (replayed checkout returns the SAME order_number — extends the SPEC-21-1 pin), serializer exposure/read-only + pk-stays-URL-key, admin `list_display`.
- verify_payment/payment semantics, permission surfaces, and URL routing untouched; no new env keys (the format and retry bound are code constants, not config).
- Suite: **416 tests, OK (412 pass, 4 pre-existing `expectedFailure` unchanged)**, coverage **100.00%** (1798 stmts, 0 miss — grew from 1665 with the new code; gate 90), `makemigrations --check` clean.

## 2026-09-20 — SPEC-8-02a (Section 8) — builder: ProductVariant schema core with globally unique SKU ([R-8.7])

Per spec 8.3 ("Enforce unique SKUs where the business requires global SKU uniqueness", line 2481; index on SKU, line 2553; FKs between core entities, 2483) the product catalogue had nowhere to hang a sellable variant, and the order-item snapshot list (8.3:2499–2515) already names "Variant name/options" and "SKU" as things orders must retain. Half of the SPEC-8-02 split: schema core ONLY — OrderItem snapshot wiring is SPEC-8-02b's, catalogue/admin depth is SPEC-6-08's.
- `products/models.py` — `ProductVariant`: FK → `products` (`on_delete=CASCADE` matching the `StockMovement` child precedent; order history retention is the snapshot's job per 2497, not FK retention), `name` CharField(100) (the variant label the snapshot list reads, 2503), `sku` CharField(64, **unique=True**) — globally unique across the whole table, the constraint and its implied backing unique index in one (the same reading the shipped `Order.order_number` pins; merchant-entered, so conventions.md:17's IntegrityError-retry has no generation path to wrap — the DB constraint is the concurrency authority), `price` DecimalField(10,2, null=True) (exact decimal money per 2489/conventions.md:15; spec 6.2's "variant-specific prices" — NULL means the product price applies, no read-side wiring invented here), `stock` PositiveBigIntegerField(default=0) mirroring `products.stock`'s type, `created_at`/`updated_at` (distinct timestamps per 2523).
- Stock boundary (kept inert by design): `products.stock` remains the ONLY order-time authority — checkout/verify_payment never read `ProductVariant.stock`; a zero-stock variant neither blocks nor consumes a checkout. Reconciliation is SPEC-6-13's; proven by an integration test, not just asserted.
- `products/admin.py` — bare `ProductVariantAdmin(admin.ModelAdmin)` registration only: entity visible, default per-model permission gating (no role holds them yet), deliberately no role matrix/fieldsets/actions (SPEC-6-08's).
- `products/migrations/0007_productvariant.py` — CreateModel; `makemigrations --check` clean.
- `products/test_variant.py` (new, 11 tests): exact field-set pin (drift guard), FK target/on_delete/reverse-accessor + cascade deletion policy, decimal-money + NULL-price metadata, distinct created/updated timestamps, `__str__`, globally-unique SKU proven across two DIFFERENT products via forced-collision IntegrityError (the race authority, deterministic), DB-level unique constraint introspected from the live schema (SQLite labels constraint-origin indexes `index=False` — the physical auto-index exists regardless), stock defaults to zero, the zero-stock-variant paid-checkout boundary pin, and bare-admin registration + reachable changelist.
- No changes to OrderItem, checkout, verify_payment, serializers, cart or URLs; no fixture edits; no new env keys.
- Suite: **427 tests, OK (423 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (1920 stmts, 0 miss — grew from 1798 with the new code; gate 90), `makemigrations --check` clean.

## 2026-09-20 — SPEC-8-02b (Section 8) — builder: OrderItem snapshot columns sku/variant_name — the historical-order immutability pattern ([R-8.13])

Per spec 8.3 "Historical snapshots" (orders must retain what was actually purchased, 2495–2515: product name 2501, variant name/options 2503, SKU 2505), order lines are now self-describing even after the catalogue changes or the product row is gone. Second half of the SPEC-8-02 split (depends on SPEC-8-02a's ProductVariant).

- **Population-source decision (documented):** no variant-selection input exists at checkout — `CartItem` references products only and variant PICKING rides SPEC-3-21/SPEC-6-08 — and `products` carries no product-level SKU (verified). The honest minimal reading: `sku` snapshots empty and `variant_name` mirrors the product name at checkout (spec's intent that lines are self-describing); a matched variant's SKU/name would fabricate history the customer never chose. Variant matching becomes real with SPEC-3-21's selection input — the columns are already variant-SKU-width (64).
- `orders/models.py` — `OrderItem.sku` (CharField(64), matches `ProductVariant.sku` width) + `OrderItem.variant_name` (CharField(200), product_name width), both `default=''`; docstring pins the frozen-at-checkout contract and the population source.
- `orders/views.py` — line-item construction inside `create_order`'s atomic block (between the 21-1 dedup guard and the 8-01 order-number mint, byte-unchanged) sets `sku=''` and `variant_name=product.name` once; no later save path mutates them.
- `orders/migrations/0007_orderitem_sku_orderitem_variant_name.py` — AddField ×2 + RunPython backfill (reverse: noop, mirroring 0006). Backfill strategy: deterministic single pass in id order over committed server-side state only (same strategy class as 0006 — no clock, no randomness, pure function of current data); `variant_name` copies the product's CURRENT name where the product still exists (best-effort for legacy rows — the at-purchase name already lives in `product_name`); rows whose product was deleted get empty snapshots (documented — nothing honest is left to copy, their `product_name`/`price` snapshots carry the record).
- `orders/serializers.py` — `OrderItemSerializer` exposes `sku`/`variant_name` in `fields` + `read_only_fields` (no URL changes).
- `orders/admin.py` — `OrderItemInline` gains an explicit `fields` ordering so the frozen columns render beside the product name they were taken from; the inline's existing deny add/change/delete keeps every snapshot column read-only (admin edits would falsify purchase history).
- `tests/test_orderitem_snapshot.py` (new, 6 tests): checkout populates the product-identity snapshot (API + DB), snapshot survives product RENAME + reprice (extends test_e2e_isolation.py:79-131, which only pinned price change + delete), snapshot survives product DELETE (FK SET_NULL, columns carry the record, API-level), serializer exposes both read-only, admin inline surfaces the columns and refuses add/change/delete, and the 0007 backfill function pinned without DDL round-trips (populated row, orphaned row → empty, product_name preserved).
- No changes to cart, verify_payment, stock gate, dedup guard, order-number mint or URLs; no fixture edits; no new env keys.
- Suite: **433 tests, OK (429 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (1923 stmts, 0 miss — grew from 1920 with the new code; gate 90), `makemigrations --check` clean.

## 2026-09-20 - SPEC-8-03 (Section 8) - builder: currency column alongside every money column, store-config-driven ([R-8.11], part 1/2)

- `config/settings.py` — `DEFAULT_CURRENCY` via `_env_currency(name, default)`, a fail-safe env resolver in the established `_env_int`/`_env_log_level` idiom: three-letter ISO 4217 shape (case-insensitive), malformed values fall back to `INR` with a warning instead of failing startup. Deliberately env config, NOT a SiteSettings column: a currency is never-changes-at-runtime config (S16 boundary) — runtime-editable currency would reprice historical rows' meaning.
- `.env.example` — `DEFAULT_CURRENCY=INR` documented (affects NEW rows only; existing rows keep their minted currency).
- `orders/models.py` — `Order.currency` + `OrderItem.currency` (`CharField(max_length=3)`, callable `default_currency()` reading `settings.DEFAULT_CURRENCY` at creation — chosen over set-at-creation so every writer, not just checkout, gets the config value; the callable reference keeps migration files deterministic). Order currency denominates `total_amount` + `discount_amount`; item currency rides `price`/`subtotal`. `Coupon.discount_value` stays out of scope: catalogue config, not transaction money.
- `orders/migrations/0008_order_currency.py` — two AddFields + `RunPython` backfill stamping every pre-existing row with the literal `'INR'` (deterministic regardless of the migrating environment's `DEFAULT_CURRENCY`, which the callable default would otherwise bake in); reverse is a documented noop.
- `orders/views.py` — both hardcoded `'INR'` literals in `create_payment` (gateway `order.create` payload + client response) now read `order.currency`; no other line touched (dedup guard, order-number mint, snapshot population byte-unchanged).
- `orders/tests.py` (+9): store-default currency on order/items with Decimal money intact, setting override changes new rows (ORM + checkout), gateway payload charged in the order's currency (mocked Razorpay), recorded currency frozen against later setting changes, `_env_currency` valid/malformed-with-warning/missing branches, and a migration round-trip (`0007` → raw pre-currency rows → `0008`) pinning the deterministic `'INR'` backfill.
- **Part 2/2 remainder (next dispatch)**: `orders/serializers.py` (`currency` in `OrderSerializer` + `OrderItemSerializer` fields/read_only), `orders/admin.py` (`OrderAdmin.list_display` + payment fieldset/readonly), and their exposure tests — spec sentence "customer-facing serializers surface the currency + admin list_display".
- Boundary note: `verify_payment` does NOT assert the gateway amount at HEAD (the dispatch premise was stale — there is no gateway fetch/reconciliation in the verify path); adding server-side amount+currency reconciliation is a separate concern, not smuggled in here. Frontend `money.ts` INR formatting is explicitly a frontend task (formatting consumption, not schema).
- Suite: **442 tests, OK (438 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (1938 stmts, 0 miss — grew from 1923 with the new code; gate 90), `makemigrations --check` clean.

## 2026-09-20 - SPEC-8-03 (Section 8) - builder: currency surfaced in customer-facing serializers and admin ([R-8.11], part 2/2 — completes 390c7ce)

- `orders/serializers.py` — `currency` added to `OrderSerializer` and `OrderItemSerializer` `fields` + `read_only_fields`, beside the money columns each denominates. `OrderSerializer` is the single surface for the checkout response, the dedup replay, and order reads, so all customer-facing money bodies now carry their denomination; `OrderItemSerializer` rides it via `items`. Explicit fields maintained (no `'__all__'`), never writable.
- `orders/admin.py` — `OrderAdmin.list_display` gains `currency` beside `total_amount`/`discount_amount`; added to `readonly_fields` and the "Payment (server-computed — read only)" fieldset so the change page renders the denomination beside the amounts (mirrors the SPEC-8-01 `order_number` integration: minimal, read-only placements). `OrderItemInline` deliberately untouched — outside the part-2 remainder scope, and `tests/test_orderitem_snapshot.py` pins its exact tuple; item currency stays surfaced via `OrderItemSerializer` (+proposed follow-up if admin-inline exposure is wanted).
- `orders/tests.py` (+4, `CurrencyExposureTests`): `currency` read-only in `OrderSerializer.Meta` with API-level list exposure, checkout + dedup-replay + item bodies carry it, `DEFAULT_CURRENCY=USD` override propagates creation → serialization, and admin list_display/readonly/fieldset membership with a rendered changelist ("Currency" column) + change page ("INR" value) — superuser session, in-process, no network.
- No model/migration/settings changes (part 1 owns those); part-1 gateway-payload test (`test_gateway_payload_uses_the_order_currency`) unchanged and passing.
- cycle-2 fix: 88-col reformat of tests.py:232 (added superuser fixture call split across lines — 89 chars → 38/62/9, zero logic change; audit BUG-1).
- Suite: **446 tests, OK (442 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (1938 stmts, 0 miss — unchanged; the surface additions are list/field-membership lines already inside fully-covered files), `makemigrations --check` clean.

## 2026-09-20 - SPEC-8-04 (Section 8) - builder: business-event timestamps on Order — the lifecycle timeline is queryable ([R-8.16])

- `orders/models.py` — six named business-event columns on `Order` (spec 8.3 "Timestamps": distinct timestamps per business event, never overload `updated_at` for one): `paid_at`, `fulfilled_at`, `shipped_at`, `delivered_at`, `cancelled_at`, `refunded_at` — each NULL until its event, written exactly once by the code path performing the event, never mutated once set. `created_at`/`updated_at` already cover Created/Updated; `Published` is a catalogue (Product) event, not an Order lifecycle event, so it is deliberately not an Order column. No index yet: spec 8.3 prescribes indexes from measured query patterns and none of these dates is queried with status today.
- `orders/migrations/0009_order_business_event_timestamps.py` — six `AddField`s (`null=True, blank=True`), no backfill: historical events' times are unknowable, rows stay NULL (documented in the model comment).
- `orders/views.py` — `verify_payment` success path stamps `paid_at` beside the pending→confirmed transition inside the same atomic block (`order.paid_at or timezone.now()` + `update_fields`): rollback-together, and the or-guard pins "written exactly once, never mutated" even beyond the already-processed replay gate. Dedup guard/order-number/snapshot/currency regions byte-unchanged.
- `orders/admin.py` — both cancellation writers stamp `cancelled_at`: the `cancel_pending` bulk action (stamp rides the same `update()` as the status transition; the pending filter makes a re-run a no-op) and the `save_model` change-form path (pending→cancelled is a legal transition there; is-none guard). Exposure: `list_display` gains `paid_at`/`cancelled_at` (the two live writers), all six join `readonly_fields` + the "Timestamps" fieldset — admin edits would falsify the lifecycle record.
- `orders/serializers.py` — all six added to `OrderSerializer` `fields` + `read_only_fields` (the `order_number`/`currency` pattern): the timeline rides every order read (checkout response, dedup replay, list/detail); a client can never claim an event happened.
- `orders/tests.py` (+6, `BusinessEventTimestampTests`): fresh order starts with all six NULL; verify stamps `paid_at` exactly once and a replayed verify never overwrites it; forged signature leaves `paid_at` NULL; admin bulk cancel stamps `cancelled_at` (and only that — cancel-before-payment never implies paid/shipped/refunded) with re-run idempotency; change-form cancel stamps too; serializer + admin expose all six read-only with NULL-over-API until the event. Admin flows driven through the real admin UI (superuser session, in-process, no network).
- Deferred writers (named pattern lands now, writers with their sections): `fulfilled_at`/`shipped_at`/`delivered_at` → fulfilment sections (SPEC-1-05/SPEC-1-08/S10); `refunded_at` → refund flow (SPEC-6-11); `mark_confirmed` deliberately stamps nothing — an unpaid admin confirmation is not a payment event.
- Suite: **452 tests, OK (448 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (1948 stmts, 0 miss — grew from 1938 with the new code), `makemigrations --check` clean.

## 2026-09-20 - SPEC-8-05 (Section 8) - builder: §8.3 "Indexes" starting set — two Order composites + Product category index, everything else satisfied-by-constraint ([R-8.17])

- `orders/models.py` — `Order.Meta.indexes`: `(user, -created_at)` (2557 "Customer ID and order creation date"; newest-first matches the customer order-history sort) and `(status, created_at)` (2567 "Frequently queried status/date combinations") — `orders_user_created_idx` / `orders_status_created_idx`.
- `products/models.py` — `products.Meta.indexes`: `("category",)` (2551 "Product status and category relationships"; category is the public listing/filter key) — `products_category_idx`. The status half of 2551 is N/A: the products table has no lifecycle-status column (stock health is derived, not stored).
- Satisfied-by-constraint (documented in model comments; NO duplicate explicit indexes minted): Product slug 2549 ← `unique=True` (2479); ProductVariant SKU 2553 ← `unique=True` (2481, already DB-pinned by `ProductVariantSkuUniquenessTests`); Order number 2555 ← `order_number unique=True` (also the mint concurrency authority); Payment provider reference 2559 ← no Payment model exists, the provider references live on `Order` (`razorpay_order_id` / `razorpay_payment_id`, both `unique=True`). PRAGMA evidence: each column's index is a `sqlite_autoindex` of origin 'u' (unique-constraint-implied); FK single-column auto-indexes keep `user_id`-only joins covered.
- `orders/migrations/0010_order_orders_user_created_idx_and_more.py` + `products/migrations/0008_products_products_category_idx.py` — pure `AddIndex` operations. Forward + reverse + re-apply verified on a scratch DB via `DATABASE_URL`: unapplying drops exactly the three explicit indexes and leaves constraint-implied autoindexes intact.
- `orders/tests.py` (+5, `OrderIndexSchemaTests`) / `products/tests.py` (+3, `ProductCatalogIndexSchemaTests`): live-DB `introspection.get_constraints` pins — both composites and the category index exist physically; the Meta sets are pinned exactly so future indexes are conscious edits ("from measured query patterns", 2569); constraint-covered columns (order_number, both razorpay refs, slug) must never grow non-unique duplicate indexes.
- Schema-only: no view/serializer/admin/settings changes; `orders/views.py` protected regions byte-untouched.
- Suite: **460 tests, OK (456 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (1952 stmts, 0 miss — grew from 1948 with the new code), `makemigrations --check` clean.

## 2026-09-20 — SPEC-9-01 (Section 9) — builder: checkout submit idempotency — Idempotency-Key honored on POST checkout, unique-by-key dedupe inside the atomic creation block ([R-9.3.14], [R-9.3.19])

- `orders/models.py` — `Order.idempotency_key` (CharField 128, nullable): set once by create_order when the client sends the header, NULL for keyless submissions; plus `Order.Meta.constraints` `UniqueConstraint(user, idempotency_key)` `orders_user_idem_key_uidx` — the concurrency authority for keyed replays (NULLs stay distinct, so keyless rows can never collide; per-user scoping means a reused key on another account is an independent submission, never an existence leak). No expiry: the key lives with the order row it deduped, so a retry collapses onto the original outcome forever.
- `orders/views.py` (NEW hunks only; all five protected regions byte-untouched — diff verified 0 deletion lines): header extraction/validation at the top of create_order (blank → absent; >128 chars → 400 "Idempotency-Key is too long" with no side effects); inside the existing atomic block, AFTER the SPEC-21-1 guard: `select_for_update` on the user row (serializes cross-session same-key submissions — the surface the cart-row lock cannot cover; lock order Cart→User, no cycle with verify_payment's Order→Products→Coupon) then the replay probe — a hit returns the original order 200 and never mints a second order_number, regardless of payload drift, dedup-window expiry, or a settled first attempt; after the mint loop: key bound to the fresh order via `save(update_fields=['idempotency_key'])` in the same transaction.
- Composition with SPEC-21-1 (3391d93), unchanged and behind it: the fingerprint guard stays the first line of defence for keyless same-session double-clicks; the key layer catches what it deliberately does not (drifted payloads, expired window, settled order, cross-session retries). Keyless behavior is pinned unchanged by CheckoutDedupTests.
- `orders/migrations/0011_order_idempotency_key_and_more.py` — `AddField` + `AddConstraint`. Forward + reverse + re-apply verified on a scratch DB via `DATABASE_URL`.
- `orders/test_idempotency.py` (new, 14 tests): creation/binding (key bound, absent-header regression pin, blank-as-absent, 128-char boundary accepted, 129-char 400 with zero side effects, failed submit burns no key), replay semantics (drifted-payload replay returns original 200 with no second order/item/trail row, cross-session replay, window-expiry replay, post-settlement replay, same-key-other-user independence), race authority (duplicate (user, key) rejected, NULL-distinctness for keyless rows, unauthenticated keyed checkout 401). Deterministic pins, no thread scheduling, Razorpay untouched.
- Suite: **474 tests, OK (470 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2098 stmts, 0 miss — grew from 1952 with the new code), `makemigrations --check` clean.

## 2026-09-20 — SPEC-9-02 (Section 9) — builder: /api/v1/ namespace + spec prefix mapping, legacy paths kept as aliases, dual-mount pins ([R-9.0] DEVIATES fix)

- `config/urls.py` — versioned namespace per spec §9 opening (2573–2589): `/api/v1/` carrying the four organizational prefixes — `store` (products/cart/orders + `/store/config` reader, §9.1/§9.3 cart-and-checkout are customer-facing hence store), `account` (accounts app, §9.2), `admin` (dashboard + audit-log, §9.4); the `webhooks` family (line 2587) is RESERVED — no webhook endpoint exists yet and SPEC-1-06 owns that mount (§11/§17 cross-refs already require the namespace). The v1 mounts RE-USE the same urlconf objects as the legacy mounts (dual `include`, zero view duplication); legacy `/api/...` + `/admin/...` paths stay alive as aliases until the coordinated frontend base-URL cutover (S9 ledger note — no frontend change in this task). Nested namespaces (`v1:store:*`, `v1:account:*`, `v1:admin:*`) keep `reverse()` unambiguous — legacy route names keep producing legacy paths (e.g. password-reset email links) until cutover; namespace naming is our reading (spec prescribes prefixes only, no namespace names).
- `config/tests.py` (new, 5 tests): v1 routes reverse under the spec prefixes; legacy `reverse()` output unchanged by the v1 mount; dual-mount identity — one representative pair per family (product list, cart, checkout, store config, register, dashboard, audit-log) resolves both URL families to the identical view callable; live alias proof on the public settings reader (byte-identical JSON via both paths); webhooks prefix pinned unrouted (`Resolver404`) until SPEC-1-06 mounts it.
- Routing-only: no view/serializer/model changes — `orders/views.py` and every other view byte-untouched (no protected-region risk); no migrations; frontend untouched (keeps its legacy base URL).
- Suite: **479 tests, OK (475 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2102 stmts, 0 miss — grew from 2098 with the namespace lines; `config/tests.py` is coverage-omitted per `.coveragerc` `*/tests.py`), `makemigrations --check` clean.

## 2026-09-20 — SPEC-9-03 (Section 9) — builder: uniform error envelope — one middleware converting DRF field errors + ad-hoc {"error":…} bodies into a single response shape ([R-9.2.19])

- Spec reading: §9.2 closes the API contract (spec line 3002) with "Use appropriate HTTP status codes, validation errors and consistent error responses" — it prescribes CONSISTENCY, not a literal shape, so the single shape is defined here and pinned by tests: `{"error": <message str>, "code": <status-family label>, "details": <field errors / context, {} when none>}`.
- `common/errors.py` (new) — `ErrorEnvelopeMiddleware` + helpers. Middleware-level, NOT a `REST_FRAMEWORK EXCEPTION_HANDLER`: most error bodies here are ad-hoc `Response({"error": ...})` returns that raise no exception, so a handler would never see them; middleware sees every response (DRF responses are still unrendered there, so rewriting `response.data` rewrites the body). Recognition is conservative (error payloads = str `error`/`detail` key or list values, i.e. serializer field errors) so non-error JSON 4xx/5xx (the health probe's 503 checks body) passes through byte-identical; HTML bodies and unparseable JSON pass through too. `code` values are status-family labels (400 validation_error, 401 not_authenticated, 403 permission_denied, 404 not_found, 405 method_not_allowed, 409 conflict, 429 throttled, 5xx server_error) — the cause lives in the message.
- `config/settings.py` — middleware appended to `MIDDLEWARE` (with rationale comment). Frontend `frontend/src/lib/api.ts` — `parseErrorBody` promotes envelope `details` content (arrays → fieldErrors for RegisterForm/ResetPasswordClient, scalars → extras for CouponForm's minimum_order_amount); every legacy flat shape still parses unchanged.
- Deliberate assertion migrations (old shape → new shape, never weakened): `orders/tests.py` coupon-uniformity exact-pin `{"error": "Invalid coupon code"}` → `{"error": "Invalid coupon code", "code": "validation_error", "details": {}}`; `orders/tests.py` ×3 `res.data["products"]` → `res.data["details"]["products"]` (checkout stock-gate context); `products/tests.py` ×4 `res.data["detail"]` → `res.data["error"]` (PermissionDenied body); `products/tests.py` ×4 `assertIn(field, res.data)` → `assertIn(field, res.data["details"])` (serializer field errors); `accounts/tests.py` ×9 `res.data["password"]` / `assertIn("password", res.data)` → details-based access; `tests/test_e2e_auth_recovery.py` ×1 same. All 17 sites assert the same semantic content in the envelope's location.
- `tests/test_error_envelope.py` (new, 8 tests): ad-hoc 404 enveloped; DRF permission-denial enveloped; DRF 401 enveloped; serializer field errors land in `details`; scalar context (coupon `minimum_order_amount`) moves into `details`; health-503 checks body untouched byte-for-byte; non-DRF JSON error body enveloped via the content path; unparseable JSON error body passes through (throwaway urlconf).
- Suite: **487 tests, OK (483 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2143 stmts, 0 miss — grew from 2102 with the new code), `makemigrations --check` clean; frontend vitest **24 passed**.

## 2026-09-20 — SPEC-9-04 (Section 9) — builder: customer order-detail endpoint (owner-checked, IDOR-pinned) + order-history pagination ([R-9.2.14], [R-9.2.15])

- Spec reading: §9.2 table rows — `GET /account/orders` = "Customer order history" (lines 2938-2946) and `GET /account/orders/:id` = "Owned order details" (lines 2951-2959). The spec pins NO page size and NO pagination envelope for order history (its only pagination word is "Paginated catalogue", §9.1), so the house page-number envelope (products-listing parity — the shape the frontend's pagination UI is built to) is used with env-driven defaults, documented below.
- `orders/views.py` (new hunks + the order_list tail; all five protected regions byte-untouched — diff verified 0 deletions): `order_detail` — GET `<id>/`, `IsAuthenticated`, ownership inside the lookup (`Order.objects.get(id, user)`), so a foreign user's order AND an unknown id both get the identical 404 `{"error": "Order not found"}` (IDOR pinned: never 200, and no existence leak via a 403); reuses `OrderSerializer` (explicit fields, no `__all__`). `order_list` — pagination via `Paginator` with a total sort (`-created_at, -id` tiebreaker added as an INSERTED line so a paginated partition never repeats/skips rows, the F-12 rationale), `?page` resolved by `Paginator.get_page` (unparsable → 1, past-end → last; never a 404 for a stale page link), `?page_size` resolved by `_history_page_size` (unparsable/non-positive → default, over-cap → capped) returning the envelope `{count, total_pages, current_page, next_page, previous_page, results}`.
- `orders/urls.py` — `path('<int:order_id>/', order_detail, name='order-detail')` (served on BOTH the legacy and `/api/v1/store/` mounts automatically). `config/settings.py` + `.env.example` — `ORDER_HISTORY_PAGE_SIZE` (default 10) and `ORDER_HISTORY_MAX_PAGE_SIZE` (default 100), env-driven per conventions (spec pins no number).
- `orders/tests.py` (+12, `OrderDetailTests` + `OrderHistoryPaginationTests`): owner detail 200 with serializer body; foreign user's order 404 NOT 200 (IDOR pin); unknown id 404; anonymous 401; v1-mirror serve; envelope key set/count/page fields; newest-first ordering with `?page=2` selection under page size 1; `?page_size` override; cap enforcement; invalid `page_size` ("abc"/"0"/"-5") falls back to default; invalid page falls back to page 1; empty history is a valid first page.
- Deliberate assertion migrations for the new list envelope (old bare array → `results`, semantic content unchanged, never weakened): `orders/tests.py` ×4 sites (currency list reads ×2, owner-scoping ids + coupon/price row, timestamps listing row), `orders/test_order_number.py` ×1, `tests/test_orderitem_snapshot.py` ×3, `tests/test_csrf_security.py` ×1, `tests/test_e2e_purchase_flow.py` ×1, `tests/test_e2e_isolation.py` ×3, `tests/test_e2e_auth_recovery.py` ×1. Frontend: `src/lib/orders-api.ts` gains `fetchOrderHistoryPage` (full envelope + page/page_size params) and `fetchOrders` unwraps `results` so both list consumers keep working (no component changes); `src/lib/api.test.ts` ×1 mocked `/api/orders/` success body updated from a bare array to the envelope shape.
- Stated-reason files beyond the declared 5: `config/settings.py` + `.env.example` (env-driven page size mandated by conventions), the 6 cross-file test modules (bare-array pins of the old list shape had to migrate with the envelope), `frontend/src/lib/orders-api.ts` + `src/lib/api.test.ts` (the unpaginated-array consumer would otherwise break — a regression).
- Suite: **499 tests, OK (495 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2167 stmts, 0 miss — grew from 2143 with the new code), `makemigrations --check` clean; frontend vitest **24 passed**, build compiled.

## 2026-09-20 — SPEC-9-06 (Section 9) — builder: REST inventory adjustment — thin endpoint reusing adjust_stock + HasInventoryAdjust, ledger-asserted ([R-9.4.7])

- Spec reading: §9.4 admin-module table, row 7 = Adjustments — `POST /admin/inventory/adjustments` (line 3280), and the §9.4 write-endpoint checklist (lines 3383-3401): required permission / fields / validation / state transition / transaction boundary / audit behaviour are all owned by the shipped service; the view maps HTTP onto it.
- `products/views.py` (new hunk) — `inventory_adjust`: POST-only, `permission_classes([HasInventoryAdjust])` (the existing-but-unused `inventory.adjust` capability class — inventory+admin roles only, no superuser bypass, matching the staff API contract). Validates `product_id` / `delta` / `reason` / optional `note`: missing fields → 400 with the field name; non-integer delta (including bool, the int subclass trap, and floats) → 400; reason checked against `StockMovement.Reason.values` (a bad choice must be a 400, never a 500 at insert); unknown product → 404 `{"error": "Product not found"}`. The mutation itself is exactly `product.adjust_stock(request.user, delta, reason, note)` — its atomic + select_for_update + below-zero-ValueError + StockMovement-ledger contract reused untouched; the ValueError surfaces as a 400 verbatim with nothing written. The movement row IS the audit record ([6.5.17] no silent inventory edits) — no duplicate LogEntry/AuditEvent invented on top. Success: 201 with `{movement_id, product_id, delta, stock_after, reason, note}`. No throttle scope: the endpoint denies anonymous callers (conventions throttle rule targets public mutating endpoints; product write endpoints match this shape).
- `products/urls.py` — `path('inventory/adjustments/', inventory_adjust, name='inventory-adjust')` before the slug catchall (served on both the legacy and `/api/v1/store/` mounts automatically).
- `products/tests.py` (+11, `InventoryAdjustApiTests`), ledger assertions mirroring the service tests' patterns: anonymous 403; catalogue-role (products.write, NOT inventory.adjust) 403 with stock+ledger untouched — the least-privilege split pinned; inventory-role 201; successful +5 restock writes exactly one ledger row (delta, stock_after, reason, note, created_by) with product.stock 10→15; negative delta within stock corrects down (10→6); below-zero rejection 400 with stock unchanged and NO ledger row (service rollback contract); unknown product 404; each missing field 400; non-integer deltas ("abc"/1.5/True) 400; invalid reason 400; v1-mirror serve.
- No model/serializer/admin changes: adjust_stock's schema was already shipped (SPEC-6-02); the admin adjust action is untouched.
- Suite: **510 tests, OK (506 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2195 stmts, 0 miss — grew from 2167 with the new code), `makemigrations --check` clean; frontend vitest **24 passed**, build compiled.

## 2026-09-20 - SPEC-9-05 (Section 9) - builder: coupon apply/remove as persistent cart state, re-validated at checkout ([R-9.3.5],[R-9.3.6])

- Spec reading: §9.3 cart table rows (lines 3073-3094) — `POST /cart/coupon` = Apply coupon, `DELETE /cart/coupon` = Remove coupon, on the same session-scoped cart that backs every other cart route; coupon rule depth stays SPEC-6-09.
- `cart/models.py` + `cart/migrations/0003_cart_coupon.py` — `Cart.coupon` nullable FK to `orders.Coupon` (`on_delete=SET_NULL`, `related_name='+'`): the applied coupon is persistent cart state, not a per-request payload; a deleted coupon drops off carts instead of stranding them on a dangling FK.
- `cart/views.py` (new hunk) — `cart_coupon`: POST applies, DELETE removes; both resolve the session cart WITHOUT lazily creating one (404 `Cart not found`, matching `orders.apply_coupon` so cartless callers learn nothing about code existence), run inside `transaction.atomic()` with `select_for_update()` on the cart row (single-row user-scoped mutations serialized against each other), and are throttled under the existing `cart` mutation scope. Apply delegates the redeemability check to `orders.validate_redeemable_coupon` (uniform `Invalid coupon code` 400, V-11) — no rule logic copied; the FK is only written for a coupon that passes. Remove is idempotent: removing when nothing is applied still returns the cart-family 200 with the cart body (`coupon_code: null`), not 404.
- `orders/views.py` — `validate_redeemable_coupon(code, cart)` extracted from the public preview's inline checks into the one shared gate (preview delegates back to it — rules stay single-source, net rule copies unchanged, checkout's differentiated copy untouched); preview behavior byte-identical (ApplyCouponTests unmodified). Checkout composition: a pure-addition hunk after `coupon_code = request.data.get('coupon_code')` — when the payload posts no explicit code, `cart.coupon.code` becomes the source, so the PRE-EXISTING checkout validation re-runs every rule at checkout time; explicit payload code wins; deletions (38 lines) confined to the non-protected preview block, ZERO deletions in checkout/protected regions (21-1, 8-01, 8-02b, 8-03, 8-04 hunks untouched).
- `cart/serializers.py` — `CartSerializer.coupon_code` (SerializerMethodField, code at minimum) so GET/POST/DELETE cart representations surface the applied coupon for the frontend; additive field, no existing cart-body assertions break. Frontend code untouched (cart API consumer is frontend-owned).
- `cart/urls.py` — `path('coupon/', cart_coupon, name='cart-coupon')`; served on both legacy `/api/cart/` and `/api/v1/store/cart/` mounts automatically (dual-mount resolve pinned).
- `cart/tests.py` (+18, `CartCouponStateTests`): apply persists FK + surfaces code on GET and apply responses; case-insensitive code stored canonically; re-apply replaces; all six rejection reasons share the exact uniform envelope `{"error","code":"validation_error","details":{}}` with nothing persisted; missing code 400; cartless 404 uniform for known/unknown codes (existence-leak guard) and for session-without-cart-row; remove clears, remove-without-coupon idempotent, remove-without-cart 404; cross-session isolation (another session can neither read nor clear my coupon — the session-scoped permission dimension); throttle scope config + apply/remove sharing one mutation budget (throttled remove never lands); dual-mount resolve pin; checkout composition — persisted coupon applied when no code posted (order gets discount 100.00, total 900.00), coupon invalidated after apply rejected through the pre-existing path with NO order created, explicit payload code wins over the persisted one, deleted coupon degrades gracefully (SET_NULL, order proceeds uncouponed).
- Migration dance on a scratch `DATABASE_URL` sqlite DB: forward `migrate` → reverse `migrate cart 0002` → re-apply `migrate cart` — all OK.
- Suite: **528 tests, OK (524 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2231 stmts, 0 miss — grew from 2195 with the new code), `makemigrations --check` clean.

## 2026-09-20 - SPEC-9-07 (Section 9) - builder: admin orders JSON seam + _bulk_set_status WHERE hardening ([R-9.4.8]-[R-9.4.11] + SPEC-6-04 advisory)

- Spec reading: §9.4 admin-module table rows 8-11 (spec lines 3285-3317) — Orders `GET /admin/orders` (3289), Order detail `GET /admin/orders/:id` (3298), Fulfilment `POST /admin/orders/:id/fulfill` (3307), Cancellation `POST /admin/orders/:id/cancel` (3316); Refunds (3325) stays SPEC-6-11 per depth policy. Admin feature depth (UI, refund flows, item-level ops) explicitly out of scope.
- Routing reading: the ratified family mapping mounts admin JSON at `v1:admin` (SPEC-9-02), and the ledger requirement names `GET /api/admin/orders/` — so these are wired like the dashboard/audit-log pair: direct path entries in `config/urls.py` under BOTH families (legacy `/api/admin/orders/...` + `v1/admin/orders/...`), same view objects, no duplication, no orders/urls.py change (that urlconf is already dual-served under the customer/store families; paths added there would also leak the admin seam onto `/api/orders/admin/...`). The §9.4 JSON precedent (SPEC-9-06 inventory adjust) went through the shared app urlconf into `v1/store/` because products.urls is the only mount for those views; here the dashboard pattern is the closer match and the only one that yields the ledger's path.
- `orders/views.py` (new hunks ONLY — imports + EOF append, 171 insertions / 0 deletions; protected regions 21-1, 8-01, 8-02b, 8-03, 8-04 byte-untouched): `admin_order_list` (unscoped order list, house page-number envelope + `ORDER_HISTORY_PAGE_SIZE`/cap config — no caller, staff included, gets an unbounded page), `admin_order_detail` (uniform 404 for unknown ids), `admin_order_fulfill` (advances one legal step per call: pending→confirmed→shipped→delivered, 409 with the machine's allowed-set in `details` on terminal/illegal states), `admin_order_cancel` (pending-only; 409 with the refund-reconciliation hint mirrors the admin wording; idempotent on cancelled via the machine's self-transition — 200 no-op, no second stamp, no duplicate audit row). Permission classes: `HasOrdersRead` / `HasOrdersFulfill` / `HasOrdersCancel` — defined in common/permissions.py since SPEC-6-03 but previously unmounted; now wired exactly as the factory names anticipated. Writes run `transaction.atomic()` + `select_for_update()` on the order row so the gate-then-update pair cannot double-pass on a stale status; every accepted write lands a privileged-action LogEntry via `common.audit.log_api_action` (the API-side twin of the admin's bulk-action trail, [6.12.6]); cancel stamps `cancelled_at` beside the transition exactly like admin `cancel_pending` (is-none guard keeps a set stamp immutable). Fulfil writes NO business-event stamps — admin `mark_shipped`/`mark_delivered` don't either; the named-stamp writers are their own later task and the JSON seam never records more than the admin surface for the same transition. No throttle scope: the endpoints deny anonymous callers outright (conventions' throttle rule targets public mutating endpoints — SPEC-9-06 same shape).
- `orders/admin.py` — SPEC-6-04 advisory hardening (sanctioned surgical edit, NOT a protected region): `_bulk_set_status`'s UPDATE now re-applies `status__in=allowed_from` in its WHERE clause. The pk snapshot and the UPDATE are two statements; a row whose status changes in between (e.g. concurrently cancelled while pending) matched by pk alone and would be swept to the new status — resurrecting a cancelled financial record. The transition predicate is now the final authority: out-of-set rows can never be written, only counted as skipped.
- `orders/tests.py` (+21): `AdminOrdersApiTests` — permission matrix (anon 403, unprivileged customer 403, finance `orders.read` 200 on list/detail unscoped, finance denied fulfill/cancel with nothing moved and zero LogEntry rows); every error pinned in the uniform envelope (`error`/`code`/`details`, codes `permission_denied`/`not_found`/`conflict`); fulfil happy path + one-step-per-call walk + terminal-state 409s (delivered AND cancelled, the pinned illegal transition) + unknown-id 404 + audit-row assertions (user, CHANGE flag, message); cancel happy path + `cancelled_at` stamp + paid-order 409 with refund hint + delivered 409 + idempotent replay (stamp identical, LogEntry count unchanged) + 404; list envelope shape; v1:admin dual-mount serves. `BulkSetStatusRaceGuardTests` — deterministic race interleave via a queryset subclass that flips a matched pending row to cancelled between the pk snapshot and the UPDATE: out-of-set row stays cancelled (old code resurrected it to confirmed), control row still transitions.
- Suite: **549 tests, OK (545 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2286 stmts, 0 miss — grew from 2231), `makemigrations --check` clean (no migrations).

## 2026-09-20 - SPEC-9-08 (Section 9) - builder: write-endpoint contract doc - per-endpoint 10-point R-9.5 table ([R-9.5])

- Spec reading: §9.5 checklist (spec lines 3383-3403) — "For every write endpoint, specify:" the ten points (required permission / required request fields / validation constraints / expected state transition / transaction boundaries / idempotency requirements / audit-log behaviour / success response / possible errors / notifications or background jobs), mirrored verbatim as the table's columns; a method+path column fronts them.
- `backend/docs/api-contract.md` (new, docs-only) — one row per write endpoint, **24 rows**, enumerated from the actual urlconfs: accounts 8 POSTs (register, login, token refresh, verify-email, resend-verification, forgot-username, password-reset, password-reset confirm), products 5 (POST create, PUT/PATCH/DELETE by slug, POST inventory adjustments), cart 5 (POST add item, PATCH/DELETE item, POST/DELETE coupon), orders 4 (checkout, apply-coupon preview, payment intent, payment verify), admin orders 2 (fulfill, cancel — shipped in SPEC-9-07 this batch). Every cell taken from the mounted views (permission classes + throttle scopes, field names, validation rules, atomic/select_for_update boundaries, idempotency contracts, AuditEvent/LogEntry behaviour, response bodies, error codes, notification dispatches) — nothing aspirational.
- Mount policy: the v1 mirror is documented ONCE (family mapping note: `/api/v1/account|store|admin/...`), not per row — both mounts serve one urlconf object. GET endpoints are excluded per the checklist's own scope ("every WRITE endpoint") — order detail/list GETs (SPEC-9-04/9-07) have no row; the Django admin site is HTML chrome, outside the API contract. The uniform error envelope (SPEC-9-03) is stated once above the table rather than repeated in every errors cell.
- No code, no tests, no settings changes. Suite state unchanged from the SPEC-9-07 row above (549 tests OK, cov 100.00%); docs-only commit re-verified by `makemigrations --check` cleanliness.

## 2026-09-20 - SPEC-10-01a (Section 10) - builder: lifecycle dimension schema - orders/state.py single source + payment_status/fulfilment_status fields + 0012 total backfill ([R-10.1],[R-10.3])

- Spec reading: §10.2 separate state dimensions (3421-3487) — payment (pending/authorized/captured/failed/partially refunded/refunded) and fulfilment (unfulfilled/partially fulfilled/fulfilled) split the conflated legacy single status; ambiguous values like "success"/"done" deliberately absent; §10.3 transition rules (3489-3522) pinned unchanged. R-10.1 DEVIATES fix is ADDITIVE: the legacy `status` column remains the compat surface (frontend/admin filters/existing tests read it — all green unmodified); nothing consumes the new fields as authority in this batch.
- `orders/state.py` (new, dependency-free so migrations import it safely) — the SINGLE SOURCE of truth for the order machine: STATUS_CHOICES, ALLOWED_TRANSITIONS + `transition_allowed`, ADMIN_FULFILMENT_NEXT consolidated verbatim from models.py/admin.py/views.py (no duplicate constants left — identity-pinned by test); NEW PAYMENT_STATUS_CHOICES, FULFILMENT_STATUS_CHOICES, LEGACY_STATUS_DIMENSIONS (total map: every legacy status → both dimensions) + `payment_for_status`/`fulfilment_for_status` helpers for the 01b writers.
- `orders/models.py` — `Order.payment_status` + `Order.fulfilment_status` (CharField choices from state.py, defaults 'pending'/'unfulfilled', null=False, short help_texts). NO db_index: §8.3's prescribed starting set (SPEC-8-05) does not include these columns; indexes come from measured query patterns (same policy as the event timestamps). `Order.STATUS_CHOICES` stays as an alias of state.STATUS_CHOICES (ops dashboard consumes it). Named imports (not `from . import state`) because the Order.address `state` field shadows the module inside the class body.
- `orders/migrations/0012_order_fulfilment_status_order_payment_status.py` — two AddFields (autodetected, frozen literal choices) + RunPython backfill importing orders.state.LEGACY_STATUS_DIMENSIONS (provenance documented: state.py is dependency-free so the import works at any migration state on a fresh DB; mapping edit can never drift between machine and backfill). Reverse = AddField reverse (columns dropped), `RunPython.noop`.
- `orders/serializers.py` — `payment_status`/`fulfilment_status` added to OrderSerializer fields + read_only_fields (explicit lists, machine-maintained like status).
- Consolidation edits (no behavior change): admin.py/views.py now import the machine from orders.state; views.py drops its local ADMIN_FULFILMENT_NEXT copy (views.py import swap only — verify_payment untouched; protected regions byte-frozen).
- `orders/tests.py` (+9): `OrderStateSourceTests` (identity pins prove single-sourcing; transition_allowed machine edges pinned incl. self-transitions + unknown-source; ADMIN_FULFILMENT_NEXT pin; mapping totality over STATUS_CHOICES); `LifecycleDimensionsFieldTests` (every legacy status → both dimensions per the mapping table + helpers agree; new orders start pending/unfulfilled; serializer Meta + API-body exposure pins); `LifecycleBackfillMigrationTests` (TransactionTestCase mirrors the 0008 pattern: drop to 0011, raw-insert one pre-dimension row per legacy status — currency NOT NULL included — migrate to head, assert BOTH columns per subTest for every legacy status).
- Migration dance on a scratch `DATABASE_URL` sqlite DB: forward `migrate` ✓ → reverse `migrate orders 0011` ✓ (PRAGMA: both columns dropped) → re-apply `migrate` ✓ (both columns back) — all exit 0.
- Suite: **558 tests, OK (554 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2298 stmts, 0 miss — grew from 2286), `makemigrations --check` clean.

## 2026-09-20 - SPEC-10-01b (Section 10) - builder: lifecycle dimension wiring - verify_payment captures payment dimension; admin + JSON seam sync fulfilment ([R-10.1],[R-10.3])

- Spec reading: §10.2/§10.3 continued — the writers keep the additive dimensions in sync with every legacy-status change (R-10.1 DEVIATES contract: legacy `status` stays the compat surface; no consumer is forced onto the new fields; COD/failure payment states are SPEC-10-04 and were NOT added).
- `orders/views.py` — **22 insertions / 0 deletions** (every hunk insertion-only; protected regions byte-frozen: dedup guard 21-1, order-number mint 8-01, snapshot writes 8-02b, currency 8-03, paid_at or-guard + frozen save 8-04 — all appear as context lines only). Three writer sites: (1) verify_payment sets `payment_status = payment_for_status('confirmed')` right after the frozen save — a second `save(update_fields=['payment_status'])` of the already-locked row in the SAME atomic block, because the 8-04 save line is byte-frozen so the dimension could not be added to its update_fields; both UPDATEs commit or roll back together, and the already-processed gate keeps the path unreachable on replay; (2) `admin_order_fulfill` sets `fulfilment_status = fulfilment_for_status(target)` beside the transition; (3) `admin_order_cancel` sets `fulfilment_for_status("cancelled")` (replay branch untouched). Checkout's `status="pending"` creates need no hunk — the field defaults ARE the pending-machine state.
- `orders/admin.py` (surgical edits sanctioned, NOT a protected region) — `save_model` syncs `fulfilment_for_status(obj.status)` after the transition guard (single-object path); `_bulk_set_status` adds `fulfilment_status=fulfilment_for_status(new_status)` to the SAME UPDATE — the 9-07 WHERE hardening (`pk__in=matched_pks, status__in=allowed_from`) preserved verbatim, only the SET list grew; `cancel_pending` rides the dimension into its existing stamp-update. Admin NEVER sets payment_status (pinned by tests, paid rows included).
- `orders/tests.py` (+9, `LifecycleWiringTests`): verify captures payment dimension (legacy status/paid_at semantics untouched, 8-04 pins green unmodified); audit-failure rollback reverts the captured dimension to pending (rollback-together); admin change form moves the dimension with the legal transition and an illegal transition moves nothing; bulk ship syncs per-row and the out-of-set row is skipped with dimensions intact (9-07 hardening re-proven); bulk cancel keeps payment untouched; JSON seam fulfil walk syncs confirmed/unfulfilled → shipped/fulfilled → delivered/fulfilled and cancel lands unfulfilled; serializer surfaces both dimensions post-transition on the admin detail AND customer list bodies. `_order_with_status` helper builds rows exactly as the writers would leave them (status + both dimensions per state.py) so assertions test real deltas, not setup artifacts.
- Suite: **567 tests, OK (563 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2306 stmts, 0 miss — grew from 2298), `makemigrations --check` clean.

## 2026-09-20 - SPEC-10-02 (Section 10) - builder: immutable transition audit trail - OrderStatusEvent written by every legal status writer; bulk path reworked to per-row saves ([R-10.12],[R-10.17],[R-10.18])

- Spec reading (§10.3 transition rules, lines 3491-3509): each transition carries an "Audit record" (R-10.12) whose content names the "Actor or triggering event" (R-10.17 — the event carries both: `actor` + `trigger`, either sufficing, so verify_payment writes actor NULL + trigger `payment_verify`); the trail is append-only (R-10.18 — immutability via a model save guard + view-only admin registration, nothing more, per the design bar). Creation event: written at checkout (NULL→pending) inside the existing atomic block — the spec's lifecycle diagram starts the machine at pending, and a trail that opened mid-life would leave every order's first transition unrecorded. No dimension events: the trail records the LEGACY status transition per R-10.12's from→to wording (the dimensions are derived state, SPEC-10-01a's mapping, not independently-written machine states). No backfill: the table is new, historical transitions are unknowable (same reasoning as the 8-16 timestamp columns).
- `orders/state.py` (+24) — trigger vocabulary single-sourced beside the machine it audits: six `TRIGGER_*` constants + `STATUS_EVENT_TRIGGERS` choices (order_create, admin_change_form, admin_bulk_action, payment_verify, admin_api_fulfil, admin_api_cancel); a new writer cannot invent an unregistered source.
- `orders/models.py` (+80) — `OrderStatusEvent`: order FK (`related_name='status_events'`, CASCADE like OrderItem), `from_status` (NULL exactly for creation events), `to_status`, `actor` (SET_NULL — deleting a user must never cascade into the audit trail), `trigger` (choices from state.py), `created_at` auto_now_add; Meta ordering newest-first with id tiebreaker; `save()` raises TypeError on any pk-set re-save (append-only guard, test-pinned); docstring cross-references SPEC-7-01's LogEntry trail (complementary, not replacement). `orders/migrations/0013_orderstatusevent.py` — CreateModel; forward/reverse/re-apply proven on a scratch DATABASE_URL sqlite DB.
- `orders/admin.py` — (1) `save_model`: event after `super().save_model()` inside an explicit `transaction.atomic()` (changeform view is already atomic; the block keeps rollback-together local), self-transitions write no event; (2) `_bulk_set_status` REWORKED: the single `.update()` becomes per-row saves in one atomic block — same selection predicate, each row's status re-checked at write time under `select_for_update()` (the 9-07 WHERE hardening's final-authority principle, per row), out-of-set rows skipped not swept; per-row saves fire auto_now, fixing the bulk path's stale `updated_at` (empirically: on Django 6.1 even `save(update_fields=...)` skips auto_now unless listed, so `updated_at` rides update_fields explicitly — pinned by test); (3) `cancel_pending` reworked identically — the re-check additionally closes the 9-07-shaped race its old `pk__in`-only UPDATE still had (a row flipped pending→confirmed between snapshot and write would have been swept to cancelled; now skipped); (4) `OrderStatusEventAdmin`: view-only registration — staff read via `orders.read`, add/delete hard-False even for the superuser, every field readonly (change form renders as a view); `_append_status_event` helper shared by the three admin writers.
- `orders/views.py` — **64 insertions / 0 deletions** (every hunk insertion-only; own import lines per the 10-01b pattern; the five byte-frozen protected regions appear as context only). Four writer sites: (1) checkout creation event after the order-number mint loop, inside the outer atomic block; (2) verify_payment — `previous_status` captured just before the frozen 8-04 region, event after the payment_status save, actor None + trigger `payment_verify`; (3) `admin_order_fulfill` — event with `request.user` + trigger `admin_api_fulfil`; (4) `admin_order_cancel` — event with trigger `admin_api_cancel`, unreachable on the idempotent replay branch (no second event).
- `orders/tests.py` (+15 net: `TransitionAuditTrailTests` with 15 tests + race-guard extension): creation event at checkout; verify event (actor NULL, trigger names the source); failed verify writes nothing; rollback-together pinned three ways — verify (audit failure after the event reverts it), change form (event-create failure reverts the whole save), bulk (implied by the atomic block + per-row tests); change-form legal event with actor, illegal/no-op saves write none; bulk per-row events with fresh `updated_at` (`assertGreater` vs the pre-action value) and no event for the skipped row; bulk-cancel events; JSON-seam fulfil walk (pending→confirmed→shipped, staff actor) and cancel with replay appending no second event; append-only save guard raises; admin surface view-only (staff: view yes/mutate no; superuser: add/delete refused); meta pins (trigger choices single-sourced, ordering, verbose name, `__str__`). `BulkSetStatusRaceGuardTests.test_out_of_set_row_is_never_swept_by_the_bulk_update` EXTENDED (enumerated rework): mechanism unchanged (RacingQuerySet.values_list interleave still works against the per-row re-check), added assertions pin the audit layer — the skipped row gets no sweep event, the swept row exactly one; intent preserved (out-of-set rows never swept), still deterministic (no threads/sleeps).
- Suite: **582 tests, OK (578 pass, 4 pre-existing `expectedFailure` unchanged)**, coverage **100.00%** (2378 stmts, 0 miss — grew from 2306), `makemigrations --check` clean.

## 2026-09-20 - SPEC-10-03 (Section 10) - builder: shipped-transition preconditions - payment-captured + items-present gate every writer that can reach shipped; state.py registry is the SPEC-1-08 extension hook ([R-10.19],[R-10.14])

- Spec reading (§10.3, lines 3489-3522): each transition carries "Preconditions"; the mark-shipped example requires the order to have fulfilment-ready items (plus operator permission, already enforced by `orders.fulfill` capabilities). Handoff pins the precondition set to: `payment_status == 'captured'` AND the order has `OrderItem` rows.
- `orders/state.py` (+33) — the machine's edges say WHICH moves are legal; preconditions say what must be TRUE about the row. New dependency-free hook: `TRANSITION_PRECONDITIONS` registry + `register_transition_preconditions(status, *checks)` (append-on-purpose: built-ins and SPEC-1-08's shipment checks compose) + `precondition_failures(order, new_status)` as the ONLY evaluation point (callables receive the Order, return human-readable failure reasons; empty = clear). Stays dependency-free: the module docstring's no-Django-imports contract (migrations import it) is why the built-ins live models-side.
- `orders/models.py` (+30) — the ORM-backed built-ins beside the model they inspect, self-registering at import (models is imported by every writer surface, so the registry is populated before any writer runs): `_require_captured_payment` (a shipped order whose payment later fails is un-reconcilable — no refund flow yet, V-03) and `_require_items_to_ship` (spec 10.3's "fulfilment-ready items"); both registered against "shipped" only.
- `orders/admin.py` — (1) `save_model`: after the machine gate, an unmet precondition aborts the save exactly like an illegal edge (messages.ERROR + return, status unchanged, no audit event); (2) `_bulk_set_status`: per-row precondition check under the row lock, same final-authority shape as the status re-check — unmet rows are skipped, never swept, and reported in their own WARNING ("N order(s) skipped — 'shipped' preconditions unmet: ...") distinct from the status-skip message (which now fires only on genuine status skips, preserving its old text and behavior when no precondition skips exist). 9-07 hardening (pk__in + status re-check) untouched.
- `orders/views.py` — **23 insertions / 1 import-line reshaped (0 logical deletions)**: `admin_order_fulfill` also advances confirmed→shipped (the handoff's "the JSON seam has no shipped endpoint" assumption was wrong — it does, via ADMIN_FULFILMENT_NEXT), so the preconditions gate it with the same authority (insertion-only hunk after the machine gate; 409 carrying the reasons — the envelope middleware exposes them at `details.preconditions`; the 9-07 write below stays byte-identical, protected regions context-only).
- `orders/tests.py` (+10 `ShippedPreconditionTests` + fixture updates): bulk ship rejects uncaptured payment / itemless rows (status unchanged, NO audit event, skip message names the unmet precondition) with an eligible row shipping alongside; happy path (paid + items → shipped + fulfilment sync + bulk-action event with actor); change form rejects both precondition failures and still delivers an itemless order (registry is per-status, not global); JSON seam rejects the unpaid ship with reasons at `details.preconditions` and ships the paid+itemful row; hook pins — shipped carries exactly the two built-ins, `precondition_failures` evaluates clean for unregistered statuses, a patched-in check evaluates AND blocks an otherwise-eligible ship through the real bulk writer (writers use the registry, not hardcoded checks; `mock.patch.dict` keeps the process registry leak-free). Existing shipped-path fixtures updated to the rows real writers produce (items and/or captured payment in orders/tests.py ×5, tests/test_e2e_admin_ops.py ×2, tests/test_admin_rbac.py setUp) — assertions unchanged.
- Suite: **592 tests, OK (588 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2410 stmts, 0 miss — grew from 2378), `makemigrations --check` clean.

## 2026-09-20 - SPEC-10-05 (Section 10) - builder: side-effect contract per transition - one dispatch point (orders/events.py) firing the shared notifications seam on shipped/delivered/cancelled; hook failure is log-only ([R-10.16])

- Spec reading (§10.3, lines 3489-3509): each transition declares its "Side effects" and "Failure/retry behaviour". Minimal contract per handoff: shipped/delivered/cancelled fire ONE notification hook; the notifications subsystem itself (handlers/templates/outbox) stays SPEC-1-12/S19 — only the seam is wired.
- `orders/events.py` (new, single dispatch point) — `TRANSITION_EVENTS` (shipped→"order.shipped", delivered→"order.delivered", cancelled→"order.cancelled"; dotted names follow the AuditEvent.EventType convention, so SPEC-1-12/S19's future EventType members + handlers bind without touching the hook sites) + `notify_transition(order, from_status, to_status)`. REUSES the existing `common.notifications.dispatch` seam (the R-19.0 dispatch verify_payment already calls for order.paid) — no second send path invented; confirmed is deliberately unhooked (the payment notification already has its owner). Placement documented: writers call it AFTER the transition save + OrderStatusEvent append, INSIDE the same `transaction.atomic()` block — the in-transaction call is safe by construction (dispatch never raises, wrapper swallows anyway; the pre-commit-send caveat is common/notifications.py's documented, accepted substrate cost). Hook cardinality equals the audit trail's: skips and idempotent replays return before the notify site.
- `orders/admin.py` (+16/-0) and `orders/views.py` (+11/-0, both hunks insertion-only; protected regions context-only) — the hook rides all five real writers: `_bulk_set_status` (shipped/delivered; `previous_status` captured beside the existing event's from_status), `cancel_pending`, `save_model` (change form), `admin_order_fulfill`, `admin_order_cancel`. verify_payment untouched.
- `orders/tests.py` (+8 `TransitionNotificationTests`): bulk ship→shipped hook then ship→delivered hook with exact payloads (`("order.shipped", {"order": order})`); change-form ship fires the hook; JSON-seam fulfil fires shipped; bulk cancel AND seam cancel each fire cancelled (distinct orders — cart reseeded so the checkout dedup guard doesn't replay row 1); rollback isolation pinned — `dispatch` mocked to raise leaves the request 200, the order shipped, the fulfilment dimension synced AND the audit row committed (a notification failure can never fail or roll back its transition); non-hooked transitions fire nothing (mark_confirmed through the bulk writer, `assert_not_called`); the hook map is pinned to exactly the three lifecycle events (`confirmed` asserted absent).
- Suite: **599 tests, OK (595 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2431 stmts, 0 miss — grew from 2410), `makemigrations --check` clean.

## 2026-09-20 - SPEC-10-04 (Section 10) - builder: COD branch + failed/retryable payment states in the order machine ([R-10.2],[R-10.4],[R-10.17])

- Spec reading (§10.1 line 3420 + §10.2/10.3): the machine "must explicitly handle ... cash-on-delivery orders, failed payments"; the payment dimension already carries `failed` (10.2). COD CAPTURE READING: spec 10.1 names COD handling but prescribes NO capture timing and no COD lifecycle text exists in §10 — implemented as the precondition variant (COD orders fulfil without pre-payment; capture belongs to the delivery surface) rather than a capture-at-delivery writer, and no new notification/notification-free behavior was invented. FAILED/RETRY READING: §10.3's per-transition "Failure/retry behaviour" — a failed payment verification marks the payment dimension `failed` while the ORDER status stays `pending` (retryable: no cancellation, no stock movement, no capture claim), and a later successful verify performs `failed -> captured` with the full success path.
- `orders/state.py` (+49) — machine vocabulary + the payment dimension's own transition table: `PAYMENT_METHOD_PREPAID/COD` + `PAYMENT_METHOD_CHOICES` (the checkout INPUT stays a checkout-section row; this is the marker the row carries), `PAYMENT_ALLOWED_TRANSITIONS` + `payment_transition_allowed` (mirrors `transition_allowed`; declares the retry edge `failed -> captured` plus the not-yet-written authorized/refund edges so the machine is complete and SPEC-1-05/6-12 pin against a table that already answers them), `TRIGGER_PAYMENT_FAILED` registered in `STATUS_EVENT_TRIGGERS` (the audit trail cannot grow unregistered sources, 10-02 contract).
- `orders/models.py` (+28/-1) — `Order.payment_method` (choices from state.py, literal default `prepaid` = the store's exact current behavior; no checkout input exists or was added); `_require_captured_payment` gains the COD variant: waives for `payment_method == 'cod'` (shipped precondition becomes items-only via the same registry — still exactly the two built-ins, 10-03 pin green), prepaid behavior byte-identical.
- `orders/migrations/0014_order_payment_method_alter_orderstatusevent_trigger.py` — AddField with total literal backfill default `prepaid` (deterministic, never environment-config) + AlterField for the new trigger choice. Dance on a scratch `DATABASE_URL` sqlite DB: forward (legacy row backfilled `prepaid`), reverse (column dropped, row preserved), re-apply (row intact, `prepaid`).
- `orders/views.py` — **46 insertions / 0 deletions** (own import lines per the 10-01b/10-02 pattern; the five byte-frozen protected regions context-only, unmodified). verify_payment's signature-failure branch marks the payment: inside one `transaction.atomic()`, the caller's own order whose stored `razorpay_order_id` equals the claimed ref, still `pending`, no capture claim, and `payment_transition_allowed(status, 'failed')` — writes `payment_status='failed'` + one OrderStatusEvent (from == to `'pending'`, trigger `payment_failed`, actor NULL). Guards make a forged/mismatched reference unable to write payment state; the 400 response stays byte-identical (no existence leak); the already-processed/replay pins are untouched and green.
- `orders/serializers.py` (+6/-0) — `payment_method` exposed read-only beside the lifecycle dimensions (order reads; nothing client-writable). `orders/admin.py` untouched (visibility deferred — the ≤7-file scope cap; a read-only changelist/fieldset slot is the natural follow-up).
- `orders/tests.py` (+11 net): state pins (payment-dimension edges incl. retry + totality over choices, method vocabulary, failure trigger registered); COD (precondition items-only + prepaid twin still gated, bulk ship of a COD row leaves `payment_status` pending with the standard audit/dimension contract, COD ship fires the 10-05 shipped hook); failed/retry (failure marks `failed` and stays retryable, retry captures normally with stock decremented exactly once, forged-ref failure writes nothing, already-processed row cannot be flapped to `failed`, exposure/default pin). `test_failed_verify_appends_no_event` RENAMED/REWRITTEN to `test_failed_verify_appends_the_payment_failed_event` — the 10-02-era pin contradicts this spec's mandated failure event; superseded, not weakened (the new assertions pin the event's from==to shape + trigger + actor).
- Suite: **610 tests, OK (606 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2449 stmts, 0 miss — grew from 2431), `makemigrations --check` clean.

## 2026-09-20 - SPEC-11-01 (Section 11) - builder: create_payment atomicity + IntegrityError retry + payment throttle scope ([R-11.1] DEVIATES row)

- Spec reading (§11 lines 3523-3578): the ledgered DEVIATES row is the only build-now item — create_payment's unlocked check-then-act on `razorpay_order_id` breaches conventions.md:16,17 (atomic + select_for_update on side-effectful flows; IntegrityError retry as the unique-constraint authority), and conventions.md:24 (public mutating endpoints carry a throttle scope). Pinned behaviors held: the 9-01 idempotent reuse path still emits nothing, test-gaps row 36 (reuse / 404 owner-scope / non-pending 400) stays green, xfail ceiling untouched at 4.
- `orders/views.py` (+42/-12, one hunk replaced, all protected regions context-only and unmodified — the [R-8.11] currency block, [SPEC-7-02] gateway-failure re-raise and the [R-7.20] comment are byte-identical; the 9-01 reuse branch untouched): (1) the intent persistence now re-fetches the order row under `select_for_update()` INSIDE the existing `transaction.atomic()` block, so the loser of a race adopts the winner's committed id instead of double-writing; (2) that atomic block is wrapped in a bounded `IntegrityError` retry loop (`PAYMENT_INTENT_ATTEMPTS = 3`, mirroring ORDER_NUMBER_ATTEMPTS: the unique constraint on razorpay_order_id is the authority, one retry converges, exhaustion re-raises — 500 semantics preserved); (3) `@throttle_scope('payment')` on create_payment. DECLARED DELETION (non-protected block, S9-05 precedent): the old in-view persistence statements `with transaction.atomic(): order.razorpay_order_id = razorpay_order_id; order.save(update_fields=['razorpay_order_id']); AuditEvent.record(PAYMENT_INITIATED, actor=request.user, order=order, detail={razorpay_order_id, amount_paise})` — their substance is preserved verbatim inside the locked retry (same write, same audit shape/actor/detail, `order=locked`).
- `config/settings.py` (+4) — `'payment': os.getenv('PAYMENT_THROTTLE_RATE', '10/min')` in DEFAULT_THROTTLE_RATES, the repo's env-driven pattern; no hardcoded rate in code.
- `backend/.env.example` (+4/-1) — `PAYMENT_THROTTLE_RATE=10/min` documented beside the other throttle keys with the why (each accepted request calls the gateway and writes a payment event).
- `orders/tests.py` (+145/-2): `CreatePaymentThrottleTests` — scope/ScopedRateThrottle/rate-key pin (default `10/min`) and a hermetic 429 test (rate patched on ScopedRateThrottle.THROTTLE_RATES + cache cleared around the window, no sleeps, no real time); `CreatePaymentAtomicityTests` — unique-violation retry converges (exactly 2 save attempts, id persisted, exactly one PAYMENT_INITIATED audit), exhausted retries re-raise IntegrityError with nothing committed (no id, no audit), and the race-loser path adopts the committed winner's id while emitting nothing (locked re-read pinned to return a row already carrying the winner's id — SQLite runs requests serially, so the interleaving is simulated at the seam; the loser's gateway mint is asserted called-once and its audit count zero).
- Suite: **615 tests, OK (611 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2462 stmts, 0 miss — grew from 2449), `makemigrations --check` clean.

## 2026-09-21 - SPEC-12-01 (Section 12) - builder: StockReservation model + env-driven RESERVATION_TTL for the inventory reservation lifecycle ([R-12.2]/[R-12.3]/[R-12.4]/[R-12.10])

- Spec reading (§12 lines 3579-3633): the data-model task only. §12.1's on-hand/reserved/available-to-sell split maps onto the schema as: on-hand stays `products.stock` (the only authority), reserved = Σ of a product's active reservations' `quantity`, available-to-sell is the derived difference (never stored — the reserved/safety-field accounting depth is SPEC-6-13's), expiry = `expires_at`, owner/reference = the `owner` + `order` FKs. Lifecycle writers are NOT here: SPEC-12-02 (create in create_order's atomic block, convert in verify_payment's locked block) and SPEC-12-03 (reconciler). `orders/views.py` untouched (byte-frozen regions); no admin registration (§12.1 prescribes none).
- `products/models.py` (+104): `StockReservation` — `Status` TextChoices {active, converted, released, expired} (§12.1's recommended-checkout vocabulary: minted active step 3, converted step 5, released step 6, expired step 7); FKs `product` (products, CASCADE), `order` ("orders.Order" — the file's existing cross-app string-FK pattern, cf. StockMovement.created_by, no orders import at model load), `owner` ("auth.User", CASCADE); `quantity` PositiveBigIntegerField (shares the products.stock / StockMovement.stock_after unit domain); `status` CharField(20, choices, default active); `expires_at`; created/updated timestamps. Meta: UniqueConstraint (order, product) `uniq_order_product_reservation` — §12.1 implies it (reserved inventory is a per-checkout hold with an owner/reference; a duplicate row would double-count reserved stock, and a retried checkout must re-target the existing hold, not mint a second); composite Index (status, expires_at) `stockres_status_expires_idx` serving the reconciler's status=active AND expires_at<=now sweep. Classmethod `expiry_from_now()` reads settings.RESERVATION_TTL at call time (ops/services.py:9 pattern — env-driven, never frozen at import).
- `products/migrations/0009_stockreservation.py` (generated, +37): CreateModel with both constraint/index options, dependency on orders 0014 + auth user model. Migration dance on a scratch sqlite DB via DATABASE_URL env var, hermetic: phase 1 full forward `migrate` (0009 applied, exit 0), phase 2 reverse `migrate products 0008` (unapplied, exit 0), phase 3 re-apply `migrate products` (exit 0), plus `showmigrations` confirming 0009 [X]; scratch DB deleted.
- `config/settings.py` (+9): `RESERVATION_TTL = _env_int('RESERVATION_TTL', 900)` — seconds a checkout hold survives before it goes stale and the reconciler (SPEC-12-03) may release it; documented as coordinated with the payment-provider session window (customer must be able to finish paying inside the hold); fail-safe parse per the house pattern.
- `backend/.env.example` (+8): `RESERVATION_TTL=900` documented beside the other deployment tunables with the why.
- `products/tests.py` (+254/-2): `StockReservationModelTests` (11 tests) — field-set pin; status vocabulary/default pin; FK targets + cascade policy + reverse accessors; (order, product) uniqueness pinned twice (forced-collision IntegrityError — savepoint-wrapped so the TestCase transaction survives — and live DB constraint introspection, the ProductVariant.sku/Order.order_number method); cross-order independent holds with Σ active reserved; reconciler sweep exact on both axes (stale active matched; fresh active and converted/released/expired with past expiry not); quantity column-type parity with products.stock; Meta constraint/index pin; TTL read at call time (override 60→120 moves the expiry); `_env_int` fail-safe parse (absent → 900, malformed → 900, set → parsed); `__str__` shape.
- Suite: **626 tests, OK (622 pass, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2490 stmts, 0 miss — grew from 2462), `makemigrations --check` clean.

## 2026-09-21 - SPEC-12-02 (Section 12) - builder: checkout reservation lifecycle — create at checkout, convert at verify, release on failed/cancelled paths, deterministic product-lock ordering ([R-12.6]/[R-12.7]/[R-12.8])

- Spec reading (§12 lines 3579-3633): §12.2's "single atomic operation that succeeds only when sufficient stock is available" maps to the CONVERSION/decrement step (the stock-consuming moment), which stays one locked atomic op — sufficiency re-check + decrement + SALE ledger row under product row locks, byte-unchanged as the oversell backstop. Reservation MINTING is deliberately lock-free and advisory (§12.1 step 3 follows step 2's validation): two concurrent last-unit checkouts both mint (soft overbooking, the section-12 verified-facts by-design pay-then-409 residual) and the verify backstop 409s the loser — hard oversell stays IMPOSSIBLE, no check-then-act introduced (R-12.12).
- Declared judgment calls: (1) conversion filters `status=active` ONLY — a hold released by a failed attempt is terminal and is never resurrected into a sale (the 10-04 retry re-checks stock cleanly with no conversion); (2) a LAPSED TTL is NOT a conversion gate — §12.2 never makes TTL an availability authority and §12.1 step 5 converts on successful confirmation; expiry belongs to the SPEC-12-03 reconciler; a captured payment proceeds on the re-checked stock (also pins legacy/no-hold orders verify unchanged); (3) NO StockMovement rows on release/convert — §12.2's text demands none at conversion time; the existing [6.5.17] SALE movement on decrement is kept as-is; release/expiry reconciliation via the ledger is SPEC-12-03's; (4) release sites = the four failed/cancelled checkout paths in scope: 10-04 failed-verify (same transaction as the failed write), stock-conflict 409 (race loser), coupon-conflict 409, admin API cancel. AMENDED (audit cycle 2): the cycle-1 build had declared the admin SURFACE cancel (admin.py `cancel_pending`/change form) out of file scope, leaning on the SPEC-12-03 reconciler for any stale holds; audit cycle 1 returned FIX P1 [R-12.8] — §12.1 step 6 makes "cancelled" a release trigger distinct from "expired", so both admin cancel writers now release too (see the admin.py bullet) and the reconciler stays a pure TTL sweep.
- `orders/views.py` (+112/-2, two declared non-protected line rewrites, everything else insertion-only; all byte-frozen regions — 21-1, 8-01, 8-02b, 8-03, 8-04, SPEC-7-02, R-7.20 — untouched): import line gains `StockReservation` (rewrite #1); `_mint_order_reservations(order, lines, user)` helper above `create_order` — per-line mint inside the caller's atomic block (rollback-together), savepoint-wrapped create so a lost (order, product) race rolls back only the insert, IntegrityError fallback RE-TARGETS the surviving hold (quantity re-armed, fresh TTL, active again) instead of double-creating or crashing — the constraint is the concurrency authority, no check-then-act; `create_order` calls it after the snapshot loop (still inside the atomic block, before the frozen R-7.20 hunk). `verify_payment`: product lock acquisition now `.order_by("id")` (rewrite #2 — closes the section-12 deadlock advisory: one total acquisition order across concurrent verifies, no more rollback-500 loser); conversion insert before the decrement loop (active→converted, idempotent, replay-gated upstream by the already-processed check); releases inserted on the 10-04 failure branch (inside the guarded write), the stock-409 branch and the coupon-409 branch (before their byte-identical returns). `admin_order_cancel`: release inserted inside the atomic block after the audit event (the idempotent replay returns before the site).
- `orders/tests.py` (+389/-2 cumulative): `ReservationLifecycleTests` (12 tests) — per-line active hold with env-driven TTL (override 120s); dedup-replay collapses without touching the hold (re-target-not-double-create at guard level); lost-race re-target via a REAL duplicate mint against the live unique constraint (quantity/TTL/status re-armed, still exactly one row); verify converts + SALE movement pin + replay cannot double-convert; lapsed-TTL conversion; legacy no-hold order verifies; failed-verify releases + retry captures with the hold staying released and stock decremented exactly once; stock-409 releases + restock retry re-checks cleanly; coupon-409 releases; admin cancel releases; AMENDED (audit cycle 2): admin change-form cancel releases the hold + the cancelled self-transition replay never resurrects it; admin bulk cancel_pending releases the hold + a re-run never resurrects it (fixtures check out BEFORE admin login — login() rotates the session key the cart is bound to).
- `orders/admin.py` (+21, audit cycle 2 — BUG-001 P1 fix; views.py untouched): `StockReservation` import + the views.py release idiom (order-scoped `filter(status=ACTIVE).update(status=RELEASED)` — a no-op on already-released holds, so double-cancel cannot resurrect) inserted into BOTH cancel writers inside their EXISTING `transaction.atomic()` blocks, after the transition+audit row and before the side-effect hook: change-form `save_model`'s cancelled-transition branch and `cancel_pending`'s per-row loop — the admin surfaces now match `admin_order_cancel` exactly. `_bulk_set_status` registers no "cancelled" action — no third site.
- `tests/test_e2e_concurrency.py` (+19/-1 cumulative): the oversell race test now also pins the race through the reservation lens — winner's hold converted, loser's released by the 409 backstop, zero active holds survive (import line gains `StockReservation`); audit cycle 2 wraps the 91-col assert continuation to the Black 88-col limit (no behavior change).
- Race/lock semantics: creation lock-free (cart/user row locks already serialize same-session/keyed submissions; the (order, product) constraint backstops the rest); conversion + releases run under the existing Order→Products→Coupon lock order inside verify's atomic block and are single `QuerySet.update()` statements filtered on active; product locks are now acquired in ascending-id order, killing the cross-cart deadlock advisory; no new lock-order cycle (create_order still locks only Cart/User).
- Suite: **638 tests, OK (632 pass + 12 new + 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2511 stmts, 0 miss — grew from 2490), `makemigrations --check` clean (no schema change — no migration). Audit cycle 2: pre-flight AND post-fix full suites both exit 0.

## 2026-09-21 - SPEC-12-03 (Section 12) - builder: expire_reservations management command - scheduled stale-reservation reconciliation via the StockMovement ledger ([R-12.9])

- Spec reading (§12 lines 3579-3633, §12.1 step 7): the reconciler's vocabulary is EXPIRED - the 12-01 Status transition map assigns step 7 to EXPIRED while RELEASED is step 6's in-flow vocabulary (owned by the 12-02 checkout/admin writers), so the sweep flips `status=active AND expires_at past -> expired` (`expires_at__lte`: the hold's TTL instant arrived; tests backdate by minutes so the lte/lt boundary is a declared reading, not a pinned behavior). A lapsed TTL remains a non-gate for verify's conversion (12-02's declared call): a swept checkout's late verify still lands on the re-checked stock - sweep and verify never resurrect each other's rows (conversion filters active; the sweep's UPDATE re-filters active+expiry).
- Ledger reading ([6.5.17] idiom consistency): each affected product lands ONE StockMovement row per sweep with delta=0 BY DESIGN - a hold never left on-hand stock (it gated available-to-sell only; the SALE decrement is verify_payment's conversion), so delta=+qty would corrupt the ledger arithmetic (stock_after = before + delta); the row records reserved units returning to the pool: reason=CORRECTION, stock_after gated on the locked on-hand, order refs in the note, created_by=None (system actor, the SALE idiom). No new Reason choice - choices edits generate an AlterField migration and this task ships none.
- Idempotency/locking: snapshot read (status+expires_at, served by `stockres_status_expires_idx`) -> per product in ascending pk: `select_for_update()` the product row (stock_after gates on the real on-hand; lock order identical to verify_payment's -> no lock-order inversion), then an atomic conditional UPDATE re-filtering status AND expires_at at flip time - a hold re-armed by a checkout retry (fresh TTL, the _mint_order_reservations IntegrityError path) or released by a concurrent writer no longer matches: never double-released, never resurrected; flipped==0 writes no phantom movement. Whole batch in one `transaction.atomic()`.
- `products/management/__init__.py` + `commands/__init__.py` (new, empty): the tree had no management commands yet (no in-repo precedent); BaseCommand style, verbosity-respected stdout, no args, no return value (call_command treats a non-None handle() result as output text).
- `products/management/commands/expire_reservations.py` (+113 new): the sweep + module docstring carrying the coordination prose.
- `tests/test_expire_reservations.py` (+208 new, 6 tests): zero-stale no-op (live hold survives, ledger untouched, verbosity-0 prints nothing); expired hold -> EXPIRED + one delta-0 CORRECTION row (stock_after=current on-hand, order ref in note, created_by None, on-hand unchanged); live + CONVERTED/RELEASED terminal holds untouched (no resurrection); re-run idempotent (exactly one ledger row across two sweeps); one row per product referencing every order + per-product stock_after pin; mid-sweep re-arm race (manager shim simulates the concurrent re-target between snapshot and flip -> raced hold survives unreleased with no phantom row while the clean product's release proceeds).
- `.env.example` (+12): SPEC-12-03 cadence note under RESERVATION_TTL - schedule `python manage.py expire_reservations` well inside the TTL (cron/celery-beat; scheduler wiring owned by S19/S22, none built here); coordination rules: TTL > payment-provider checkout-session window, sweep cadence < TTL; the sweep reclaims the reservation book only - payment truth (webhook/timeout reconciliation, SPEC-1-06/SPEC-1-18) is never touched. No new settings key (cadence is scheduler-side, not app config). orders/views.py and orders/admin.py untouched (protected regions).
- Suite: **644 tests, OK (640 pass = 634 prior + 6 new, 4 pre-existing `expectedFailure` unchanged - ceiling held)**, coverage **100.00%** (2543 stmts, 0 miss - grew from 2511), `makemigrations --check` clean (no schema change - no migration).

## Next (per fix-plan.md)

- Phase 0 remaining: rotate Razorpay keys, add CI.
- Phase 1: webhook + reconciliation + refund path.

## 2026-09-21 — SPEC-17-01 (Section 17) — builder: JWT lifecycle — SIMPLE_JWT config, token-blacklisting logout, session invalidation on password reset [R-17.5/R-17.8/R-17.10]

Per section-17.md SPEC-17-01 row and BACKEND_REQUESTS.md [P1] "SIMPLE_JWT configuration" (the ledger notes this task matches that contract — verified: the suggested contract's 15min+/7d/ROTATE/BLACKLIST shape is exactly what shipped).
- `config/settings.py` — `rest_framework_simplejwt.token_blacklist` added to INSTALLED_APPS (app ships its own migrations 0001–0013; none for our apps). New `SIMPLE_JWT` block: `ACCESS_TOKEN_LIFETIME` = env `JWT_ACCESS_TOKEN_LIFETIME_SECONDS` (default 900s = 15 min), `REFRESH_TOKEN_LIFETIME` = env `JWT_REFRESH_TOKEN_LIFETIME_SECONDS` (default 604800s = 7 days), both integer seconds via the `_env_int` fail-safe pattern; `ROTATE_REFRESH_TOKENS = True` + `BLACKLIST_AFTER_ROTATION = True` — every refresh mints a fresh refresh token and the presented one is blacklisted, so a leaked refresh token cannot be replayed. requirements.txt untouched (simplejwt 5.5.1 already pinned, line 5).
- `accounts/views.py` — new `LogoutView` (DRF APIView, `permission_classes = [IsAuthenticated]`, 'auth' throttle scope): POST with body `{"refresh": "<token>"}` blacklists the presented refresh token (200); missing/non-string/empty `refresh` → 400 field error; invalid/expired token → 400; anonymous → 401 (nothing to revoke, no token-validity oracle). New `_blacklist_user_refresh_tokens(user)` hook [R-17.10 "Session invalidation after critical account changes"]: blacklists every outstanding refresh token for the user, idempotent via `get_or_create`, called inside `reset_password`'s existing `transaction.atomic()` block — old sessions die exactly when the credential change lands. No email-change endpoint exists in the accounts surface today (verified against views.py/urls.py), so the hook is wired at the only critical-change site; it is user-generic and ready for any future change endpoint (declared in the builder report).
- `accounts/urls.py` — `logout/` route (serves both `/api/accounts/logout/` and the v1 alias `/api/v1/account/logout/` via the shared urlconf include).
- `.env.example` (+12) — the two JWT lifetime keys with defaults and the coordinate-the-pair note (access = stolen-bearer usability bound; refresh = sign-in duration bound).
- `accounts/tests.py` (+11 tests: config pin ×3, logout ×4 incl. anonymous-401 permission test, rotation/invalidation ×1, reset-hook ×3 incl. bystander-isolation) and `tests/test_settings_security.py` (+4: env-override lifetimes, default lifetimes, malformed-value fallback, rotation/blacklist flags + app presence — subprocess, clean environment). Throttle wiring guard extended: `logout` joins the pinned scope map.
- Suite: **659 tests, OK (655 pass = 644 prior + 15 new, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2567 stmts, 0 miss — grew from 2543), `makemigrations --check` clean, token_blacklist migration dance on a scratch DATABASE_URL sqlite: forward OK / reverse-to-zero OK / re-apply OK.

## 2026-09-21 — V-02 (Section 16/17 rider) — builder: DEBUG fails closed by default, pin inverted

Per the pinned flip instruction at tests/test_settings_security.py (and the section-17.md owner-attribution note: the S17 build loop owns V-02).
- `config/settings.py` — `DEBUG = os.getenv('DJANGO_DEBUG', 'false').lower() == 'true'`: an absent DJANGO_DEBUG now means DEBUG=False (fail-closed), so an unconfigured deployment boots hardened and, per the pre-existing guard, is refused outright without a real DJANGO_SECRET_KEY. The dev-only `unsafe-development-key-change-me` default stays available only when DEBUG is explicitly true.
- `tests/test_settings_security.py` — the V-02 pin inverted per its own docstring instruction (`test_v02_debug_fails_closed_by_default` asserts `DEBUG_IS False` via subprocess, secret key provided so the import clears the now-active DEBUG-false guard); new companion `test_v02_debug_absent_and_secret_absent_is_refused` observes the fail-closed default through the guard itself (the SECRET_KEY refusal only fires under a falsy DEBUG, so the refusal proves the default flipped). The four subprocess `DatabaseUrlImportTests` boot with a development DJANGO_SECRET_KEY — their subject is DATABASE_URL parsing, not DEBUG, and a clean-environment import needs the key now that the default is fail-closed. CI unaffected (workflow already sets DJANGO_DEBUG=true).
- Test-env consequence (worktree-only, git-ignored, NOT committed): the bare `manage.py test` boot from a checkout without any .env now needs config — created `backend/.env` in the spec-comp worktree from the documented `.env.example` workflow (DJANGO_SECRET_KEY dev value + DJANGO_DEBUG=false). Verified the subprocess pin tests' neutral-cwd isolation still holds (python-dotenv does not discover the worktree .env from the `-c` snippets — the full settings-security class passes unchanged).
- `.env.example` — DEBUG doc line now states the fail-closed default and the guard interaction.
- Suite: **660 tests, OK (656 pass = 659 prior - 1 flipped pin + 2 new, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2567 stmts, 0 miss), `makemigrations --check` clean.

## 2026-09-21 — SPEC-17-02 (Section 17) — builder: refresh token moved to HttpOnly backend-set cookie; localStorage persistence removed [R-17.12, DEVIATES row]

Per the section-17.md SPEC-17-02 row and spec §17.1 lines 4267–4269 ("use secure, HttpOnly cookies where appropriate ... Do not store long-lived authentication tokens in browser local storage by default"). Cookie contract: `Set-Cookie: refresh_token=<jwt>; Path=/api/; HttpOnly; Secure (when not DEBUG); SameSite=Lax; Max-Age=<REFRESH_TOKEN_LIFETIME>`. SameSite reading declared: **Lax** — the cookie's only consumers are POST endpoints (refresh/logout), which Lax never serves cross-site, so the cookie itself is CSRF-immune without Strict's navigation stranding; it matches the same-site frontend/API deployment constraint BACKEND_REQUESTS.md [P1] already documents for the session cookie, and explicit CSRF enforcement for cookie-authenticated mutations stays SPEC-17-03's follow-up. All three knobs env-driven (`JWT_REFRESH_COOKIE_NAME/PATH/SAMESITE`).
- `config/settings.py` (+16) — the three cookie settings with documented defaults (Path `/api/` covers both mounts of accounts/urls.py: legacy `/api/accounts/` and v1 `/api/v1/account/`; Secure follows DEBUG, fail-closed per V-02).
- `accounts/views.py` (+122/-13) — `_set_refresh_cookie(response)`: moves the minted refresh token out of the JSON body (the JS-readable leak surface) into the HttpOnly cookie, max-age synced to the token lifetime so the browser never presents a cookie older than the token it holds; `_clear_refresh_cookie(response)`: expires it in place with matching Name+Path. `LoginView.post` wires the helper on success (audit trail unchanged); `RefreshView` moved here from urls.py: body token still accepted for non-browser API clients, otherwise the cookie token is merged into the parsed JSON body in place (DRF 3.18 exposes no `request.data` setter; immutable QueryDict bodies excluded — the browser client is JSON-only); on success the ROTATED token re-enters the cookie, never the body; `handle_exception` override sweeps the cookie on a 401 authentication verdict only (429 throttle refusals are not a verdict on the token — the cookie must survive shared-budget refusals). `LogoutView.post`: cookie is the token source for browser clients (body fallback kept for API clients), blacklist + cookie clear on 200; a garbage/dead cookie token gets the same 400 field error (envelope shape unchanged) but the cookie is swept so a dead token cannot pin the browser.
- `accounts/urls.py` (-14) — `RefreshView` now imported from views (cookie logic lives with the views); routes unchanged.
- `.env.example` (+13) — the three cookie knobs, the mount-coverage note for PATH, the Secure-follows-DEBUG rule, and the tighten-to-Strict caveat.
- `frontend/src/lib/tokens.ts` — `REFRESH_STORAGE_KEY`/`aurel.refresh` localStorage persistence deleted; `setTokens(access)` is memory-only; `getStoredRefreshToken` removed; `doRefresh` posts an empty body with `credentials: 'include'` (the cookie is the credential) and clears the session state on a server rejection (transient network failures keep it). New `aurel.session` localStorage key is a NON-SECRET boolean hint (never a token) gating the boot-time refresh probe so anonymous page loads don't burn the 'auth' throttle budget.
- `frontend/src/lib/auth.tsx` — login consumes `{access}` only (no body refresh); bootstrap probes refresh when the hint says a cookie may exist; logout now calls `POST /api/accounts/logout/` (auth'd, best-effort) so the backend blacklists the cookie token and clears the cookie — the browser can no longer present the token itself. `api.ts` untouched: the refresh-call credentials path was already `include` and the X-CSRFToken slot stays 17-03's scope.
- `frontend/src/lib/api.test.ts` (+8/-8) — the three silent-refresh tests re-pinned from localStorage seeding to memory token + hint, plus cookie-contract pins (credentials include, empty body, no token persisted); `tokens.test.ts` (new, 7 tests) pins the storage contract: no token value ever written to localStorage (only the "1" hint), memory-only access token, hint lifecycle (set on session, cleared on server-rejected refresh, kept on network failure), single-flight, empty-body refresh shape.
- `accounts/tests.py` (+9 tests / 1 re-pinned): cookie flags on login (HttpOnly/Secure-DEBUG-False/SameSite/Path/max-age-synced, body `refresh` dropped) incl. the DEBUG=True Secure-off flip; refresh-via-cookie rotation (rotated token re-set in cookie, body access-only, original blacklisted, session continuity, dead-cookie sweep); refresh without cookie or body → 400; logout-with-cookie blacklist + clear; garbage-cookie logout reject + sweep. `RefreshRotationTests` re-pinned to the cookie carrier (17-01 semantics preserved: body-presented token still rotates + blacklists); `test_login_rate_limit_engages` re-pinned from body-refresh to cookie. Settings-defaults pin class added.
- Suite: **668 tests, OK (664 pass = 656 prior + 8 net new accounts tests, 4 pre-existing `expectedFailure` unchanged — ceiling held)**, coverage **100.00%** (2592 stmts, 0 miss — grew from 2567), `makemigrations --check` clean (no schema change). Frontend: vitest **31 tests passed (6 files)**, `next build` exit 0.

## 2026-09-21 - SPEC-17-03 (Section 17) - builder: CSRF enforced on session-cookie cart/checkout mutations; SPA X-CSRFToken slot activated ([R-17.18])

- Spec reading: section 17.3 risk table, CSRF row (lines 4318-4322) "Appropriate CSRF protection for cookie-based state-changing requests" + 17.1 line 4267 ("Carefully configure SameSite, HTTPS and CSRF protection based on the authentication architecture"); the exploitable surface is BACKEND_REQUESTS.md [P1] lines 43-48 (live-confirmed: POST/DELETE on /api/cart/ with only the session cookie and no token returned 201/200).
- Enforcement mechanism chosen: `common/authentication.py` (new) - `SessionCartCSRFAuthentication(SessionAuthentication)` in DEFAULT_AUTHENTICATION_CLASSES AFTER `JWTAuthentication`. Why a subclass rather than stock SessionAuthentication: DRF 3.18.1's stock class returns None for anonymous users ("Unauthenticated, CSRF validation not required"), which would have left the GUEST session cart - the exact live-confirmed exploit - unprotected; the subclass extends `enforce_csrf` to every request that presents the session cookie (named user or anonymous guest) while returning None in all paths (a pure gate: JWT stays the only credential in the chain, so no permission surface changes and bearer requests short-circuit the chain before the gate - an Authorization header is not an ambient credential, nothing to forge). Requests with no session cookie (fresh visitor's first mutation, non-browser API clients, fresh login) are ungated by design.
- CSRF-cookie issuance reading: `cart/views.py` `cart_detail` GET decorated `@ensure_csrf_cookie` - GET /api/cart/ is the SPA's boot call (Header's cart badge react-query runs it on every page load), so the browser holds the csrftoken cookie before the first gated mutation and api.ts replays it as X-CSRFToken (double-submit). `config/urls.py` untouched.
- Cookie-flag reading: `SESSION_COOKIE_SAMESITE` now explicit, env-driven, default 'Lax' - aligned with 17-02's JWT_REFRESH_COOKIE_SAMESITE reading (cross-site POSTs cannot attach the cookie; the server-side gate is the enforcement, SameSite is the same-site belt). CSRF_TRUSTED_ORIGINS already env-driven (pre-existing) covers HTTPS Referer/Origin checking for the deployed storefront origin.
- Frontend: the api.ts X-CSRFToken slot (lines 29-39, 118-121) was discovered ALREADY WIRED (reads the csrftoken cookie, sends the header on POST/PATCH/PUT/DELETE) - 17-03's frontend work was therefore: exported `csrfTokenFromCookie` from api.ts (stale "backend never issues one" docstring rewritten), and `tokens.ts` `doRefresh` now attaches the same header - closing a real 17-02 interplay gap: a browser that built a guest cart sends its sessionid cookie on the refresh POST too, so without the header the gate would 403 the refresh and spuriously log the user out.
- `tests/test_csrf_security.py` (SPEC-21-2 security-truth layer) FLIPPED per its own instruction ("when SPEC-17-03 lands ... updating this module is the deliberate contract update, not an accident"; same pattern as the V-02 flip): CSRF half now pins the enforced truth (tokenless enforcing client -> 403, valid pair -> 201, cookie issuance on the cart GET, suppression-flag differential), wiring pin asserts [JWTAuthentication, SessionCartCSRFAuthentication], JWT-bearer money-path pins re-documented as designed behavior (unchanged assertions); XSS half untouched.
- `cart/test_csrf.py` (new, 11 tests): Django's hermetic CSRF recipe (enforce_csrf_checks=True + token pair acquired through the API); guest enforcement (add no-token 403 with NO CartItem written / valid token 201 / mismatched token 403 / PATCH+DELETE enforced); orders-family apply-coupon covered by the global chain WITHOUT touching orders/views.py; fresh-visitor first mutation ungated; Django-session-authenticated user enforced (stock half); JWT-bypass pin (login -> session cart -> checkout 201 with no CSRF header); 17-02 interplay pin (login/refresh/logout all 200 with session cookie present + header); fresh login without any cookie ungated.
- `tests/test_settings_security.py` (+2, subprocess pattern): SESSION_COOKIE_SAMESITE defaults to 'Lax' and honors the env override.
- `.env.example` (+11): SESSION_COOKIE_SAMESITE block - the belt-vs-gate reading, the GET /api/cart/ issuance contract, JWT-never-gated note, CSRF_TRUSTED_ORIGINS deployment note.
- Suite: **681 tests, OK (677 pass + 4 pre-existing `expectedFailure` unchanged - ceiling held at 4)**, coverage **100.00%** (2707 stmts, 0 miss - grew from 2592), `makemigrations --check` clean (no schema change). Frontend: vitest **36 tests passed (6 files, +5 CSRF pins)**, `next build` exit 0.

## 2026-09-21 - SPEC-17-05 (Section 17) - builder: mandatory TOTP MFA for privileged roles at the admin door + staff API login ([R-17.9])

- Spec reading: 17.1 line 4263 "Staff MFA, preferably mandatory for privileged roles" - a single line naming no mechanism, no artefact and no package (no QR image anywhere in the section), so the factor is built on a stdlib RFC 6238 TOTP implementation (hmac/hashlib/base64/secrets, ~90 lines, pinned against the public RFC 6238 SHA-1 appendix-B vectors) plus an otpauth:// enrollment URI STRING (not a QR image). No dependency was added. Privileged = `staff.manage` capability holders (`admin` role per CAPABILITY_ROLES) plus Django superusers - the trust anchor every admin surface preserves a bypass for must not escape the factor that anchors it (`common/permissions.py`: `is_privileged` + `IsPrivilegedRole`).
- `common/totp.py` (new): RFC 4226 HOTP (SHA-1, dynamic truncation) under the RFC 6238 30s step, 160-bit base32 secret generation, `otpauth://` URI (issuer:account label, literal colon delimiter), and `verify_code` with a +-1 step drift window and the RFC 6238 section 5.2 replay guard (a counter at or below the stored watermark never verifies again). Step/digits/window are code constants on purpose: a deployment-tunable drift between server and enrollment URI would silently break every staff login.
- `accounts/models.py`: `TOTPDevice` (OneToOne user, base32 secret, enabled flag, confirmed_at, last_used_counter replay watermark) + the shared user-facing MFA messages. Enforcement gate = enabled AND confirmed; setup mints an unconfirmed disabled row. Migration `accounts/0001_initial`. Secret exposure discipline: returned exactly once by setup, never by status/confirm/disable, never in `__str__`, never logged.
- `accounts/serializers.py` + `accounts/views.py`: `LoginView` now runs `MFATokenObtainPairSerializer` - privileged users must present a valid `totp` code in the login body (BACKEND_REQUESTS.md defines no MFA contract; the simpler single-request field was chosen and declared); missing code -> 400 "Enter your authentication code.", wrong/expired/replayed -> 400, unenrolled privileged -> 400 with the enrollment path named; every rejection lands in the existing LoginView audit path as AUTH_LOGIN_FAILED and no tokens are ever minted without the factor. Non-privileged logins (customers, the five non-admin staff roles) are byte-identical to before, extra `totp` field ignored.
- Enrollment API (all 'auth'-throttled, mounted in both route families via the shared urlconf): `GET /api/accounts/mfa/status/` (privileged-only, `{enabled}` only), `POST /mfa/setup/` (secret + otpauth URI shown once; re-enrollment while a device is active re-proves the factor), `POST /mfa/confirm/` (valid code flips enabled), `POST /mfa/disable/` (valid code strips the factor). Setup/confirm run a DUAL trust path, declared: the privileged JWT session (steady state) OR a credential bootstrap (username+password) available exactly while no device is active - the rollout path that keeps blocked-at-login enforcement reachable, because a privileged account with no device can never obtain a JWT (industry-standard first-factor-enrollment window: once a device is active, credentials alone are refused with 403; an attacker with only a password is exactly where the pre-MFA world was, and strictly worse off against every enrolled account).
- Admin surface: `config/admin.py` (new) swaps the default admin site via `MFAAdminConfig.default_site` -> `MFAAdminSite` whose login form is `MFAAdminAuthenticationForm` (`accounts/admin.py`): privileged users need a valid TOTP code, unenrolled privileged accounts are refused with the enrollment path named, `login_form` resolves lazily to avoid re-entering accounts.admin during app-registry bootstrap. `templates/admin/login.html` overrides the stock template solely to render the code field (stock template hardcodes username/password rows only); kept byte-faithful otherwise. `config/settings.py`: `django.contrib.admin` -> `config.admin.MFAAdminConfig` (same app label, same registration flow, all existing `admin.site._registry` pins hold).
- Rollout semantics (declared): mandatory means mandatory - an unenrolled privileged account is blocked at BOTH surfaces until enrolled, with the enrollment path named in the error; recovery is self-service through the credential bootstrap (setup -> confirm with the fresh secret's code). Disable under a mandatory policy blocks the next password-only login until re-enrollment (pinned by test).
- `common/testing.py`: `api_login` lazily ensures the fixture device (RFC vector secret, recognizably a test constant) and attaches a fresh code, rewinding the replay watermark one step so the suite's staff logins survive the replay guard across the 30s window; `make_staff` unchanged.
- `accounts/tests.py`: URLConf throttle-wiring guard's expected route->scope map extended with the four MFA routes ('auth').
- `tests/test_mfa.py` (new, 52 tests): RFC 6238 vectors (8- and 6-digit) + every verify/replay branch; privileged predicate matrix; enrollment happy/malicious paths on both trust paths; secret-shown-exactly-once pin; disable flow; API-login enforcement pins (missing/invalid/expired/replayed codes, watermark persistence, unenrolled block, audit trail, customer + non-admin-staff + superuser differentials); admin-login enforcement pins (field rendered, code required/invalid/unenrolled, same-code replay across clients, roleless-staff unaffected, superuser enforced); admin-site wiring pin.
- Frontend (minimal, backend-enforced contract): `LoginForm.tsx` + `lib/auth.tsx` gain one labeled optional "Authentication code" input (inputmode=numeric, autocomplete=one-time-code, WCAG-labelled) sent only when filled; a 400 `totp` field error surfaces its message (names the enrollment path). No e2e change (e2e exercises a customer login, unaffected).
- Suite: **733 tests, OK (729 pass + 4 pre-existing `expectedFailure` unchanged - ceiling held at 4)**, coverage **100.00%** (2929 stmts, 0 miss - grew from 2707), `makemigrations --check` clean (accounts/0001_initial only). Frontend: vitest **36 tests passed**, `next build` exit 0.

## 2026-09-22 - SPEC-17-07 (Section 17) - builder: env-gated transport/cookie hardening flags [R-17.11]

- Spec reading: 17.1 "Carefully configure SameSite, HTTPS and CSRF protection based on the authentication architecture" - the settings carried ZERO SECURE_*/HSTS/cookie-secure flags (implicit Django defaults), so the hardening was unreachable from deployment config. Coordinates with the S22 loop, which owns the production TLS topology: this task makes the hardening REACHABLE, never on where it would break plain-HTTP local development.
- config/settings.py: new `_env_bool` helper (truthy spellings 1/true/yes/on, everything else falls back to the documented default - the same fail-safe pattern as `_env_int`, never crashing startup). Added: SECURE_HSTS_SECONDS (default 0 = header unset; enabling HSTS on a partly-HTTP host locks users out for the declared window), SECURE_HSTS_INCLUDE_SUBDOMAINS/SECURE_HSTS_PRELOAD (default false), SECURE_SSL_REDIRECT (default false), SECURE_PROXY_SSL_HEADER assembled from SECURE_PROXY_SSL_HEADER_NAME/_VALUE - unconfigured unless BOTH parts are set, because a half-trusted scheme header is worse than none (a forgeable pair lets clients lie about their scheme). SESSION_COOKIE_SECURE/CSRF_COOKIE_SECURE default to `not DEBUG`, mirroring the JWT refresh cookie's secure=not-DEBUG contract in accounts/views.py so the three auth cookies can never disagree about transport security (the cart's csrftoken cookie and CSRF_COOKIE_SECURE flip together, keeping SPA form posts alive over HTTPS).
- tests/test_settings_security.py: TransportHardeningTests (8 tests) on the existing subprocess recipe - safe-off defaults, env-on flips, proxy header pair/half-configured, cookie-secure follows DEBUG for local dev, malformed HSTS seconds and malformed bool fall back to defaults. The truthy branch of `_env_bool` is covered in-process via the pure helper with a temporarily-set env var.
- .env.example: SPEC-17-07 block documenting every flag, the ordering constraints (preload effectively irreversible; subdomains only once every subdomain is HTTPS), the proxy-header trust warning, and the cookie-secure default rationale.
- Suite: **740 tests, OK (736 pass + 4 pre-existing `expectedFailure` unchanged - ceiling held at 4)**, coverage **100.00%** (2943 stmts, 0 miss - grew from 2929), `makemigrations --check` clean.

## 2026-09-22 - SPEC-17-08 (Section 17) - builder: product image upload size cap [R-17.21]

- Spec reading: 17.3 application-security table row "File upload abuse -> Type/size validation, safe storage, scanning where appropriate". The type half was already covered (ImageField's Pillow check, products/models.py); the size half was absent - no cap existed on any surface.
- Entry surfaces found (both enforced): (1) the staff REST API - POST/PUT/PATCH /api/products/ accept `image` through ProductSerializer (DRF ModelSerializer field); (2) the admin - products.products add/change pages (django ModelForm via ProductAdmin's Image fieldset). There is no other upload path (no user-generated images, no other model carries an ImageField).
- products/models.py: `validate_image_size` model-field validator, so ONE enforcement point reaches both surfaces automatically (DRF copies model-field validators onto serializer fields; ModelForm validates them natively). Size is read via getattr(handle, "size") falling back to seek(0, SEEK_END)/tell - upload wrappers differ (BytesIO under the 2.5 MB in-memory threshold has no .size; TemporaryFile past it) and a naive .size read 500s the boundary case. Rejects with ValidationError -> 400 "Image exceeds the maximum upload size of N MB." for the API (uniform error envelope, `image` in details) and a form error on the admin page; rejected uploads write nothing to storage. Cap = MAX_UPLOAD_MB * 1024 * 1024, env-driven via the fail-safe `_env_int` pattern (default 5), settings.MAX_UPLOAD_MB.
- config/settings.py: MAX_UPLOAD_MB (5); .env.example documents it, the both-surfaces enforcement, and the web-server body-limit interplay (proxy/DATA_UPLOAD_MAX_MEMORY_SIZE fires first with its own 413).
- products/migrations/0010_alter_products_image: field-level validators are part of the model definition, so the alteration is generated and applied (no schema/data change, safe no-op at the database level).
- products/tests.py: ImageUploadSizeCapTests (6 tests) - oversized API rejection (400, image in details, no row created), at-cap pass (payload trimmed by the TINY_PNG header so the file body lands exactly on the limit), no-image passthrough, empty-value validator passthrough (covers the guard branch), env override tightening to 1 MB (message reflects the new cap), admin form rejection (re-render with the error, image column stays empty).
- Suite: **746 tests, OK (742 pass + 4 pre-existing `expectedFailure` unchanged - ceiling held at 4)**, coverage **100.00%** (2959 stmts, 0 miss - grew from 2943), `makemigrations --check` clean.

## 2026-09-22 - SPEC-17-10 (Section 17) - builder: ops dashboard gated by reports.read capability [RBAC least privilege]

- Spec reading: 17.2 "Use server-side RBAC" / least privilege - the ops dashboard (route /admin/dashboard, mounted in both families) rendered revenue, AOV and customer rows to ANY staff account via a blanket @staff_member_required, the one chrome route the SPEC-6-03 capability layer never reached (the sibling audit-log route already used capability_required).
- ops/views.py: @staff_member_required -> @capability_required("reports.read") - the repo's canonical capability (CAPABILITY_ROLES grants it to finance, marketing and admin; HasReportsRead exists for DRF views, capability_required is its admin-chrome twin for session views). Deny shape matches the audit-log route exactly: anonymous -> redirect to /admin/login/, authenticated staff without the capability -> visible 403 (never a login loop, never a silent empty page), superuser bypass preserved, the five non-grantee roles (support, catalogue, inventory and role-less staff) can no longer see revenue/customer aggregates. The stale staff_member_required import is removed.
- ops/tests.py: DashboardCapabilityGateTests (6 tests) - anonymous redirect to admin login, all three grantee roles render (subTest per role), all three non-grantee roles 403, role-less staff 403, superuser bypass renders, and the /api/v1/admin/dashboard/ dual mount inherits the same gate (SPEC-9-02 same-view-object guarantee).
- Suite: **752 tests, OK (748 pass + 4 pre-existing `expectedFailure` unchanged - ceiling held at 4)**, coverage **100.00%** (2958 stmts, 0 miss - net -1 from the removed import line), `makemigrations --check` clean.

## 2026-09-22 - SPEC-17-06 (Section 17) - builder: JSON-LD script-context escaping + baseline security headers [R-17.17]

- Spec reading: 17.3 table row "XSS -> Output encoding, safe rich-text handling, CSP" (R-17.17). Two halves shipped together: the sink (staff-controlled product.name/description embedded into two <script type="application/ld+json"> blocks via raw JSON.stringify on the product page) and the missing CSP/headers baseline.
- frontend/src/lib/json-ld.ts (new): `toJsonLdScriptContent` - JSON.stringify then escapes `<` `>` `&` U+2028 U+2029 as their \uXXXX forms. `</script>` (and `<!--` openers) in a product name can no longer terminate the script element early; the escapes are JSON-transparent (JSON.parse decodes them back to the identical string), so the LD+JSON payload is semantically unchanged for crawlers. `&` is included because HTML-entity decoding inside script-adjacent contexts can otherwise re-introduce `<`.
- frontend/src/app/products/[slug]/page.tsx: both JSON.stringify call sites (product + breadcrumb JSON-LD) route through the helper. frontend/src/app/page.tsx: the homepage Organization JSON-LD also routes through it (corrected in audit cycle 2 — raw JSON.stringify here was missed in cycle 1, so the ld+json sinks closed by this task are TWO: products/[slug] product + breadcrumb blocks, AND the homepage Organization block; grep re-verified those are all of them).
- frontend/src/lib/json-ld.test.ts (new, 5 tests): literal `</script><script>alert("xss")</script>` product name serializes with no `</script>`/`<script` in the output while JSON.parse recovers the exact payload; `a<b>c&d` escapes to the exact expected string; U+2028/U+2029 cases; nested-object/list traversal; innocent payloads byte-identical to plain JSON.stringify. Audit cycle 2 adds a homepage-payload escape pin (6 tests total), mirroring page.tsx's Organization payload shape.
- frontend/next.config.ts: headers() applying to every route - CSP baseline `default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; font-src 'self'; img-src 'self' <API origin> data:; connect-src 'self' <API origin>; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; upgrade-insecure-requests`. Choice declared: 'unsafe-inline' stays in script-src because Next's App Router injects inline bootstrap scripts that cannot be nonced without forking the document render; fonts are self-hosted via next/font (no fonts.googleapis.com connect/style needed); API origin (env-driven NEXT_PUBLIC_API_BASE_URL) is added to img-src/connect-src because product media and all fetches target it. Plus X-Content-Type-Options: nosniff, Referrer-Policy: strict-origin-when-cross-origin, X-Frame-Options: DENY (legacy-browser belt to frame-ancestors 'none'), Permissions-Policy camera/microphone/geolocation denied.
- Verification: vitest 41 passed (36 -> 41), `npm run build` green with the headers config active.

## 2026-09-22 - SPEC-17-09 (Section 17) - builder: retention/erasure field-purpose register + audit-logger username decision [R-17.29/31/32/33]

- Spec reading: 17.4 privacy bullets - document why each field is collected (R-17.29, line 4411), establish retention/deletion policies (R-17.31, line 4415), avoid personal information in logs (R-17.32, line 4417), support customer data access/deletion rights (R-17.33, line 4419).
- backend/docs/retention.md (new): the field-purpose/retention/erasure register enumerating the ACTUAL collected PII per app (grep-verified against models, nothing invented): accounts (User.username/email/password, TOTPDevice.secret), orders (Order.full_name/phone/address/city/state/pincode, user FK, razorpay gateway ids - with the no-card-data note), cart (session_id: opaque token, non-personal), audit trails (AuditEvent.detail/actor, admin LogEntry, app log output) - each row with purpose, retention, erasure path. "Not collected" rows state the absence explicitly (no legal name/DOB/marketing surface/guest email yet, with a SPEC-3-02 warning for guest checkout).
- SPEC-6-10 boundary declared: section-19's note assigns "R-19.36 data-deletion request processing" to SPEC-6-10 (customer data export/deletion workflow owner, PENDING). This task therefore builds NO parallel deletion-request endpoint (that would duplicate 6-10's surface); it documents the boundary + the working manual erasure path (support email -> admin mechanics per data class: user-row delete with its cascade/null behaviour, in-window orders deidentified by blanking the six address fields, financial columns retained) that 6-10's self-service workflow will replace.
- R-17.32 username decision, implemented: RETAIN usernames in AuditEvent.detail and the common.audit log mirror - justification: accountability/forensics need a human-meaningful stable identifier, a username is the login credential (not sensitive PII), the no-secrets log pin already guards credentials, and detail.username cannot be re-associated with an erased person once email/address (account-row-only data) is gone. Pinned as a code comment at the emission site (common/models.py AuditEvent.record) with the forward guard: email/phone/address/full name are forbidden in log output. One-liner test surface unchanged (no behavior change, 752 tests still pass untouched).
- Suite: **752 tests, OK (748 pass + 4 pre-existing `expectedFailure` unchanged - ceiling held at 4)**, coverage **100.00%** (2958 stmts, 0 miss), `makemigrations --check` clean.

## 2026-09-23 - SPEC-19-2 (Section 19) - builder: admin alert dispatch reusing existing detection [R-19.15/20/21/27 alerting halves]

- Spec reading: §19.2 admin notifications; the ledger row scopes 19-2 to the ALERTING halves of R-19.15 (inventory attention), R-19.20 (repeated payment failures), R-19.21 (background/process failures surfacing as integration outages), R-19.27 (security-sensitive account changes) — the notification halves of R-19.3..14 stay with SPEC-1-12/SPEC-3-14 per the owner attributions below the table.
- backend/ops/alerts.py (new): five alert types with one dispatch function each — low_stock, out_of_stock, payment_failure_spike, security_alert, integration_outage. ALL sends ride the SPEC-19-1 single send path (`common.notifications.send_email`; zero new send_mail calls — the R-19.0 conformance point). Recipients = `ALERT_RECIPIENTS` env (comma-separated, parsed/deduped; EMPTY disables admin alerts entirely — the earlier staff@… fallback idea rejected as a hardcoded address, conventions.md). Dedupe = per-alert-type cooldown in the default cache: `ALERT_COOLDOWN_SECONDS` (default 300s) — the /health/ probe and dashboard load are pollable triggers, so without the window a degraded store would email-bomb the staff inbox; suppressed sends are INFO-logged, not silent. Send failure = log-only (`logger.exception` on the dedicated `ops.alerts` channel), identical to dispatch's contract: an alert can never break the flow that tripped it.
- Declared spike rule ([R-19.20], spec names the alert class but no number): ≥ `PAYMENT_FAILURE_SPIKE_COUNT` (default 3) failed-payment attempts within trailing `PAYMENT_FAILURE_SPIKE_WINDOW_SECONDS` (default 300) fire one spike alert, then the cooldown applies (the condition stays true until the burst ages out). Detection reads the AuditEvent trail (`payment.signature_rejected/order_not_found/reference_mismatch/stock_conflict/coupon_invalid`) — the verify-failure branches already record exactly these beside their warnings, so orders/views.py stays untouched (frozen; the audit hook IS the existing seam).
- Trigger wirings (insertion at existing detection sites only): ops/services.py `check_stock_alerts()` (low/out-of-stock sets from the same LOW_STOCK_THRESHOLD definition the dashboard shows; called from the dashboard view and exported for pollers), ops/views.py `health` (degraded → integration_outage alert, best-effort detail rendering — the error-envelope seam substitutes arbitrary 503 bodies, so the alert can never 500 the probe; the payload also carries the `stock_alert_checked` key (ops/services.py:141), always False in get_health's own output because the stock-breach dispatch is deliberately outside the probe — a raise inside it must never flip /health/ into a 503 — and fires beside the dashboard's `check_stock_alerts()` call and for pollers), accounts/views.py `reset_password` (security_alert beside the AUTH_PASSWORD_RESET audit hook, username-only identity line per the R-17.32 allowlist), ops/views.py `dashboard` (payment_failure_spike via ops/alerts.py `check_payment_failure_spike` — audit c1 BUG-1's production call site, wired beside `check_stock_alerts()` mirroring the stock-alert poll symmetry; the detector (ops/alerts.py) reads the AuditEvent trail (`payment.signature_rejected/order_not_found/reference_mismatch/stock_conflict/coupon_invalid`) and skips the query entirely when recipients are unconfigured, so an unconfigured dashboard load stays cheap).
- templates/emails/alert_*.txt (new, 5): plain-text staff alert bodies. config/settings.py + .env.example: ALERT_RECIPIENTS / ALERT_COOLDOWN_SECONDS / PAYMENT_FAILURE_SPIKE_COUNT / PAYMENT_FAILURE_SPIKE_WINDOW_SECONDS documented.
- Tests: backend/tests/test_alerts.py (new, 21) — per-type fire + outbox assertions (locmem), recipients parsing/disabled-alerts, per-type cooldown pin + expiry + suppression logging, spike threshold/window-edge (out-of-window failures don't count), no-recipients negative paths (incl. spike query skipped), end-to-end password-reset → security alert, degraded /health/ → outage alert, send-failure log-only contract.
- Suite: **773 tests, OK (769 pass + 4 pre-existing `expectedFailure` unchanged - ceiling held at 4)**, coverage **100.00%** (3048 stmts, 0 miss), `makemigrations --check` clean.

## 2026-09-23 - SPEC-19-4 (Section 19) - builder: back-in-stock opt-in + restock trigger [R-19.11]

- Spec reading: §19.1 table "Back-in-stock event -> Optional opt-in notification" — the customer opt-in model, the REST opt-in/opt-out surface, and the trigger hook on stock edits; the trigger reuses existing detection (adjust_stock, the SPEC-6-02 sanctioned inventory mutation used by both the admin action and the REST inventory endpoint — zero edits to frozen orders/views.py).
- products/models.py — `RestockNotification` (new model): user FK + product FK (CASCADE), `active` flag, `notified_at` spend stamp, created/updated; `UniqueConstraint(user, product)` (`uniq_user_product_restock_optin`) as the concurrency authority (no check-then-act); composite index `(product, active, notified_at)` = the trigger's exact lookup. Lifecycle semantics: opt-in arms (active=True, notified_at NULL); the restock trigger emails once and stamps notified_at (spent); a sell-out crossing (stock positive -> 0, in adjust_stock) RE-ARMS spent rows (clears notified_at) so the next 0 -> positive restock re-mails — one email per stock cycle; explicit opt-out (active=False) never mails; re-opt-in re-arms.
- Trigger hook: `adjust_stock` fires `notify_back_in_stock` on the 0 -> positive crossing (old-stock condition checked first so the query only runs on real restocks) and `rearm_restock_notifications` on the positive -> 0 crossing — both inside adjust_stock's existing atomic block (rollback-together, the StockMovement pattern; a forced post-send failure rolls back stock, movement AND the spent stamp — pinned by test). Sends ride the SPEC-19-1 single send path (`common.notifications.send_email`, template `back_in_stock.txt` — no new send path); per-recipient send failures are logged on the dedicated `products.restock` channel and skip only that row's stamp (retried on the next trigger); the block never raises (notification outage cannot fail the inventory write, the alert/dispatch log-only contract).
- products/restock_views.py (new) — POST/DELETE `/api/products/<slug>/restock-notifications/`: `IsAuthenticated`, ownership = `request.user` filter (no body user field), `restock` throttle scope (`THROTTLE_RESTOCK_RATE`, default 5/min — recovery-shaped budget: each opt-in is intent to receive outbound email). Opt-in refused 400 while in stock (nothing to notify); accepted idempotently via get_or_create + re-arm on re-opt-in (201 created / 200 re-armed); DELETE deactivates with a uniform 200 whether or not the row exists (no existence leak). Wired in products/urls.py ahead of the slug catchall (mounted at /api/products/, /api/v1/store/products/, /api/... aliases — no new urlconf).
- Migration: products/migrations/0011_restocknotification.py (Create model RestockNotification + constraint + index). Dance: forward 0000->0011 on scratch sqlite, unapply 0011 (reverse drops the table), re-apply — all clean; scratch DATABASE_URL used, real db untouched.
- Tests: products/test_restock.py (new, 20) — opt-in/out lifecycle incl. auth (401), in-stock refusal, idempotent re-arm, uniform 404/DELETE, ownership scoping, unique-constraint IntegrityError pin, str; trigger: fires per armed opt-in on 0->positive (admin adjust_stock + REST inventory/adjustments paths), spent rows silent without sell-out, re-arm after sellout, non-crossing top-ups and sale decrements never fire, send-failure log-only + non-blocking for remaining recipients, rollback-takes-the-stamp, duplicate-send pin.
- Suite: **793 tests, OK (789 pass + 4 pre-existing `expectedFailure` unchanged - ceiling held at 4)**, coverage **100.00%** (3287 stmts, 0 miss), `makemigrations --check` clean.
